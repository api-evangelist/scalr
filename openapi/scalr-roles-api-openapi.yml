openapi: 3.2.0
info:
  title: Scalr Roles API
  version: 1.0.0
  description: 'Operations tagged Roles across 3 of this provider''s published API definitions: scalr-account-openapi.yml, scalr-global-openapi.yml, scalr-user-openapi.yml. Each path carries the servers of the definition it was published in.'
servers:
- url: /api/v1beta0/account
- url: /api/v1beta0/global
- url: /api/v1beta0/user
tags:
- name: Roles
paths:
  /{accountId}/roles/:
    parameters:
    - description: The ID of the Account scoping this request.
      in: path
      name: accountId
      required: true
      schema:
        type: integer
    get:
      description: List all the Roles available in this Account.
      responses:
        200:
          description: A list of AccountRole objects.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AccountRoleListResponse'
        400:
          description: A client-side error was made.
          x-errorCodes:
          - description: The request was structurally incorrect, and was not understood by the API.
            name: InvalidStructure
          - description: The request was understood by the API, but included invalid data.
            name: InvalidValue
          - description: The request wasn't understood by the API.
            name: BadRequest
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
        401:
          description: The request was not authenticated.
          x-errorCodes:
          - description: The request was not properly authenticated.
            name: BadAuthentication
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
        403:
          description: Insufficient permissions.
          x-errorCodes:
          - description: This request should be made in a different Scope.
            name: ScopeViolation
          - description: You do not have the necessary permissions to perform this request.
            name: PermissionViolation
          - description: The maximum memory limit in GB has been reached.
            name: MemoryQuotaExceeded
          - description: The vCPUs limit has been reached.
            name: VcpusQuotaExceeded
          - description: The maximum number of the Servers that you can launch or resume has been reached.
            name: ServersQuotaExceeded
          - description: The maximum number of the Servers that you can launch or resume on current Farm has been reached.
            name: ServersPerFarmQuotaExceeded
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
        404:
          description: Resource not found.
          x-errorCodes:
          - description: This endpoint does not exist.
            name: EndpointNotFound
            noDoc: true
          - description: The object you are trying to access does not exist.
            name: ObjectNotFound
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
        409:
          description: Conflict with current state.
          x-errorCodes:
          - description: These changes aren't possible while this object is in use.
            name: ObjectInUse
          - description: These changes would violate a policy.
            name: PolicyViolation
          - description: These changes would violate a unicity constraint.
            name: UnicityViolation
          - description: Some of the objects being changed are locked and cannot be changed.
            name: Locked
          - description: This Cloud platform is not enabled.
            name: NotEnabledPlatform
          - description: The Operating System does not match.
            name: OperatingSystemMismatch
          - description: Some of these settings cannot be applied, they either conflict with the current state or are invalid.
            name: UnacceptableObjectConfiguration
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
        422:
          description: This request was well-formed but was rejected due to semantic errors.
          x-errorCodes:
          - description: Some of the configuration settings cannot be applied because they conflict with the current state.
            name: ConfigurationMismatch
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
        500:
          description: A server-side error occurred.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
        501:
          description: This feature is not implemented in Scalr.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
        503:
          description: The service is currently unavailable.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
      tags:
      - Roles
      summary: Get by account id roles
      x-summary-source: derived
      operationId: getByAccountIdRoles
      x-operation-id-source: derived
    post:
      description: Create a new Role in this Account.
      responses:
        201:
          description: The JSON representation of a AccountRole object.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AccountRoleDetailsResponse'
        400:
          description: A client-side error was made.
          x-errorCodes:
          - description: The request was structurally incorrect, and was not understood by the API.
            name: InvalidStructure
          - description: The request was understood by the API, but included invalid data.
            name: InvalidValue
          - description: The request wasn't understood by the API.
            name: BadRequest
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
        401:
          description: The request was not authenticated.
          x-errorCodes:
          - description: The request was not properly authenticated.
            name: BadAuthentication
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
        403:
          description: Insufficient permissions.
          x-errorCodes:
          - description: This request should be made in a different Scope.
            name: ScopeViolation
          - description: You do not have the necessary permissions to perform this request.
            name: PermissionViolation
          - description: The maximum memory limit in GB has been reached.
            name: MemoryQuotaExceeded
          - description: The vCPUs limit has been reached.
            name: VcpusQuotaExceeded
          - description: The maximum number of the Servers that you can launch or resume has been reached.
            name: ServersQuotaExceeded
          - description: The maximum number of the Servers that you can launch or resume on current Farm has been reached.
            name: ServersPerFarmQuotaExceeded
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
        404:
          description: Resource not found.
          x-errorCodes:
          - description: This endpoint does not exist.
            name: EndpointNotFound
            noDoc: true
          - description: The object you are trying to access does not exist.
            name: ObjectNotFound
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
        409:
          description: Conflict with current state.
          x-errorCodes:
          - description: These changes aren't possible while this object is in use.
            name: ObjectInUse
          - description: These changes would violate a policy.
            name: PolicyViolation
          - description: These changes would violate a unicity constraint.
            name: UnicityViolation
          - description: Some of the objects being changed are locked and cannot be changed.
            name: Locked
          - description: This Cloud platform is not enabled.
            name: NotEnabledPlatform
          - description: The Operating System does not match.
            name: OperatingSystemMismatch
          - description: Some of these settings cannot be applied, they either conflict with the current state or are invalid.
            name: UnacceptableObjectConfiguration
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
        422:
          description: This request was well-formed but was rejected due to semantic errors.
          x-errorCodes:
          - description: Some of the configuration settings cannot be applied because they conflict with the current state.
            name: ConfigurationMismatch
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
        500:
          description: A server-side error occurred.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
        501:
          description: This feature is not implemented in Scalr.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
        503:
          description: The service is currently unavailable.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/AccountRole'
        description: The JSON representation of a AccountRole object.
        required: true
      tags:
      - Roles
      summary: Create by account id roles
      x-summary-source: derived
      operationId: postByAccountIdRoles
      x-operation-id-source: derived
    servers:
    - url: /api/v1beta0/account
  /{accountId}/roles/{roleId}/:
    parameters:
    - description: The ID of the Account scoping this request.
      in: path
      name: accountId
      required: true
      schema:
        type: integer
    - description: The ID of a Role object.
      in: path
      name: roleId
      required: true
      x-references:
        $ref: '#/components/schemas/Role'
      schema:
        type: integer
    delete:
      description: Delete a Role from this Account.
      responses:
        204:
          description: Successful deletion.
        400:
          description: A client-side error was made.
          x-errorCodes:
          - description: The request was structurally incorrect, and was not understood by the API.
            name: InvalidStructure
          - description: The request was understood by the API, but included invalid data.
            name: InvalidValue
          - description: The request wasn't understood by the API.
            name: BadRequest
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
        401:
          description: The request was not authenticated.
          x-errorCodes:
          - description: The request was not properly authenticated.
            name: BadAuthentication
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
        403:
          description: Insufficient permissions.
          x-errorCodes:
          - description: This request should be made in a different Scope.
            name: ScopeViolation
          - description: You do not have the necessary permissions to perform this request.
            name: PermissionViolation
          - description: The maximum memory limit in GB has been reached.
            name: MemoryQuotaExceeded
          - description: The vCPUs limit has been reached.
            name: VcpusQuotaExceeded
          - description: The maximum number of the Servers that you can launch or resume has been reached.
            name: ServersQuotaExceeded
          - description: The maximum number of the Servers that you can launch or resume on current Farm has been reached.
            name: ServersPerFarmQuotaExceeded
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
        404:
          description: Resource not found.
          x-errorCodes:
          - description: This endpoint does not exist.
            name: EndpointNotFound
            noDoc: true
          - description: The object you are trying to access does not exist.
            name: ObjectNotFound
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
        409:
          description: Conflict with current state.
          x-errorCodes:
          - description: These changes aren't possible while this object is in use.
            name: ObjectInUse
          - description: These changes would violate a policy.
            name: PolicyViolation
          - description: These changes would violate a unicity constraint.
            name: UnicityViolation
          - description: Some of the objects being changed are locked and cannot be changed.
            name: Locked
          - description: This Cloud platform is not enabled.
            name: NotEnabledPlatform
          - description: The Operating System does not match.
            name: OperatingSystemMismatch
          - description: Some of these settings cannot be applied, they either conflict with the current state or are invalid.
            name: UnacceptableObjectConfiguration
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
        422:
          description: This request was well-formed but was rejected due to semantic errors.
          x-errorCodes:
          - description: Some of the configuration settings cannot be applied because they conflict with the current state.
            name: ConfigurationMismatch
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
        500:
          description: A server-side error occurred.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
        501:
          description: This feature is not implemented in Scalr.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
        503:
          description: The service is currently unavailable.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
      tags:
      - Roles
      summary: Delete by account id roles by role id
      x-summary-source: derived
      operationId: deleteByAccountIdRolesByRoleId
      x-operation-id-source: derived
    get:
      description: Retrieve detailed information about a given Role.
      responses:
        200:
          description: The JSON representation of a AccountRole object.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AccountRoleDetailsResponse'
        400:
          description: A client-side error was made.
          x-errorCodes:
          - description: The request was structurally incorrect, and was not understood by the API.
            name: InvalidStructure
          - description: The request was understood by the API, but included invalid data.
            name: InvalidValue
          - description: The request wasn't understood by the API.
            name: BadRequest
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
        401:
          description: The request was not authenticated.
          x-errorCodes:
          - description: The request was not properly authenticated.
            name: BadAuthentication
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
        403:
          description: Insufficient permissions.
          x-errorCodes:
          - description: This request should be made in a different Scope.
            name: ScopeViolation
          - description: You do not have the necessary permissions to perform this request.
            name: PermissionViolation
          - description: The maximum memory limit in GB has been reached.
            name: MemoryQuotaExceeded
          - description: The vCPUs limit has been reached.
            name: VcpusQuotaExceeded
          - description: The maximum number of the Servers that you can launch or resume has been reached.
            name: ServersQuotaExceeded
          - description: The maximum number of the Servers that you can launch or resume on current Farm has been reached.
            name: ServersPerFarmQuotaExceeded
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
        404:
          description: Resource not found.
          x-errorCodes:
          - description: This endpoint does not exist.
            name: EndpointNotFound
            noDoc: true
          - description: The object you are trying to access does not exist.
            name: ObjectNotFound
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
        409:
          description: Conflict with current state.
          x-errorCodes:
          - description: These changes aren't possible while this object is in use.
            name: ObjectInUse
          - description: These changes would violate a policy.
            name: PolicyViolation
          - description: These changes would violate a unicity constraint.
            name: UnicityViolation
          - description: Some of the objects being changed are locked and cannot be changed.
            name: Locked
          - description: This Cloud platform is not enabled.
            name: NotEnabledPlatform
          - description: The Operating System does not match.
            name: OperatingSystemMismatch
          - description: Some of these settings cannot be applied, they either conflict with the current state or are invalid.
            name: UnacceptableObjectConfiguration
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
        422:
          description: This request was well-formed but was rejected due to semantic errors.
          x-errorCodes:
          - description: Some of the configuration settings cannot be applied because they conflict with the current state.
            name: ConfigurationMismatch
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
        500:
          description: A server-side error occurred.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
        501:
          description: This feature is not implemented in Scalr.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
        503:
          description: The service is currently unavailable.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
      tags:
      - Roles
      summary: Get by account id roles by role id
      x-summary-source: derived
      operationId: getByAccountIdRolesByRoleId
      x-operation-id-source: derived
    patch:
      description: Modify the attributes of a given Role.
      responses:
        200:
          description: The JSON representation of a AccountRole object.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AccountRoleDetailsResponse'
        400:
          description: A client-side error was made.
          x-errorCodes:
          - description: The request was structurally incorrect, and was not understood by the API.
            name: InvalidStructure
          - description: The request was understood by the API, but included invalid data.
            name: InvalidValue
          - description: The request wasn't understood by the API.
            name: BadRequest
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
        401:
          description: The request was not authenticated.
          x-errorCodes:
          - description: The request was not properly authenticated.
            name: BadAuthentication
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
        403:
          description: Insufficient permissions.
          x-errorCodes:
          - description: This request should be made in a different Scope.
            name: ScopeViolation
          - description: You do not have the necessary permissions to perform this request.
            name: PermissionViolation
          - description: The maximum memory limit in GB has been reached.
            name: MemoryQuotaExceeded
          - description: The vCPUs limit has been reached.
            name: VcpusQuotaExceeded
          - description: The maximum number of the Servers that you can launch or resume has been reached.
            name: ServersQuotaExceeded
          - description: The maximum number of the Servers that you can launch or resume on current Farm has been reached.
            name: ServersPerFarmQuotaExceeded
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
        404:
          description: Resource not found.
          x-errorCodes:
          - description: This endpoint does not exist.
            name: EndpointNotFound
            noDoc: true
          - description: The object you are trying to access does not exist.
            name: ObjectNotFound
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
        409:
          description: Conflict with current state.
          x-errorCodes:
          - description: These changes aren't possible while this object is in use.
            name: ObjectInUse
          - description: These changes would violate a policy.
            name: PolicyViolation
          - description: These changes would violate a unicity constraint.
            name: UnicityViolation
          - description: Some of the objects being changed are locked and cannot be changed.
            name: Locked
          - description: This Cloud platform is not enabled.
            name: NotEnabledPlatform
          - description: The Operating System does not match.
            name: OperatingSystemMismatch
          - description: Some of these settings cannot be applied, they either conflict with the current state or are invalid.
            name: UnacceptableObjectConfiguration
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
        422:
          description: This request was well-formed but was rejected due to semantic errors.
          x-errorCodes:
          - description: Some of the configuration settings cannot be applied because they conflict with the current state.
            name: ConfigurationMismatch
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
        500:
          description: A server-side error occurred.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
        501:
          description: This feature is not implemented in Scalr.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
        503:
          description: The service is currently unavailable.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/AccountRole'
        description: The JSON representation of a AccountRole object.
        required: true
      tags:
      - Roles
      summary: Update by account id roles by role id
      x-summary-source: derived
      operationId: patchByAccountIdRolesByRoleId
      x-operation-id-source: derived
    servers:
    - url: /api/v1beta0/account
  /{accountId}/roles/{roleId}/actions/clone/:
    parameters:
    - description: The ID of the Account scoping this request.
      in: path
      name: accountId
      required: true
      schema:
        type: integer
    - description: The ID of a Role object.
      in: path
      name: roleId
      required: true
      x-references:
        $ref: '#/components/schemas/Role'
      schema:
        type: integer
    post:
      description: Make a copy of an existing Role by cloning it.
      responses:
        201:
          description: The JSON representation of a AccountRole object.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AccountRoleDetailsResponse'
        400:
          description: A client-side error was made.
          x-errorCodes:
          - description: The request was structurally incorrect, and was not understood by the API.
            name: InvalidStructure
          - description: The request was understood by the API, but included invalid data.
            name: InvalidValue
          - description: The request wasn't understood by the API.
            name: BadRequest
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
        401:
          description: The request was not authenticated.
          x-errorCodes:
          - description: The request was not properly authenticated.
            name: BadAuthentication
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
        403:
          description: Insufficient permissions.
          x-errorCodes:
          - description: This request should be made in a different Scope.
            name: ScopeViolation
          - description: You do not have the necessary permissions to perform this request.
            name: PermissionViolation
          - description: The maximum memory limit in GB has been reached.
            name: MemoryQuotaExceeded
          - description: The vCPUs limit has been reached.
            name: VcpusQuotaExceeded
          - description: The maximum number of the Servers that you can launch or resume has been reached.
            name: ServersQuotaExceeded
          - description: The maximum number of the Servers that you can launch or resume on current Farm has been reached.
            name: ServersPerFarmQuotaExceeded
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
        404:
          description: Resource not found.
          x-errorCodes:
          - description: This endpoint does not exist.
            name: EndpointNotFound
            noDoc: true
          - description: The object you are trying to access does not exist.
            name: ObjectNotFound
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
        409:
          description: Conflict with current state.
          x-errorCodes:
          - description: These changes aren't possible while this object is in use.
            name: ObjectInUse
          - description: These changes would violate a policy.
            name: PolicyViolation
          - description: These changes would violate a unicity constraint.
            name: UnicityViolation
          - description: Some of the objects being changed are locked and cannot be changed.
            name: Locked
          - description: This Cloud platform is not enabled.
            name: NotEnabledPlatform
          - description: The Operating System does not match.
            name: OperatingSystemMismatch
          - description: Some of these settings cannot be applied, they either conflict with the current state or are invalid.
            name: UnacceptableObjectConfiguration
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
        422:
          description: This request was well-formed but was rejected due to semantic errors.
          x-errorCodes:
          - description: Some of the configuration settings cannot be applied because they conflict with the current state.
            name: ConfigurationMismatch
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
        500:
          description: A server-side error occurred.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
        501:
          description: This feature is not implemented in Scalr.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
        503:
          description: The service is currently unavailable.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
      requestBody:
        content:
          application/json:
            schema:
              properties:
                name:
                  description: The name of the new Role.
                  type: string
              required:
              - name
        description: Role cloning options.
        required: true
      tags:
      - Roles
      summary: Create by account id roles by role id actions clone
      x-summary-source: derived
      operationId: postByAccountIdRolesByRoleIdActionsClone
      x-operation-id-source: derived
    servers:
    - url: /api/v1beta0/account
  /{accountId}/roles/{roleId}/actions/deprecate/:
    parameters:
    - description: The ID of the Account scoping this request.
      in: path
      name: accountId
      required: true
      schema:
        type: integer
    - description: The ID of a Role object.
      in: path
      name: roleId
      required: true
      x-references:
        $ref: '#/components/schemas/Role'
      schema:
        type: integer
    post:
      description: Deprecate a Role and set another Role to replace it.
      responses:
        200:
          description: The JSON representation of a AccountRole object.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AccountRoleDetailsResponse'
        400:
          description: A client-side error was made.
          x-errorCodes:
          - description: The request was structurally incorrect, and was not understood by the API.
            name: InvalidStructure
          - description: The request was understood by the API, but included invalid data.
            name: InvalidValue
          - description: The request wasn't understood by the API.
            name: BadRequest
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
        401:
          description: The request was not authenticated.
          x-errorCodes:
          - description: The request was not properly authenticated.
            name: BadAuthentication
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
        403:
          description: Insufficient permissions.
          x-errorCodes:
          - description: This request should be made in a different Scope.
            name: ScopeViolation
          - description: You do not have the necessary permissions to perform this request.
            name: PermissionViolation
          - description: The maximum memory limit in GB has been reached.
            name: MemoryQuotaExceeded
          - description: The vCPUs limit has been reached.
            name: VcpusQuotaExceeded
          - description: The maximum number of the Servers that you can launch or resume has been reached.
            name: ServersQuotaExceeded
          - description: The maximum number of the Servers that you can launch or resume on current Farm has been reached.
            name: ServersPerFarmQuotaExceeded
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
        404:
          description: Resource not found.
          x-errorCodes:
          - description: This endpoint does not exist.
            name: EndpointNotFound
            noDoc: true
          - description: The object you are trying to access does not exist.
            name: ObjectNotFound
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
        409:
          description: Conflict with current state.
          x-errorCodes:
          - description: These changes aren't possible while this object is in use.
            name: ObjectInUse
          - description: These changes would violate a policy.
            name: PolicyViolation
          - description: These changes would violate a unicity constraint.
            name: UnicityViolation
          - description: Some of the objects being changed are locked and cannot be changed.
            name: Locked
          - description: This Cloud platform is not enabled.
            name: NotEnabledPlatform
          - description: The Operating System does not match.
            name: OperatingSystemMismatch
          - description: Some of these settings cannot be applied, they either conflict with the current state or are invalid.
            name: UnacceptableObjectConfiguration
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
        422:
          description: This request was well-formed but was rejected due to semantic errors.
          x-errorCodes:
          - description: Some of the configuration settings cannot be applied because they conflict with the current state.
            name: ConfigurationMismatch
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
        500:
          description: A server-side error occurred.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
        501:
          description: This feature is not implemented in Scalr.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
        503:
          description: The service is currently unavailable.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
      requestBody:
        content:
          application/json:
            schema:
              properties:
                deprecate:
                  default: true
                  description: By default deprecate Role If a value is true clears the deprecate from Role.
                  type: boolean
                replacement:
                  $ref: '#/components/schemas/RoleForeignKey'
                  description: The suggested replacement Role foreign key for the current Role.
        description: Deprecate Role options.
      tags:
      - Roles
      summary: Create by account id roles by role id actions deprecate
      x-summary-source: derived
      operationId: postByAccountIdRolesByRoleIdActionsDeprecate
      x-operation-id-source: derived
    servers:
    - url: /api/v1beta0/account
  /{accountId}/roles/{roleId}/global-variables/:
    parameters:
    - description: The ID of the Account scoping this request.
      in: path
      name: accountId
      required: true
      schema:
        type: integer
    - description: The ID of a Role object.
      in: path
      name: roleId
      required: true
      x-references:
        $ref: '#/components/schemas/Role'
      schema:
        type: integer
    get:
      description: List all the Global Variables associated with a given Role.
      responses:
        200:
          description: A list of GlobalVariable objects.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/GlobalVariableListResponse'
        400:
          description: A client-side error was made.
          x-errorCodes:
          - description: The request was structurally incorrect, and was not understood by the API.
            name: InvalidStructure
          - description: The request was understood by the API, but included invalid data.
            name: InvalidValue
          - description: The request wasn't understood by the API.
            name: BadRequest
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
        401:
          description: The request was not authenticated.
          x-errorCodes:
          - description: The request was not properly authenticated.
            name: BadAuthentication
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
        403:
          description: Insufficient permissions.
          x-errorCodes:
          - description: This request should be made in a different Scope.
            name: ScopeViolation
          - description: You do not have the necessary permissions to perform this request.
            name: PermissionViolation
          - description: The maximum memory limit in GB has been reached.
            name: MemoryQuotaExceeded
          - description: The vCPUs limit has been reached.
            name: VcpusQuotaExceeded
          - description: The maximum number of the Servers that you can launch or resume has been reached.
            name: ServersQuotaExceeded
          - description: The maximum number of the Servers that you can launch or resume on current Farm has been reached.
            name: ServersPerFarmQuotaExceeded
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
        404:
          description: Resource not found.
          x-errorCodes:
          - description: This endpoint does not exist.
            name: EndpointNotFound
            noDoc: true
          - description: The object you are trying to access does not exist.
            name: ObjectNotFound
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
        409:
          description: Conflict with current state.
          x-errorCodes:
          - description: These changes aren't possible while this object is in use.
            name: ObjectInUse
          - description: These changes would violate a policy.
            name: PolicyViolation
          - description: These changes would violate a unicity constraint.
            name: UnicityViolation
          - description: Some of the objects being changed are locked and cannot be changed.
            name: Locked
          - description: This Cloud platform is not enabled.
            name: NotEnabledPlatform
          - description: The Operating System does not match.
            name: OperatingSystemMismatch
          - description: Some of these settings cannot be applied, they either conflict with the current state or are invalid.
            name: UnacceptableObjectConfiguration
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
        422:
          description: This request was well-formed but was rejected due to semantic errors.
          x-errorCodes:
          - description: Some of the configuration settings cannot be applied because they conflict with the current state.
            name: ConfigurationMismatch
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
        500:
          description: A server-side error occurred.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
        501:
          description: This feature is not implemented in Scalr.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
        503:
          description: The service is currently unavailable.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
      tags:
      - Roles
      summary: Get by account id roles by role id global variables
      x-summary-source: derived
      operationId: getByAccountIdRolesByRoleIdGlobalVariables
      x-operation-id-source: derived
    post:
      description: Declare a new Global Variable for this Role.
      responses:
        201:
          description: The JSON representation of a GlobalVariable object.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/GlobalVariableDetailsResponse'
        400:
          description: A client-side error was made.
          x-errorCodes:
          - description: The request was structurally incorrect, and was not understood by the API.
            name: InvalidStructure
          - description: The request was understood by the API, but included invalid data.
            name: InvalidValue
          - description: The request wasn't understood by the API.
            name: BadRequest
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
        401:
          description: The request was not authenticated.
          x-errorCodes:
          - description: The request was not properly authenticated.
            name: BadAuthentication
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
        403:
          description: Insufficient permissions.
          x-errorCodes:
          - description: This request should be made in a different Scope.
            name: ScopeViolation
          - description: You do not have the necessary permissions to perform this request.
            name: PermissionViolation
          - description: The maximum memory limit in GB has been reached.
            name: MemoryQuotaExceeded
          - description: The vCPUs limit has been reached.
            name: VcpusQuotaExceeded
          - description: The maximum number of the Servers that you can launch or resume has been reached.
            name: ServersQuotaExceeded
          - description: The maximum number of the Servers that you can launch or resume on current Farm has been reached.
            name: ServersPerFarmQuotaExceeded
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
        404:
          description: Resource not found.
          x-errorCodes:
          - description: This endpoint does not exist.
            name: EndpointNotFound
            noDoc: true
          - description: The object you are trying to access does not exist.
            name: ObjectNotFound
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
        409:
          description: Conflict with current state.
          x-errorCodes:
          - description: These changes aren't possible while this object is in use.
            name: ObjectInUse
          - description: These changes would violate a policy.
            name: PolicyViolation
          - description: These changes would violate a unicity constraint.
            name: UnicityViolation
          - description: Some of the objects being changed are locked and cannot be changed.
            name: Locked
          - description: This Cloud platform is not enabled.
            name: NotEnabledPlatform
          - description: The Operating System does not match.
            name: OperatingSystemMismatch
          - description: Some of these settings cannot be applied, they either conflict with the current state or are invalid.
            name: UnacceptableObjectConfiguration
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
        422:
          description: This request was well-formed but was rejected due to semantic errors.
          x-errorCodes:
          - description: Some of the configuration settings cannot be applied because they conflict with the current state.
            name: ConfigurationMismatch
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
        500:
          description: A server-side error occurred.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
        501:
          description: This feature is not implemented in Scalr.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
        503:
          description: The service is currently unavailable.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/GlobalVariable'
        description: The JSON representation of a GlobalVariable object.
        required: true
      tags:
      - Roles
      summary: Create by account id roles by role id global variables
      x-summary-source: derived
      operationId: postByAccountIdRolesByRoleIdGlobalVariables
      x-operation-id-source: derived
    servers:
    - url: /api/v1beta0/account
  /{accountId}/roles/{roleId}/global-variables/{globalVariableName}/:
    parameters:
    - description: The ID of the Account scoping this request.
      in: path
      name: accountId
      required: true
      schema:
        type: integer
    - description: The ID of a Role object.
      in: path
      name: roleId
      required: true
      x-references:
        $ref: '#/components/schemas/Role'
      schema:
        type: integer
    - description: The name of the Global Variable being accessed.
      in: path
      name: globalVariableName
      required: true
      schema:
        type: string
        maxLength: 128
        minLength: 2
        pattern: \w+
    delete:
      description: Delete a Global Variable from a given Role's scope.
      responses:
        204:
          description: Successful deletion.
        400:
          description: A client-side error was made.
          x-errorCodes:
          - description: The request was structurally incorrect, and was not understood by the API.
            name: InvalidStructure
          - description: The request was understood by the API, but included invalid data.
            name: InvalidValue
          - description: The request wasn't understood by the API.
            name: BadRequest
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
        401:
          description: The request was not authenticated.
          x-errorCodes:
          - description: The request was not properly authenticated.
            name: BadAuthentication
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
        403:
          description: Insufficient permissions.
          x-errorCodes:
          - description: This request should be made in a different Scope.
            name: ScopeViolation
          - description: You do not have the necessary permissions to perform this request.
            name: PermissionViolation
          - description: The maximum memory limit in GB has been reached.
            name: MemoryQuotaExceeded
          - description: The vCPUs limit has been reached.
            name: VcpusQuotaExceeded
          - description: The maximum number of the Servers that you can launch or resume has been reached.
            name: ServersQuotaExceeded
          - description: The maximum number of the Servers that you can launch or resume on current Farm has been reached.
            name: ServersPerFarmQuotaExceeded
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
        404:
          description: Resource not found.
          x-errorCodes:
          - description: This endpoint does not exist.
            name: EndpointNotFound
            noDoc: true
          - description: The object you are trying to access does not exist.
            name: ObjectNotFound
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
        409:
          description: Conflict with current state.
          x-errorCodes:
          - description: These changes aren't possible while this object is in use.
            name: ObjectInUse
          - description: These changes would violate a policy.
            name: PolicyViolation
          - description: These changes would violate a unicity constraint.
            name: UnicityViolation
          - description: Some of the objects being changed are locked and cannot be changed.
            name: Locked
          - description: This Cloud platform is not enabled.
            name: NotEnabledPlatform
          - description: The Operating System does not match.
            name: OperatingSystemMismatch
          - description: Some of these settings cannot be applied, they either conflict with the current state or are invalid.
            name: UnacceptableObjectConfiguration
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
        422:
          description: This request was well-formed but was rejected due to semantic errors.
          x-errorCodes:
          - description: Some of the configuration settings cannot be applied because they conflict with the current state.
            name: ConfigurationMismatch
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
        500:
          description: A server-side error occurred.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
        501:
          description: This feature is not implemented in Scalr.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
        503:
          description: The service is currently unavailable.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
      tags:
      - Roles
      summary: Delete by account id roles by role id global variables by global variable name
      x-summary-source: derived
      operationId: deleteByAccountIdRolesByRoleIdGlobalVariablesByGlobalVariableName
      x-operation-id-source: derived
    get:
      description: Retrieve detailed information about Global Variable in the Role's scope.
      responses:
        200:
          description: The JSON representation of a GlobalVariable object.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/GlobalVariableDetailsResponse'
        400:
          description: A client-side error was made.
          x-errorCodes:
          - description: The request was structurally incorrect, and was not understood by the API.
            name: InvalidStructure
          - description: The request was understood by the API, but included invalid data.
            name: InvalidValue
          - description: The request wasn't understood by the API.
            name: BadRequest
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
        401:
          description: The request was not authenticated.
          x-errorCodes:
          - description: The request was not properly authenticated.
            name: BadAuthentication
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
        403:
          description: Insufficient permissions.
          x-errorCodes:
          - description: This request should be made in a different Scope.
            name: ScopeViolation
          - description: You do not have the necessary permissions to perform this request.
            name: PermissionViolation
          - description: The maximum memory limit in GB has been reached.
            name: MemoryQuotaExceeded
          - description: The vCPUs limit has been reached.
            name: VcpusQuotaExceeded
          - description: The maximum number of the Servers that you can launch or resume has been reached.
            name: ServersQuotaExceeded
          - description: The maximum number of the Servers that you can launch or resume on current Farm has been reached.
            name: ServersPerFarmQuotaExceeded
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
        404:
          description: Resource not found.
          x-errorCodes:
          - description: This endpoint does not exist.
            name: EndpointNotFound
            noDoc: true
          - description: The object you are trying to access does not exist.
            name: ObjectNotFound
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
        409:
          description: Conflict with current state.
          x-errorCodes:
          - description: These changes aren't possible while this object is in use.
            name: ObjectInUse
          - description: These changes would violate a policy.
            name: PolicyViolation
          - description: These changes would violate a unicity constraint.
            name: UnicityViolation
          - description: Some of the objects being changed are locked and cannot be changed.
            name: Locked
          - description: This Cloud platform is not enabled.
            name: NotEnabledPlatform
          - description: The Operating System does not match.
            name: OperatingSystemMismatch
          - description: Some of these settings cannot be applied, they either conflict with the current state or are invalid.
            name: UnacceptableObjectConfiguration
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
        422:
          description: This request was well-formed but was rejected due to semantic errors.
          x-errorCodes:
          - description: Some of the configuration settings cannot be applied because they conflict with the current state.
            name: ConfigurationMismatch
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
        500:
          description: A server-side error occurred.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
        501:
          description: This feature is not implemented in Scalr.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
        503:
          description: The service is currently unavailable.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
      tags:
      - Roles
      summary: Get by account id roles by role id global variables by global variable name
      x-summary-source: derived
      operationId: getByAccountIdRolesByRoleIdGlobalVariablesByGlobalVariableName
      x-operation-id-source: derived
    patch:
      description: Modify the attributes of a Global Variable in the Role's scope.
      responses:
        200:
          description: The JSON representation of a GlobalVariable object.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/GlobalVariableDetailsResponse'
        400:
          description: A client-side error was made.
          x-errorCodes:
          - description: The request was structurally incorrect, and was not understood by the API.
            name: InvalidStructure
          - description: The request was understood by the API, but included invalid data.
            name: InvalidValue
          - description: The request wasn't understood by the API.
            name: BadRequest
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
        401:
          description: The request was not authenticated.
          x-errorCodes:
          - description: The request was not properly authenticated.
            name: BadAuthentication
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
        403:
          description: Insufficient permissions.
          x-errorCodes:
          - description: This request should be made in a different Scope.
            name: ScopeViolation
          - description: You do not have the necessary permissions to perform this request.
            name: PermissionViolation
          - description: The maximum memory limit in GB has been reached.
            name: MemoryQuotaExceeded
          - description: The vCPUs limit has been reached.
            name: VcpusQuotaExceeded
          - description: The maximum number of the Servers that you can launch or resume has been reached.
            name: ServersQuotaExceeded
          - description: The maximum number of the Servers that you can launch or resume on current Farm has been reached.
            name: ServersPerFarmQuotaExceeded
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
        404:
          description: Resource not found.
          x-errorCodes:
          - description: This endpoint does not exist.
            name: EndpointNotFound
            noDoc: true
          - description: The object you are trying to access does not exist.
            name: ObjectNotFound
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
        409:
          description: Conflict with current state.
          x-errorCodes:
          - description: These changes aren't possible while this object is in use.
            name: ObjectInUse
          - description: These changes would violate a policy.
            name: PolicyViolation
          - description: These changes would violate a unicity constraint.
            name: UnicityViolation
          - description: Some of the objects being changed are locked and cannot be changed.
            name: Locked
          - description: This Cloud platform is not enabled.
            name: NotEnabledPlatform
          - description: The Operating System does not match.
            name: OperatingSystemMismatch
          - description: Some of these settings cannot be applied, they either conflict with the current state or are invalid.
            name: UnacceptableObjectConfiguration
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
        422:
          description: This request was well-formed but was rejected due to semantic errors.
          x-errorCodes:
          - description: Some of the configuration settings cannot be applied because they conflict with the current state.
            name: ConfigurationMismatch
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
        500:
          description: A server-side error occurred.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
        501:
          description: This feature is not implemented in Scalr.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
        503:
          description: The service is currently unavailable.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/GlobalVariable'
        description: The JSON representation of a GlobalVariable object.
        required: true
      tags:
      - Roles
      summary: Update by account id roles by role id global variables by global variable name
      x-summary-source: derived
      operationId: patchByAccountIdRolesByRoleIdGlobalVariablesByGlobalVariableName
      x-operation-id-source: derived
    servers:
    - url: /api/v1beta0/account
  /{accountId}/roles/{roleId}/images/:
    parameters:
    - description: The ID of the Account scoping this request.
      in: path
      name: accountId
      required: true
      schema:
        type: integer
    - description: The ID of a Role object.
      in: path
      name: roleId
      required: true
      x-references:
        $ref: '#/components/schemas/Role'
      schema:
        type: integer
    get:
      description: List all the Images associated with a given Role.
      responses:
        200:
          description: A list of RoleImage objects.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/RoleImageListResponse'
        400:
          description: A client-side error was made.
          x-errorCodes:
          - description: The request was structurally incorrect, and was not understood by the API.
            name: InvalidStructure
          - description: The request was understood by the API, but included invalid data.
            name: InvalidValue
          - description: The request wasn't understood by the API.
            name: BadRequest
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
        401:
          description: The request was not authenticated.
          x-errorCodes:
          - description: The request was not properly authenticated.
            name: BadAuthentication
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
        403:
          description: Insufficient permissions.
          x-errorCodes:
          - description: This request should be made in a different Scope.
            name: ScopeViolation
          - description: You do not have the necessary permissions to perform this request.
            name: PermissionViolation
          - description: The maximum memory limit in GB has been reached.
            name: MemoryQuotaExceeded
          - description: The vCPUs limit has been reached.
            name: VcpusQuotaExceeded
          - description: The maximum number of the Servers that you can launch or resume has been reached.
            name: ServersQuotaExceeded
          - description: The maximum number of the Servers that you can launch or resume on current Farm has been reached.
            name: ServersPerFarmQuotaExceeded
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
        404:
          description: Resource not found.
          x-errorCodes:
          - description: This endpoint does not exist.
            name: EndpointNotFound
            noDoc: true
          - description: The object you are trying to access does not exist.
            name: ObjectNotFound
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
        409:
          description: Conflict with current state.
          x-errorCodes:
          - description: These changes aren't possible while this object is in use.
            name: ObjectInUse
          - description: These changes would violate a policy.
            name: PolicyViolation
          - description: These changes would violate a unicity constraint.
            name: UnicityViolation
          - description: Some of the objects being changed are locked and cannot be changed.
            name: Locked
          - description: This Cloud platform is not enabled.
            name: NotEnabledPlatform
          - description: The Operating System does not match.
            name: OperatingSystemMismatch
          - description: Some of these settings cannot be applied, they either conflict with the current state or are invalid.
            name: UnacceptableObjectConfiguration
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
        422:
          description: This request was well-formed but was rejected due to semantic errors.
          x-errorCodes:
          - description: Some of the configuration settings cannot be applied because they conflict with the current state.
            name: ConfigurationMismatch
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
        500:
          description: A server-side error occurred.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
        501:
          description: This feature is not implemented in Scalr.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
        503:
          description: The service is currently unavailable.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
      tags:
      - Roles
      summary: Get by account id roles by role id images
      x-summary-source: derived
      operationId: getByAccountIdRolesByRoleIdImages
      x-operation-id-source: derived
    post:
      description: Associate a new Image with a given Role. This will fail if an Image associated to the Role is in the same location than the Image you are trying to add.
      responses:
        201:
          description: The JSON representation of a RoleImage object.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/RoleImageDetailsResponse'
        400:
          description: A client-side error was made.
          x-errorCodes:
          - description: The request was structurally incorrect, and was not understood by the API.
            name: InvalidStructure
          - description: The request was understood by the API, but included invalid data.
            name: InvalidValue
          - description: The request wasn't understood by the API.
            name: BadRequest
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
        401:
          description: The request was not authenticated.
          x-errorCodes:
          - description: The request was not properly authenticated.
            name: BadAuthentication
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
        403:
          description: Insufficient permissions.
          x-errorCodes:
          - description: This request should be made in a different Scope.
            name: ScopeViolation
          - description: You do not have the necessary permissions to perform this request.
            name: PermissionViolation
          - description: The maximum memory limit in GB has been reached.
            name: MemoryQuotaExceeded
          - description: The vCPUs limit has been reached.
            name: VcpusQuotaExceeded
          - description: The maximum number of the Servers that you can launch or resume has been reached.
            name: ServersQuotaExceeded
          - description: The maximum number of the Servers that you can launch or resume on current Farm has been reached.
            name: ServersPerFarmQuotaExceeded
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
        404:
          description: Resource not found.
          x-errorCodes:
          - description: This endpoint does not exist.
            name: EndpointNotFound
            noDoc: true
          - description: The object you are trying to access does not exist.
            name: ObjectNotFound
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
        409:
          description: Conflict with current state.
          x-errorCodes:
          - description: These changes aren't possible while this object is in use.
            name: ObjectInUse
          - description: These changes would violate a policy.
            name: PolicyViolation
          - description: These changes would violate a unicity constraint.
            name: UnicityViolation
          - description: Some of the objects being changed are locked and cannot be changed.
            name: Locked
          - description: This Cloud platform is not enabled.
            name: NotEnabledPlatform
          - description: The Operating System does not match.
            name: OperatingSystemMismatch
          - description: Some of these settings cannot be applied, they either conflict with the current state or are invalid.
            name: UnacceptableObjectConfiguration
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
        422:
          description: This request was well-formed but was rejected due to semantic errors.
          x-errorCodes:
          - description: Some of the configuration settings cannot be applied because they conflict with the current state.
            name: ConfigurationMismatch
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
        500:
          description: A server-side error occurred.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
        501:
          description: This feature is not implemented in Scalr.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
        503:
          description: The service is currently unavailable.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/RoleImage'
        description: The JSON representation of a RoleImage object.
        required: true
      tags:
      - Roles
      summary: Create by account id roles by role id images
      x-summary-source: derived
      operationId: postByAccountIdRolesByRoleIdImages
      x-operation-id-source: derived
    servers:
    - url: /api/v1beta0/account
  /{accountId}/roles/{roleId}/images/{imageId}/:
    parameters:
    - description: The ID of the Account scoping this request.
      in: path
      name: accountId
      required: true
      schema:
        type: integer
    - description: The ID of a Role object.
      in: path
      name: roleId
      required: true
      x-references:
        $ref: '#/components/schemas/Role'
      schema:
        type: integer
    - description: The ID of a Image object.
      in: path
      name: imageId
      required: true
      x-references:
        $ref: '#/components/schemas/Image'
      schema:
        type: string
        maxLength: 36
        minLength: 36
        pattern: '[A-Fa-f0-9]{8}-([A-Fa-f0-9]{4}-){3}[A-Fa-f0-9]{12}'
    delete:
      description: Dis-associate an Image from a given Role.
      responses:
        204:
          description: Successful deletion.
        400:
          description: A client-side error was made.
          x-errorCodes:
          - description: The request was structurally incorrect, and was not understood by the API.
            name: InvalidStructure
          - description: The request was understood by the API, but included invalid data.
            name: InvalidValue
          - description: The request wasn't understood by the API.
            name: BadRequest
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
        401:
          description: The request was not authenticated.
          x-errorCodes:
          - description: The request was not properly authenticated.
            name: BadAuthentication
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
        403:
          description: Insufficient permissions.
          x-errorCodes:
          - description: This request should be made in a different Scope.
            name: ScopeViolation
          - description: You do not have the necessary permissions to perform this request.
            name: PermissionViolation
          - description: The maximum memory limit in GB has been reached.
            name: MemoryQuotaExceeded
          - description: The vCPUs limit has been reached.
            name: VcpusQuotaExceeded
          - description: The maximum number of the Servers that you can launch or resume has been reached.
            name: ServersQuotaExceeded
          - description: The maximum number of the Servers that you can launch or resume on current Farm has been reached.
            name: ServersPerFarmQuotaExceeded
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
        404:
          description: Resource not found.
          x-errorCodes:
          - description: This endpoint does not exist.
            name: EndpointNotFound
            noDoc: true
          - description: The object you are trying to access does not exist.
            name: ObjectNotFound
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
        409:
          description: Conflict with current state.
          x-errorCodes:
          - description: These changes aren't possible while this object is in use.
            name: ObjectInUse
          - description: These changes would violate a policy.
            name: PolicyViolation
          - description: These changes would violate a unicity constraint.
            name: UnicityViolation
          - description: Some of the objects being changed are locked and cannot be changed.
            name: Locked
          - description: This Cloud platform is not enabled.
            name: NotEnabledPlatform
          - description: The Operating System does not match.
            name: OperatingSystemMismatch
          - description: Some of these settings cannot be applied, they either conflict with the current state or are invalid.
            name: UnacceptableObjectConfiguration
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
        422:
          description: This request was well-formed but was rejected due to semantic errors.
          x-errorCodes:
          - description: Some of the configuration settings cannot be applied because they conflict with the current state.
            name: ConfigurationMismatch
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
        500:
          description: A server-side error occurred.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
        501:
          description: This feature is not implemented in Scalr.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
        503:
          description: The service is currently unavailable.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
      tags:
      - Roles
      summary: Delete by account id roles by role id images by image id
      x-summary-source: derived
      operationId: deleteByAccountIdRolesByRoleIdImagesByImageId
      x-operation-id-source: derived
    get:
      description: Retrieve detailed information about an Image.
      responses:
        200:
          description: Redirects to the Image.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ImageDetailsResponse'
        400:
          description: A client-side error was made.
          x-errorCodes:
          - description: The request was structurally incorrect, and was not understood by the API.
            name: InvalidStructure
          - description: The request was understood by the API, but included invalid data.
            name: InvalidValue
          - description: The request wasn't understood by the API.
            name: BadRequest
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
        401:
          description: The request was not authenticated.
          x-errorCodes:
          - description: The request was not properly authenticated.
            name: BadAuthentication
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
        403:
          description: Insufficient permissions.
          x-errorCodes:
          - description: This request should be made in a different Scope.
            name: ScopeViolation
          - description: You do not have the necessary permissions to perform this request.
            name: PermissionViolation
          - description: The maximum memory limit in GB has been reached.
            name: MemoryQuotaExceeded
          - description: The vCPUs limit has been reached.
            name: VcpusQuotaExceeded
          - description: The maximum number of the Servers that you can launch or resume has been reached.
            name: ServersQuotaExceeded
          - description: The maximum number of the Servers that you can launch or resume on current Farm has been reached.
            name: ServersPerFarmQuotaExceeded
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
        404:
          description: Resource not found.
          x-errorCodes:
          - description: This endpoint does not exist.
            name: EndpointNotFound
            noDoc: true
          - description: The object you are trying to access does not exist.
            name: ObjectNotFound
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
        409:
          description: Conflict with current state.
          x-errorCodes:
          - description: These changes aren't possible while this object is in use.
            name: ObjectInUse
          - description: These changes would violate a policy.
            name: PolicyViolation
          - description: These changes would violate a unicity constraint.
            name: UnicityViolation
          - description: Some of the objects being changed are locked and cannot be changed.
            name: Locked
          - description: This Cloud platform is not enabled.
            name: NotEnabledPlatform
          - description: The Operating System does not match.
            name: OperatingSystemMismatch
          - description: Some of these settings cannot be applied, they either conflict with the current state or are invalid.
            name: UnacceptableObjectConfiguration
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
        422:
          description: This request was well-formed but was rejected due to semantic errors.
          x-errorCodes:
          - description: Some of the configuration settings cannot be applied because they conflict with the current state.
            name: ConfigurationMismatch
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
        500:
          description: A server-side error occurred.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
        501:
          description: This feature is not implemented in Scalr.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
        503:
          description: The service is currently unavailable.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
      tags:
      - Roles
      summary: Get by account id roles by role id images by image id
      x-summary-source: derived
      operationId: getByAccountIdRolesByRoleIdImagesByImageId
      x-operation-id-source: derived
    servers:
    - url: /api/v1beta0/account
  /{accountId}/roles/{roleId}/images/{imageId}/actions/replace/:
    parameters:
    - description: The ID of the Account scoping this request.
      in: path
      name: accountId
      required: true
      schema:
        type: integer
    - description: The ID of a Role object.
      in: path
      name: roleId
      required: true
      x-references:
        $ref: '#/components/schemas/Role'
      schema:
        type: integer
    - description: The ID of a Image object.
      in: path
      name: imageId
      required: true
      x-references:
        $ref: '#/components/schemas/Image'
      schema:
        type: string
        maxLength: 36
        minLength: 36
        pattern: '[A-Fa-f0-9]{8}-([A-Fa-f0-9]{4}-){3}[A-Fa-f0-9]{12}'
    post:
      description: Replace an Image in-place in a given Role. This allows to replace an Image without having to dis-associate it and then associate a new one, which may not be possible if a Farm Role is currently using this Image.
      responses:
        200:
          description: The JSON representation of a RoleImage object.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/RoleImageDetailsResponse'
        400:
          description: A client-side error was made.
          x-errorCodes:
          - description: The request was structurally incorrect, and was not understood by the API.
            name: InvalidStructure
          - description: The request was understood by the API, but included invalid data.
            name: InvalidValue
          - description: The request wasn't understood by the API.
            name: BadRequest
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
        401:
          description: The request was not authenticated.
          x-errorCodes:
          - description: The request was not properly authenticated.
            name: BadAuthentication
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
        403:
          description: Insufficient permissions.
          x-errorCodes:
          - description: This request should be made in a different Scope.
            name: ScopeViolation
          - description: You do not have the necessary permissions to perform this request.
            name: PermissionViolation
          - description: The maximum memory limit in GB has been reached.
            name: MemoryQuotaExceeded
          - description: The vCPUs limit has been reached.
            name: VcpusQuotaExceeded
          - description: The maximum number of the Servers that you can launch or resume has been reached.
            name: ServersQuotaExceeded
          - description: The maximum number of the Servers that you can launch or resume on current Farm has been reached.
            name: ServersPerFarmQuotaExceeded
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
        404:
          description: Resource not found.
          x-errorCodes:
          - description: This endpoint does not exist.
            name: EndpointNotFound
            noDoc: true
          - description: The object you are trying to access does not exist.
            name: ObjectNotFound
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
        409:
          description: Conflict with current state.
          x-errorCodes:
          - description: These changes aren't possible while this object is in use.
            name: ObjectInUse
          - description: These changes would violate a policy.
            name: PolicyViolation
          - description: These changes would violate a unicity constraint.
            name: UnicityViolation
          - description: Some of the objects being changed are locked and cannot be changed.
            name: Locked
          - description: This Cloud platform is not enabled.
            name: NotEnabledPlatform
          - description: The Operating System does not match.
            name: OperatingSystemMismatch
          - description: Some of these settings cannot be applied, they either conflict with the current state or are invalid.
            name: UnacceptableObjectConfiguration
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
        422:
          description: This request was well-formed but was rejected due to semantic errors.
          x-errorCodes:
          - description: Some of the configuration settings cannot be applied because they conflict with the current state.
            name: ConfigurationMismatch
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
        500:
          description: A server-side error occurred.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
        501:
          description: This feature is not implemented in Scalr.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
        503:
          description: The service is currently unavailable.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/RoleImage'
        description: The JSON representation of a RoleImage object.
        required: true
      tags:
      - Roles
      summary: Create by account id roles by role id images by image id actions replace
      x-summary-source: derived
      operationId: postByAccountIdRolesByRoleIdImagesByImageIdActionsReplace
      x-operation-id-source: derived
    servers:
    - url: /api/v1beta0/account
  /{accountId}/roles/{roleId}/orchestration-rules/:
    parameters:
    - description: The ID of the Account scoping this request.
      in: path
      name: accountId
      required: true
      schema:
        type: integer
    - description: The ID of a Role object.
      in: path
      name: roleId
      required: true
      x-references:
        $ref: '#/components/schemas/Role'
      schema:
        type: integer
    get:
      description: List the Orchestration Rules associated with a given Role, including Rules from higher Scopes (those are read-only).
      responses:
        200:
          description: A list of OrchestrationRule objects.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/OrchestrationRuleListResponse'
        400:
          description: A client-side error was made.
          x-errorCodes:
          - description: The request was structurally incorrect, and was not understood by the API.
            name: InvalidStructure
          - description: The request was understood by the API, but included invalid data.
            name: InvalidValue
          - description: The request wasn't understood by the API.
            name: BadRequest
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
        401:
          description: The request was not authenticated.
          x-errorCodes:
          - description: The request was not properly authenticated.
            name: BadAuthentication
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
        403:
          description: Insufficient permissions.
          x-errorCodes:
          - description: This request should be made in a different Scope.
            name: ScopeViolation
          - description: You do not have the necessary permissions to perform this request.
            name: PermissionViolation
          - description: The maximum memory limit in GB has been reached.
            name: MemoryQuotaExceeded
          - description: The vCPUs limit has been reached.
            name: VcpusQuotaExceeded
          - description: The maximum number of the Servers that you can launch or resume has been reached.
            name: ServersQuotaExceeded
          - description: The maximum number of the Servers that you can launch or resume on current Farm has been reached.
            name: ServersPerFarmQuotaExceeded
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
        404:
          description: Resource not found.
          x-errorCodes:
          - description: This endpoint does not exist.
            name: EndpointNotFound
            noDoc: true
          - description: The object you are trying to access does not exist.
            name: ObjectNotFound
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
        409:
          description: Conflict with current state.
          x-errorCodes:
          - description: These changes aren't possible while this object is in use.
            name: ObjectInUse
          - description: These changes would violate a policy.
            name: PolicyViolation
          - description: These changes would violate a unicity constraint.
            name: UnicityViolation
          - description: Some of the objects being changed are locked and cannot be changed.
            name: Locked
          - description: This Cloud platform is not enabled.
            name: NotEnabledPlatform
          - description: The Operating System does not match.
            name: OperatingSystemMismatch
          - description: Some of these settings cannot be applied, they either conflict with the current state or are invalid.
            name: UnacceptableObjectConfiguration
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
        422:
          description: This request was well-formed but was rejected due to semantic errors.
          x-errorCodes:
          - description: Some of the configuration settings cannot be applied because they conflict with the current state.
            name: ConfigurationMismatch
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
        500:
          description: A server-side error occurred.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
        501:
          description: This feature is not implemented in Scalr.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
        503:
          description: The service is currently unavailable.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
      tags:
      - Roles
      summary: Get by account id roles by role id orchestration rules
      x-summary-source: derived
      operationId: getByAccountIdRolesByRoleIdOrchestrationRules
      x-operation-id-source: derived
    post:
      description: Create a new Orchestration Rule associated to a Role.
      responses:
        201:
          description: The JSON representation of a OrchestrationRule object.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/OrchestrationRuleDetailsResponse'
        400:
          description: A client-side error was made.
          x-errorCodes:
          - description: The request was structurally incorrect, and was not understood by the API.
            name: InvalidStructure
          - description: The request was understood by the API, but included invalid data.
            name: InvalidValue
          - description: The request wasn't understood by the API.
            name: BadRequest
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
        401:
          description: The request was not authenticated.
          x-errorCodes:
          - description: The request was not properly authenticated.
            name: BadAuthentication
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
        403:
          description: Insufficient permissions.
          x-errorCodes:
          - description: This request should be made in a different Scope.
            name: ScopeViolation
          - description: You do not have the necessary permissions to perform this request.
            name: PermissionViolation
          - description: The maximum memory limit in GB has been reached.
            name: MemoryQuotaExceeded
          - description: The vCPUs limit has been reached.
            name: VcpusQuotaExceeded
          - description: The maximum number of the Servers that you can launch or resume has been reached.
            name: ServersQuotaExceeded
          - description: The maximum number of the Servers that you can launch or resume on current Farm has been reached.
            name: ServersPerFarmQuotaExceeded
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
        404:
          description: Resource not found.
          x-errorCodes:
          - description: This endpoint does not exist.
            name: EndpointNotFound
            noDoc: true
          - description: The object you are trying to access does not exist.
            name: ObjectNotFound
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
        409:
          description: Conflict with current state.
          x-errorCodes:
          - description: These changes aren't possible while this object is in use.
            name: ObjectInUse
          - description: These changes would violate a policy.
            name: PolicyViolation
          - description: These changes would violate a unicity constraint.
            name: UnicityViolation
          - description: Some of the objects being changed are locked and cannot be changed.
            name: Locked
          - description: This Cloud platform is not enabled.
            name: NotEnabledPlatform
          - description: The Operating System does not match.
            name: OperatingSystemMismatch
          - description: Some of these settings cannot be applied, they either conflict with the current state or are invalid.
            name: UnacceptableObjectConfiguration
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
        422:
          description: This request was well-formed but was rejected due to semantic errors.
          x-errorCodes:
          - description: Some of the configuration settings cannot be applied because they conflict with the current state.
            name: ConfigurationMismatch
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
        500:
          description: A server-side error occurred.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
        501:
          description: This feature is not implemented in Scalr.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
        503:
          description: The service is currently unavailable.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/OrchestrationRule'
        description: The JSON representation of a OrchestrationRule object.
        required: true
      tags:
      - Roles
      summary: Create by account id roles by role id orchestration rules
      x-summary-source: derived
      operationId: postByAccountIdRolesByRoleIdOrchestrationRules
      x-operation-id-source: derived
    servers:
    - url: /api/v1beta0/account
  /{accountId}/roles/{roleId}/orchestration-rules/{orchestrationRuleId}/:
    parameters:
    - description: The ID of the Account scoping this request.
      in: path
      name: accountId
      required: true
      schema:
        type: integer
    - description: The ID of a Role object.
      in: path
      name: roleId
      required: true
      x-references:
        $ref: '#/components/schemas/Role'
      schema:
        type: integer
    - description: The ID of a OrchestrationRule object.
      in: path
      name: orchestrationRuleId
      required: true
      x-references:
        $ref: '#/components/schemas/OrchestrationRule'
      schema:
        type: string
        maxLength: 36
        minLength: 36
        pattern: '[A-Fa-f0-9]{8}-([A-Fa-f0-9]{4}-){3}[A-Fa-f0-9]{12}'
    delete:
      description: Delete an Orchestration Rule from the Role.
      responses:
        204:
          description: Successful deletion.
        400:
          description: A client-side error was made.
          x-errorCodes:
          - description: The request was structurally incorrect, and was not understood by the API.
            name: InvalidStructure
          - description: The request was understood by the API, but included invalid data.
            name: InvalidValue
          - description: The request wasn't understood by the API.
            name: BadRequest
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
        401:
          description: The request was not authenticated.
          x-errorCodes:
          - description: The request was not properly authenticated.
            name: BadAuthentication
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
        403:
          description: Insufficient permissions.
          x-errorCodes:
          - description: This request should be made in a different Scope.
            name: ScopeViolation
          - description: You do not have the necessary permissions to perform this request.
            name: PermissionViolation
          - description: The maximum memory limit in GB has been reached.
            name: MemoryQuotaExceeded
          - description: The vCPUs limit has been reached.
            name: VcpusQuotaExceeded
          - description: The maximum number of the Servers that you can launch or resume has been reached.
            name: ServersQuotaExceeded
          - description: The maximum number of the Servers that you can launch or resume on current Farm has been reached.
            name: ServersPerFarmQuotaExceeded
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
        404:
          description: Resource not found.
          x-errorCodes:
          - description: This endpoint does not exist.
            name: EndpointNotFound
            noDoc: true
          - description: The object you are trying to access does not exist.
            name: ObjectNotFound
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
        409:
          description: Conflict with current state.
          x-errorCodes:
          - description: These changes aren't possible while this object is in use.
            name: ObjectInUse
          - description: These changes would violate a policy.
            name: PolicyViolation
          - description: These changes would violate a unicity constraint.
            name: UnicityViolation
          - description: Some of the objects being changed are locked and cannot be changed.
            name: Locked
          - description: This Cloud platform is not enabled.
            name: NotEnabledPlatform
          - description: The Operating System does not match.
            name: OperatingSystemMismatch
          - description: Some of these settings cannot be applied, they either conflict with the current state or are invalid.
            name: UnacceptableObjectConfiguration
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
        422:
          description: This request was well-formed but was rejected due to semantic errors.
          x-errorCodes:
          - description: Some of the configuration settings cannot be applied because they conflict with the current state.
            name: ConfigurationMismatch
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
        500:
          description: A server-side error occurred.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
        501:
          description: This feature is not implemented in Scalr.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
        503:
          description: The service is currently unavailable.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
      tags:
      - Roles
      summary: Delete by account id roles by role id orchestration rules by orchestration rule…
      x-summary-source: derived
      operationId: deleteByAccountIdRolesByRoleIdOrchestrationRulesByOrchestrationRuleId
      x-operation-id-source: derived
    get:
      description: Retrieve detailed information about a given Orchestration Rule associated to this Role.
      responses:
        200:
          description: The JSON representation of a OrchestrationRule object.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/OrchestrationRuleDetailsResponse'
        400:
          description: A client-side error was made.
          x-errorCodes:
          - description: The request was structurally incorrect, and was not understood by the API.
            name: InvalidStructure
          - description: The request was understood by the API, but included invalid data.
            name: InvalidValue
          - description: The request wasn't understood by the API.
            name: BadRequest
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
        401:
          description: The request was not authenticated.
          x-errorCodes:
          - description: The request was not properly authenticated.
            name: BadAuthentication
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
        403:
          description: Insufficient permissions.
          x-errorCodes:
          - description: This request should be made in a different Scope.
            name: ScopeViolation
          - description: You do not have the necessary permissions to perform this request.
            name: PermissionViolation
          - description: The maximum memory limit in GB has been reached.
            name: MemoryQuotaExceeded
          - description: The vCPUs limit has been reached.
            name: VcpusQuotaExceeded
          - description: The maximum number of the Servers that you can launch or resume has been reached.
            name: ServersQuotaExceeded
          - description: The maximum number of the Servers that you can launch or resume on current Farm has been reached.
            name: ServersPerFarmQuotaExceeded
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
        404:
          description: Resource not found.
          x-errorCodes:
          - description: This endpoint does not exist.
            name: EndpointNotFound
            noDoc: true
          - description: The object you are trying to access does not exist.
            name: ObjectNotFound
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
        409:
          description: Conflict with current state.
          x-errorCodes:
          - description: These changes aren't possible while this object is in use.
            name: ObjectInUse
          - description: These changes would violate a policy.
            name: PolicyViolation
          - description: These changes would violate a unicity constraint.
            name: UnicityViolation
          - description: Some of the objects being changed are locked and cannot be changed.
            name: Locked
          - description: This Cloud platform is not enabled.
            name: NotEnabledPlatform
          - description: The Operating System does not match.
            name: OperatingSystemMismatch
          - description: Some of these settings cannot be applied, they either conflict with the current state or are invalid.
            name: UnacceptableObjectConfiguration
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
        422:
          description: This request was well-formed but was rejected due to semantic errors.
          x-errorCodes:
          - description: Some of the configuration settings cannot be applied because they conflict with the current state.
            name: ConfigurationMismatch
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
        500:
          description: A server-side error occurred.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
        501:
          description: This feature is not implemented in Scalr.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
        503:
          description: The service is currently unavailable.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
      tags:
      - Roles
      summary: Get by account id roles by role id orchestration rules by orchestration rule id
      x-summary-source: derived
      operationId: getByAccountIdRolesByRoleIdOrchestrationRulesByOrchestrationRuleId
      x-operation-id-source: derived
    patch:
      description: Modify the attributes of a given Orchestration Rule.
      responses:
        200:
          description: The JSON representation of a OrchestrationRule object.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/OrchestrationRuleDetailsResponse'
        400:
          description: A client-side error was made.
          x-errorCodes:
          - description: The request was structurally incorrect, and was not understood by the API.
            name: InvalidStructure
          - description: The request was understood by the API, but included invalid data.
            name: InvalidValue
          - description: The request wasn't understood by the API.
            name: BadRequest
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
        401:
          description: The request was not authenticated.
          x-errorCodes:
          - description: The request was not properly authenticated.
            name: BadAuthentication
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
        403:
          description: Insufficient permissions.
          x-errorCodes:
          - description: This request should be made in a different Scope.
            name: ScopeViolation
          - description: You do not have the necessary permissions to perform this request.
            name: PermissionViolation
          - description: The maximum memory limit in GB has been reached.
            name: MemoryQuotaExceeded
          - description: The vCPUs limit has been reached.
            name: VcpusQuotaExceeded
          - description: The maximum number of the Servers that you can launch or resume has been reached.
            name: ServersQuotaExceeded
          - description: The maximum number of the Servers that you can launch or resume on current Farm has been reached.
            name: ServersPerFarmQuotaExceeded
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
        404:
          description: Resource not found.
          x-errorCodes:
          - description: This endpoint does not exist.
            name: EndpointNotFound
            noDoc: true
          - description: The object you are trying to access does not exist.
            name: ObjectNotFound
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
        409:
          description: Conflict with current state.
          x-errorCodes:
          - description: These changes aren't possible while this object is in use.
            name: ObjectInUse
          - description: These changes would violate a policy.
            name: PolicyViolation
          - description: These changes would violate a unicity constraint.
            name: UnicityViolation
          - description: Some of the objects being changed are locked and cannot be changed.
            name: Locked
          - description: This Cloud platform is not enabled.
            name: NotEnabledPlatform
          - description: The Operating System does not match.
            name: OperatingSystemMismatch
          - description: Some of these settings cannot be applied, they either conflict with the current state or are invalid.
            name: UnacceptableObjectConfiguration
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
        422:
          description: This request was well-formed but was rejected due to semantic errors.
          x-errorCodes:
          - description: Some of the configuration settings cannot be applied because they conflict with the current state.
            name: ConfigurationMismatch
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
        500:
          description: A server-side error occurred.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
        501:
          description: This feature is not implemented in Scalr.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
        503:
          description: The service is currently unavailable.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/OrchestrationRule'
        description: The JSON representation of a OrchestrationRule object.
        required: true
      tags:
      - Roles
      summary: Update by account id roles by role id orchestration rules by orchestration rule…
      x-summary-source: derived
      operationId: patchByAccountIdRolesByRoleIdOrchestrationRulesByOrchestrationRuleId
      x-operation-id-source: derived
    servers:
    - url: /api/v1beta0/account
  /roles/:
    get:
      description: List all the Roles available in the global scope.
      responses:
        200:
          description: A list of Role objects.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/RoleListResponse'
        400:
          description: A client-side error was made.
          x-errorCodes:
          - description: The request was structurally incorrect, and was not understood by the API.
            name: InvalidStructure
          - description: The request was understood by the API, but included invalid data.
            name: InvalidValue
          - description: The request wasn't understood by the API.
            name: BadRequest
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse_2'
        401:
          description: The request was not authenticated.
          x-errorCodes:
          - description: The request was not properly authenticated.
            name: BadAuthentication
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse_2'
        403:
          description: Insufficient permissions.
          x-errorCodes:
          - description: This request should be made in a different Scope.
            name: ScopeViolation
          - description: You do not have the necessary permissions to perform this request.
            name: PermissionViolation
          - description: The maximum memory limit in GB has been reached.
            name: MemoryQuotaExceeded
          - description: The vCPUs limit has been reached.
            name: VcpusQuotaExceeded
          - description: The maximum number of the Servers that you can launch or resume has been reached.
            name: ServersQuotaExceeded
          - description: The maximum number of the Servers that you can launch or resume on current Farm has been reached.
            name: ServersPerFarmQuotaExceeded
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse_2'
        404:
          description: Resource not found.
          x-errorCodes:
          - description: This endpoint does not exist.
            name: EndpointNotFound
            noDoc: true
          - description: The object you are trying to access does not exist.
            name: ObjectNotFound
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse_2'
        409:
          description: Conflict with current state.
          x-errorCodes:
          - description: These changes aren't possible while this object is in use.
            name: ObjectInUse
          - description: These changes would violate a policy.
            name: PolicyViolation
          - description: These changes would violate a unicity constraint.
            name: UnicityViolation
          - description: Some of the objects being changed are locked and cannot be changed.
            name: Locked
          - description: This Cloud platform is not enabled.
            name: NotEnabledPlatform
          - description: The Operating System does not match.
            name: OperatingSystemMismatch
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse_2'
        422:
          description: This request was well-formed but was rejected due to semantic errors.
          x-errorCodes:
          - description: Some of the configuration settings cannot be applied because they conflict with the current state.
            name: ConfigurationMismatch
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse_2'
        500:
          description: A server-side error occurred.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse_2'
        501:
          description: This feature is not implemented in Scalr.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse_2'
        503:
          description: The service is currently unavailable.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse_2'
      tags:
      - Roles
      summary: Get roles
      x-summary-source: derived
      operationId: getRoles
      x-operation-id-source: derived
    post:
      description: Create a new Role in the global scope.
      responses:
        201:
          description: The JSON representation of a Role object.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/RoleDetailsResponse'
        400:
          description: A client-side error was made.
          x-errorCodes:
          - description: The request was structurally incorrect, and was not understood by the API.
            name: InvalidStructure
          - description: The request was understood by the API, but included invalid data.
            name: InvalidValue
          - description: The request wasn't understood by the API.
            name: BadRequest
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse_2'
        401:
          description: The request was not authenticated.
          x-errorCodes:
          - description: The request was not properly authenticated.
            name: BadAuthentication
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse_2'
        403:
          description: Insufficient permissions.
          x-errorCodes:
          - description: This request should be made in a different Scope.
            name: ScopeViolation
          - description: You do not have the necessary permissions to perform this request.
            name: PermissionViolation
          - description: The maximum memory limit in GB has been reached.
            name: MemoryQuotaExceeded
          - description: The vCPUs limit has been reached.
            name: VcpusQuotaExceeded
          - description: The maximum number of the Servers that you can launch or resume has been reached.
            name: ServersQuotaExceeded
          - description: The maximum number of the Servers that you can launch or resume on current Farm has been reached.
            name: ServersPerFarmQuotaExceeded
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse_2'
        404:
          description: Resource not found.
          x-errorCodes:
          - description: This endpoint does not exist.
            name: EndpointNotFound
            noDoc: true
          - description: The object you are trying to access does not exist.
            name: ObjectNotFound
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse_2'
        409:
          description: Conflict with current state.
          x-errorCodes:
          - description: These changes aren't possible while this object is in use.
            name: ObjectInUse
          - description: These changes would violate a policy.
            name: PolicyViolation
          - description: These changes would violate a unicity constraint.
            name: UnicityViolation
          - description: Some of the objects being changed are locked and cannot be changed.
            name: Locked
          - description: This Cloud platform is not enabled.
            name: NotEnabledPlatform
          - description: The Operating System does not match.
            name: OperatingSystemMismatch
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse_2'
        422:
          description: This request was well-formed but was rejected due to semantic errors.
          x-errorCodes:
          - description: Some of the configuration settings cannot be applied because they conflict with the current state.
            name: ConfigurationMismatch
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse_2'
        500:
          description: A server-side error occurred.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse_2'
        501:
          description: This feature is not implemented in Scalr.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse_2'
        503:
          description: The service is currently unavailable.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse_2'
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/Role_2'
        description: The JSON representation of a Role object.
        required: true
      tags:
      - Roles
      summary: Create roles
      x-summary-source: derived
      operationId: postRoles
      x-operation-id-source: derived
    servers:
    - url: /api/v1beta0/global
  /roles/{roleId}/:
    parameters:
    - description: The ID of a Role object.
      in: path
      name: roleId
      required: true
      x-references:
        $ref: '#/components/schemas/Role_2'
      schema:
        type: integer
    delete:
      description: Delete a Role from the global scope.
      responses:
        204:
          description: Successful deletion.
        400:
          description: A client-side error was made.
          x-errorCodes:
          - description: The request was structurally incorrect, and was not understood by the API.
            name: InvalidStructure
          - description: The request was understood by the API, but included invalid data.
            name: InvalidValue
          - description: The request wasn't understood by the API.
            name: BadRequest
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse_2'
        401:
          description: The request was not authenticated.
          x-errorCodes:
          - description: The request was not properly authenticated.
            name: BadAuthentication
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse_2'
        403:
          description: Insufficient permissions.
          x-errorCodes:
          - description: This request should be made in a different Scope.
            name: ScopeViolation
          - description: You do not have the necessary permissions to perform this request.
            name: PermissionViolation
          - description: The maximum memory limit in GB has been reached.
            name: MemoryQuotaExceeded
          - description: The vCPUs limit has been reached.
            name: VcpusQuotaExceeded
          - description: The maximum number of the Servers that you can launch or resume has been reached.
            name: ServersQuotaExceeded
          - description: The maximum number of the Servers that you can launch or resume on current Farm has been reached.
            name: ServersPerFarmQuotaExceeded
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse_2'
        404:
          description: Resource not found.
          x-errorCodes:
          - description: This endpoint does not exist.
            name: EndpointNotFound
            noDoc: true
          - description: The object you are trying to access does not exist.
            name: ObjectNotFound
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse_2'
        409:
          description: Conflict with current state.
          x-errorCodes:
          - description: These changes aren't possible while this object is in use.
            name: ObjectInUse
          - description: These changes would violate a policy.
            name: PolicyViolation
          - description: These changes would violate a unicity constraint.
            name: UnicityViolation
          - description: Some of the objects being changed are locked and cannot be changed.
            name: Locked
          - description: This Cloud platform is not enabled.
            name: NotEnabledPlatform
          - description: The Operating System does not match.
            name: OperatingSystemMismatch
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse_2'
        422:
          description: This request was well-formed but was rejected due to semantic errors.
          x-errorCodes:
          - description: Some of the configuration settings cannot be applied because they conflict with the current state.
            name: ConfigurationMismatch
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse_2'
        500:
          description: A server-side error occurred.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse_2'
        501:
          description: This feature is not implemented in Scalr.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse_2'
        503:
          description: The service is currently unavailable.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse_2'
      tags:
      - Roles
      summary: Delete roles by role id
      x-summary-source: derived
      operationId: deleteRolesByRoleId
      x-operation-id-source: derived
    get:
      description: Retrieve detailed information about a given Role.
      responses:
        200:
          description: The JSON representation of a Role object.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/RoleDetailsResponse'
        400:
          description: A client-side error was made.
          x-errorCodes:
          - description: The request was structurally incorrect, and was not understood by the API.
            name: InvalidStructure
          - description: The request was understood by the API, but included invalid data.
            name: InvalidValue
          - description: The request wasn't understood by the API.
            name: BadRequest
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse_2'
        401:
          description: The request was not authenticated.
          x-errorCodes:
          - description: The request was not properly authenticated.
            name: BadAuthentication
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse_2'
        403:
          description: Insufficient permissions.
          x-errorCodes:
          - description: This request should be made in a different Scope.
            name: ScopeViolation
          - description: You do not have the necessary permissions to perform this request.
            name: PermissionViolation
          - description: The maximum memory limit in GB has been reached.
            name: MemoryQuotaExceeded
          - description: The vCPUs limit has been reached.
            name: VcpusQuotaExceeded
          - description: The maximum number of the Servers that you can launch or resume has been reached.
            name: ServersQuotaExceeded
          - description: The maximum number of the Servers that you can launch or resume on current Farm has been reached.
            name: ServersPerFarmQuotaExceeded
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse_2'
        404:
          description: Resource not found.
          x-errorCodes:
          - description: This endpoint does not exist.
            name: EndpointNotFound
            noDoc: true
          - description: The object you are trying to access does not exist.
            name: ObjectNotFound
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse_2'
        409:
          description: Conflict with current state.
          x-errorCodes:
          - description: These changes aren't possible while this object is in use.
            name: ObjectInUse
          - description: These changes would violate a policy.
            name: PolicyViolation
          - description: These changes would violate a unicity constraint.
            name: UnicityViolation
          - description: Some of the objects being changed are locked and cannot be changed.
            name: Locked
          - description: This Cloud platform is not enabled.
            name: NotEnabledPlatform
          - description: The Operating System does not match.
            name: OperatingSystemMismatch
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse_2'
        422:
          description: This request was well-formed but was rejected due to semantic errors.
          x-errorCodes:
          - description: Some of the configuration settings cannot be applied because they conflict with the current state.
            name: ConfigurationMismatch
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse_2'
        500:
          description: A server-side error occurred.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse_2'
        501:
          description: This feature is not implemented in Scalr.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse_2'
        503:
          description: The service is currently unavailable.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse_2'
      tags:
      - Roles
      summary: Get roles by role id
      x-summary-source: derived
      operationId: getRolesByRoleId
      x-operation-id-source: derived
    patch:
      description: Modify the attributes of a given Role.
      responses:
        200:
          description: The JSON representation of a Role object.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/RoleDetailsResponse'
        400:
          description: A client-side error was made.
          x-errorCodes:
          - description: The request was structurally incorrect, and was not understood by the API.
            name: InvalidStructure
          - description: The request was understood by the API, but included invalid data.
            name: InvalidValue
          - description: The request wasn't understood by the API.
            name: BadRequest
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse_2'
        401:
          description: The request was not authenticated.
          x-errorCodes:
          - description: The request was not properly authenticated.
            name: BadAuthentication
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse_2'
        403:
          description: Insufficient permissions.
          x-errorCodes:
          - description: This request should be made in a different Scope.
            name: ScopeViolation
          - description: You do not have the necessary permissions to perform this request.
            name: PermissionViolation
          - description: The maximum memory limit in GB has been reached.
            name: MemoryQuotaExceeded
          - description: The vCPUs limit has been reached.
            name: VcpusQuotaExceeded
          - description: The maximum number of the Servers that you can launch or resume has been reached.
            name: ServersQuotaExceeded
          - description: The maximum number of the Servers that you can launch or resume on current Farm has been reached.
            name: ServersPerFarmQuotaExceeded
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse_2'
        404:
          description: Resource not found.
          x-errorCodes:
          - description: This endpoint does not exist.
            name: EndpointNotFound
            noDoc: true
          - description: The object you are trying to access does not exist.
            name: ObjectNotFound
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse_2'
        409:
          description: Conflict with current state.
          x-errorCodes:
          - description: These changes aren't possible while this object is in use.
            name: ObjectInUse
          - description: These changes would violate a policy.
            name: PolicyViolation
          - description: These changes would violate a unicity constraint.
            name: UnicityViolation
          - description: Some of the objects being changed are locked and cannot be changed.
            name: Locked
          - description: This Cloud platform is not enabled.
            name: NotEnabledPlatform
          - description: The Operating System does not match.
            name: OperatingSystemMismatch
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse_2'
        422:
          description: This request was well-formed but was rejected due to semantic errors.
          x-errorCodes:
          - description: Some of the configuration settings cannot be applied because they conflict with the current state.
            name: ConfigurationMismatch
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse_2'
        500:
          description: A server-side error occurred.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse_2'
        501:
          description: This feature is not implemented in Scalr.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse_2'
        503:
          description: The service is currently unavailable.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse_2'
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/Role_2'
        description: The JSON representation of a Role object.
        required: true
      tags:
      - Roles
      summary: Update roles by role id
      x-summary-source: derived
      operationId: patchRolesByRoleId
      x-operation-id-source: derived
    servers:
    - url: /api/v1beta0/global
  /roles/{roleId}/actions/deprecate/:
    parameters:
    - description: The ID of a Role object.
      in: path
      name: roleId
      required: true
      x-references:
        $ref: '#/components/schemas/Role_2'
      schema:
        type: integer
    post:
      description: Deprecate a Role and set another Role to replace it.
      responses:
        200:
          description: The JSON representation of a Role object.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/RoleDetailsResponse'
        400:
          description: A client-side error was made.
          x-errorCodes:
          - description: The request was structurally incorrect, and was not understood by the API.
            name: InvalidStructure
          - description: The request was understood by the API, but included invalid data.
            name: InvalidValue
          - description: The request wasn't understood by the API.
            name: BadRequest
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse_2'
        401:
          description: The request was not authenticated.
          x-errorCodes:
          - description: The request was not properly authenticated.
            name: BadAuthentication
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse_2'
        403:
          description: Insufficient permissions.
          x-errorCodes:
          - description: This request should be made in a different Scope.
            name: ScopeViolation
          - description: You do not have the necessary permissions to perform this request.
            name: PermissionViolation
          - description: The maximum memory limit in GB has been reached.
            name: MemoryQuotaExceeded
          - description: The vCPUs limit has been reached.
            name: VcpusQuotaExceeded
          - description: The maximum number of the Servers that you can launch or resume has been reached.
            name: ServersQuotaExceeded
          - description: The maximum number of the Servers that you can launch or resume on current Farm has been reached.
            name: ServersPerFarmQuotaExceeded
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse_2'
        404:
          description: Resource not found.
          x-errorCodes:
          - description: This endpoint does not exist.
            name: EndpointNotFound
            noDoc: true
          - description: The object you are trying to access does not exist.
            name: ObjectNotFound
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse_2'
        409:
          description: Conflict with current state.
          x-errorCodes:
          - description: These changes aren't possible while this object is in use.
            name: ObjectInUse
          - description: These changes would violate a policy.
            name: PolicyViolation
          - description: These changes would violate a unicity constraint.
            name: UnicityViolation
          - description: Some of the objects being changed are locked and cannot be changed.
            name: Locked
          - description: This Cloud platform is not enabled.
            name: NotEnabledPlatform
          - description: The Operating System does not match.
            name: OperatingSystemMismatch
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse_2'
        422:
          description: This request was well-formed but was rejected due to semantic errors.
          x-errorCodes:
          - description: Some of the configuration settings cannot be applied because they conflict with the current state.
            name: ConfigurationMismatch
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse_2'
        500:
          description: A server-side error occurred.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse_2'
        501:
          description: This feature is not implemented in Scalr.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse_2'
        503:
          description: The service is currently unavailable.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse_2'
      requestBody:
        content:
          application/json:
            schema:
              properties:
                deprecate:
                  default: true
                  description: By default deprecate Role If a value is true clears the deprecate from Role.
                  type: boolean
                replacement:
                  $ref: '#/components/schemas/RoleForeignKey'
                  description: The suggested replacement Role foreign key for the current Role.
        description: Deprecate Role options.
      tags:
      - Roles
      summary: Create roles by role id actions deprecate
      x-summary-source: derived
      operationId: postRolesByRoleIdActionsDeprecate
      x-operation-id-source: derived
    servers:
    - url: /api/v1beta0/global
  /roles/{roleId}/global-variables/:
    parameters:
    - description: The ID of a Role object.
      in: path
      name: roleId
      required: true
      x-references:
        $ref: '#/components/schemas/Role_2'
      schema:
        type: integer
    get:
      description: List all the Global Variables associated with a given Role.
      responses:
        200:
          description: A list of GlobalVariable objects.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/GlobalVariableListResponse_2'
        400:
          description: A client-side error was made.
          x-errorCodes:
          - description: The request was structurally incorrect, and was not understood by the API.
            name: InvalidStructure
          - description: The request was understood by the API, but included invalid data.
            name: InvalidValue
          - description: The request wasn't understood by the API.
            name: BadRequest
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse_2'
        401:
          description: The request was not authenticated.
          x-errorCodes:
          - description: The request was not properly authenticated.
            name: BadAuthentication
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse_2'
        403:
          description: Insufficient permissions.
          x-errorCodes:
          - description: This request should be made in a different Scope.
            name: ScopeViolation
          - description: You do not have the necessary permissions to perform this request.
            name: PermissionViolation
          - description: The maximum memory limit in GB has been reached.
            name: MemoryQuotaExceeded
          - description: The vCPUs limit has been reached.
            name: VcpusQuotaExceeded
          - description: The maximum number of the Servers that you can launch or resume has been reached.
            name: ServersQuotaExceeded
          - description: The maximum number of the Servers that you can launch or resume on current Farm has been reached.
            name: ServersPerFarmQuotaExceeded
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse_2'
        404:
          description: Resource not found.
          x-errorCodes:
          - description: This endpoint does not exist.
            name: EndpointNotFound
            noDoc: true
          - description: The object you are trying to access does not exist.
            name: ObjectNotFound
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse_2'
        409:
          description: Conflict with current state.
          x-errorCodes:
          - description: These changes aren't possible while this object is in use.
            name: ObjectInUse
          - description: These changes would violate a policy.
            name: PolicyViolation
          - description: These changes would violate a unicity constraint.
            name: UnicityViolation
          - description: Some of the objects being changed are locked and cannot be changed.
            name: Locked
          - description: This Cloud platform is not enabled.
            name: NotEnabledPlatform
          - description: The Operating System does not match.
            name: OperatingSystemMismatch
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse_2'
        422:
          description: This request was well-formed but was rejected due to semantic errors.
          x-errorCodes:
          - description: Some of the configuration settings cannot be applied because they conflict with the current state.
            name: ConfigurationMismatch
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse_2'
        500:
          description: A server-side error occurred.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse_2'
        501:
          description: This feature is not implemented in Scalr.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse_2'
        503:
          description: The service is currently unavailable.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse_2'
      tags:
      - Roles
      summary: Get roles by role id global variables
      x-summary-source: derived
      operationId: getRolesByRoleIdGlobalVariables
      x-operation-id-source: derived
    post:
      description: Declares a new Global Variable for this Role.
      responses:
        201:
          description: The JSON representation of a GlobalVariable object.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/GlobalVariableDetailsResponse_2'
        400:
          description: A client-side error was made.
          x-errorCodes:
          - description: The request was structurally incorrect, and was not understood by the API.
            name: InvalidStructure
          - description: The request was understood by the API, but included invalid data.
            name: InvalidValue
          - description: The request wasn't understood by the API.
            name: BadRequest
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse_2'
        401:
          description: The request was not authenticated.
          x-errorCodes:
          - description: The request was not properly authenticated.
            name: BadAuthentication
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse_2'
        403:
          description: Insufficient permissions.
          x-errorCodes:
          - description: This request should be made in a different Scope.
            name: ScopeViolation
          - description: You do not have the necessary permissions to perform this request.
            name: PermissionViolation
          - description: The maximum memory limit in GB has been reached.
            name: MemoryQuotaExceeded
          - description: The vCPUs limit has been reached.
            name: VcpusQuotaExceeded
          - description: The maximum number of the Servers that you can launch or resume has been reached.
            name: ServersQuotaExceeded
          - description: The maximum number of the Servers that you can launch or resume on current Farm has been reached.
            name: ServersPerFarmQuotaExceeded
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse_2'
        404:
          description: Resource not found.
          x-errorCodes:
          - description: This endpoint does not exist.
            name: EndpointNotFound
            noDoc: true
          - description: The object you are trying to access does not exist.
            name: ObjectNotFound
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse_2'
        409:
          description: Conflict with current state.
          x-errorCodes:
          - description: These changes aren't possible while this object is in use.
            name: ObjectInUse
          - description: These changes would violate a policy.
            name: PolicyViolation
          - description: These changes would violate a unicity constraint.
            name: UnicityViolation
          - description: Some of the objects being changed are locked and cannot be changed.
            name: Locked
          - description: This Cloud platform is not enabled.
            name: NotEnabledPlatform
          - description: The Operating System does not match.
            name: OperatingSystemMismatch
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse_2'
        422:
          description: This request was well-formed but was rejected due to semantic errors.
          x-errorCodes:
          - description: Some of the configuration settings cannot be applied because they conflict with the current state.
            name: ConfigurationMismatch
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse_2'
        500:
          description: A server-side error occurred.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse_2'
        501:
          description: This feature is not implemented in Scalr.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse_2'
        503:
          description: The service is currently unavailable.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse_2'
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/GlobalVariable_2'
        description: The JSON representation of a GlobalVariable object.
        required: true
      tags:
      - Roles
      summary: Create roles by role id global variables
      x-summary-source: derived
      operationId: postRolesByRoleIdGlobalVariables
      x-operation-id-source: derived
    servers:
    - url: /api/v1beta0/global
  /roles/{roleId}/global-variables/{globalVariableName}/:
    parameters:
    - description: The ID of a Role object.
      in: path
      name: roleId
      required: true
      x-references:
        $ref: '#/components/schemas/Role_2'
      schema:
        type: integer
    - description: The name of the Global Variable being accessed.
      in: path
      name: globalVariableName
      required: true
      schema:
        type: string
        maxLength: 128
        minLength: 2
        pattern: \w+
    delete:
      description: Deletes a Global Variable from a given Role's scope.
      responses:
        204:
          description: Successful deletion.
        400:
          description: A client-side error was made.
          x-errorCodes:
          - description: The request was structurally incorrect, and was not understood by the API.
            name: InvalidStructure
          - description: The request was understood by the API, but included invalid data.
            name: InvalidValue
          - description: The request wasn't understood by the API.
            name: BadRequest
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse_2'
        401:
          description: The request was not authenticated.
          x-errorCodes:
          - description: The request was not properly authenticated.
            name: BadAuthentication
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse_2'
        403:
          description: Insufficient permissions.
          x-errorCodes:
          - description: This request should be made in a different Scope.
            name: ScopeViolation
          - description: You do not have the necessary permissions to perform this request.
            name: PermissionViolation
          - description: The maximum memory limit in GB has been reached.
            name: MemoryQuotaExceeded
          - description: The vCPUs limit has been reached.
            name: VcpusQuotaExceeded
          - description: The maximum number of the Servers that you can launch or resume has been reached.
            name: ServersQuotaExceeded
          - description: The maximum number of the Servers that you can launch or resume on current Farm has been reached.
            name: ServersPerFarmQuotaExceeded
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse_2'
        404:
          description: Resource not found.
          x-errorCodes:
          - description: This endpoint does not exist.
            name: EndpointNotFound
            noDoc: true
          - description: The object you are trying to access does not exist.
            name: ObjectNotFound
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse_2'
        409:
          description: Conflict with current state.
          x-errorCodes:
          - description: These changes aren't possible while this object is in use.
            name: ObjectInUse
          - description: These changes would violate a policy.
            name: PolicyViolation
          - description: These changes would violate a unicity constraint.
            name: UnicityViolation
          - description: Some of the objects being changed are locked and cannot be changed.
            name: Locked
          - description: This Cloud platform is not enabled.
            name: NotEnabledPlatform
          - description: The Operating System does not match.
            name: OperatingSystemMismatch
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse_2'
        422:
          description: This request was well-formed but was rejected due to semantic errors.
          x-errorCodes:
          - description: Some of the configuration settings cannot be applied because they conflict with the current state.
            name: ConfigurationMismatch
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse_2'
        500:
          description: A server-side error occurred.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse_2'
        501:
          description: This feature is not implemented in Scalr.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse_2'
        503:
          description: The service is currently unavailable.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse_2'
      tags:
      - Roles
      summary: Delete roles by role id global variables by global variable name
      x-summary-source: derived
      operationId: deleteRolesByRoleIdGlobalVariablesByGlobalVariableName
      x-operation-id-source: derived
    get:
      description: Retrieve detailed information about a Global Variable in the Role's scope.
      responses:
        200:
          description: The JSON representation of a GlobalVariable object.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/GlobalVariableDetailsResponse_2'
        400:
          description: A client-side error was made.
          x-errorCodes:
          - description: The request was structurally incorrect, and was not understood by the API.
            name: InvalidStructure
          - description: The request was understood by the API, but included invalid data.
            name: InvalidValue
          - description: The request wasn't understood by the API.
            name: BadRequest
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse_2'
        401:
          description: The request was not authenticated.
          x-errorCodes:
          - description: The request was not properly authenticated.
            name: BadAuthentication
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse_2'
        403:
          description: Insufficient permissions.
          x-errorCodes:
          - description: This request should be made in a different Scope.
            name: ScopeViolation
          - description: You do not have the necessary permissions to perform this request.
            name: PermissionViolation
          - description: The maximum memory limit in GB has been reached.
            name: MemoryQuotaExceeded
          - description: The vCPUs limit has been reached.
            name: VcpusQuotaExceeded
          - description: The maximum number of the Servers that you can launch or resume has been reached.
            name: ServersQuotaExceeded
          - description: The maximum number of the Servers that you can launch or resume on current Farm has been reached.
            name: ServersPerFarmQuotaExceeded
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse_2'
        404:
          description: Resource not found.
          x-errorCodes:
          - description: This endpoint does not exist.
            name: EndpointNotFound
            noDoc: true
          - description: The object you are trying to access does not exist.
            name: ObjectNotFound
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse_2'
        409:
          description: Conflict with current state.
          x-errorCodes:
          - description: These changes aren't possible while this object is in use.
            name: ObjectInUse
          - description: These changes would violate a policy.
            name: PolicyViolation
          - description: These changes would violate a unicity constraint.
            name: UnicityViolation
          - description: Some of the objects being changed are locked and cannot be changed.
            name: Locked
          - description: This Cloud platform is not enabled.
            name: NotEnabledPlatform
          - description: The Operating System does not match.
            name: OperatingSystemMismatch
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse_2'
        422:
          description: This request was well-formed but was rejected due to semantic errors.
          x-errorCodes:
          - description: Some of the configuration settings cannot be applied because they conflict with the current state.
            name: ConfigurationMismatch
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse_2'
        500:
          description: A server-side error occurred.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse_2'
        501:
          description: This feature is not implemented in Scalr.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse_2'
        503:
          description: The service is currently unavailable.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse_2'
      tags:
      - Roles
      summary: Get roles by role id global variables by global variable name
      x-summary-source: derived
      operationId: getRolesByRoleIdGlobalVariablesByGlobalVariableName
      x-operation-id-source: derived
    patch:
      description: Modify the attributes of a Global Variable in the Role's scope.
      responses:
        200:
          description: The JSON representation of a GlobalVariable object.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/GlobalVariableDetailsResponse_2'
        400:
          description: A client-side error was made.
          x-errorCodes:
          - description: The request was structurally incorrect, and was not understood by the API.
            name: InvalidStructure
          - description: The request was understood by the API, but included invalid data.
            name: InvalidValue
          - description: The request wasn't understood by the API.
            name: BadRequest
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse_2'
        401:
          description: The request was not authenticated.
          x-errorCodes:
          - description: The request was not properly authenticated.
            name: BadAuthentication
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse_2'
        403:
          description: Insufficient permissions.
          x-errorCodes:
          - description: This request should be made in a different Scope.
            name: ScopeViolation
          - description: You do not have the necessary permissions to perform this request.
            name: PermissionViolation
          - description: The maximum memory limit in GB has been reached.
            name: MemoryQuotaExceeded
          - description: The vCPUs limit has been reached.
            name: VcpusQuotaExceeded
          - description: The maximum number of the Servers that you can launch or resume has been reached.
            name: ServersQuotaExceeded
          - description: The maximum number of the Servers that you can launch or resume on current Farm has been reached.
            name: ServersPerFarmQuotaExceeded
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse_2'
        404:
          description: Resource not found.
          x-errorCodes:
          - description: This endpoint does not exist.
            name: EndpointNotFound
            noDoc: true
          - description: The object you are trying to access does not exist.
            name: ObjectNotFound
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse_2'
        409:
          description: Conflict with current state.
          x-errorCodes:
          - description: These changes aren't possible while this object is in use.
            name: ObjectInUse
          - description: These changes would violate a policy.
            name: PolicyViolation
          - description: These changes would violate a unicity constraint.
            name: UnicityViolation
          - description: Some of the objects being changed are locked and cannot be changed.
            name: Locked
          - description: This Cloud platform is not enabled.
            name: NotEnabledPlatform
          - description: The Operating System does not match.
            name: OperatingSystemMismatch
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse_2'
        422:
          description: This request was well-formed but was rejected due to semantic errors.
          x-errorCodes:
          - description: Some of the configuration settings cannot be applied because they conflict with the current state.
            name: ConfigurationMismatch
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse_2'
        500:
          description: A server-side error occurred.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse_2'
        501:
          description: This feature is not implemented in Scalr.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse_2'
        503:
          description: The service is currently unavailable.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse_2'
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/GlobalVariable_2'
        description: The JSON representation of a GlobalVariable object.
        required: true
      tags:
      - Roles
      summary: Update roles by role id global variables by global variable name
      x-summary-source: derived
      operationId: patchRolesByRoleIdGlobalVariablesByGlobalVariableName
      x-operation-id-source: derived
    servers:
    - url: /api/v1beta0/global
  /roles/{roleId}/images/:
    parameters:
    - description: The ID of a Role object.
      in: path
      name: roleId
      required: true
      x-references:
        $ref: '#/components/schemas/Role_2'
      schema:
        type: integer
    get:
      description: List all the Images associated with the specified Role.
      responses:
        200:
          description: A list of RoleImage objects.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/RoleImageListResponse_2'
        400:
          description: A client-side error was made.
          x-errorCodes:
          - description: The request was structurally incorrect, and was not understood by the API.
            name: InvalidStructure
          - description: The request was understood by the API, but included invalid data.
            name: InvalidValue
          - description: The request wasn't understood by the API.
            name: BadRequest
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse_2'
        401:
          description: The request was not authenticated.
          x-errorCodes:
          - description: The request was not properly authenticated.
            name: BadAuthentication
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse_2'
        403:
          description: Insufficient permissions.
          x-errorCodes:
          - description: This request should be made in a different Scope.
            name: ScopeViolation
          - description: You do not have the necessary permissions to perform this request.
            name: PermissionViolation
          - description: The maximum memory limit in GB has been reached.
            name: MemoryQuotaExceeded
          - description: The vCPUs limit has been reached.
            name: VcpusQuotaExceeded
          - description: The maximum number of the Servers that you can launch or resume has been reached.
            name: ServersQuotaExceeded
          - description: The maximum number of the Servers that you can launch or resume on current Farm has been reached.
            name: ServersPerFarmQuotaExceeded
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse_2'
        404:
          description: Resource not found.
          x-errorCodes:
          - description: This endpoint does not exist.
            name: EndpointNotFound
            noDoc: true
          - description: The object you are trying to access does not exist.
            name: ObjectNotFound
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse_2'
        409:
          description: Conflict with current state.
          x-errorCodes:
          - description: These changes aren't possible while this object is in use.
            name: ObjectInUse
          - description: These changes would violate a policy.
            name: PolicyViolation
          - description: These changes would violate a unicity constraint.
            name: UnicityViolation
          - description: Some of the objects being changed are locked and cannot be changed.
            name: Locked
          - description: This Cloud platform is not enabled.
            name: NotEnabledPlatform
          - description: The Operating System does not match.
            name: OperatingSystemMismatch
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse_2'
        422:
          description: This request was well-formed but was rejected due to semantic errors.
          x-errorCodes:
          - description: Some of the configuration settings cannot be applied because they conflict with the current state.
            name: ConfigurationMismatch
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse_2'
        500:
          description: A server-side error occurred.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse_2'
        501:
          description: This feature is not implemented in Scalr.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse_2'
        503:
          description: The service is currently unavailable.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse_2'
      tags:
      - Roles
      summary: Get roles by role id images
      x-summary-source: derived
      operationId: getRolesByRoleIdImages
      x-operation-id-source: derived
    post:
      description: Associate a new Image with this Role. This will fail if an Image associated to the Role is in the same location than the Image you are trying to add.
      responses:
        201:
          description: The JSON representation of a RoleImage object.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/RoleImageDetailsResponse_2'
        400:
          description: A client-side error was made.
          x-errorCodes:
          - description: The request was structurally incorrect, and was not understood by the API.
            name: InvalidStructure
          - description: The request was understood by the API, but included invalid data.
            name: InvalidValue
          - description: The request wasn't understood by the API.
            name: BadRequest
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse_2'
        401:
          description: The request was not authenticated.
          x-errorCodes:
          - description: The request was not properly authenticated.
            name: BadAuthentication
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse_2'
        403:
          description: Insufficient permissions.
          x-errorCodes:
          - description: This request should be made in a different Scope.
            name: ScopeViolation
          - description: You do not have the necessary permissions to perform this request.
            name: PermissionViolation
          - description: The maximum memory limit in GB has been reached.
            name: MemoryQuotaExceeded
          - description: The vCPUs limit has been reached.
            name: VcpusQuotaExceeded
          - description: The maximum number of the Servers that you can launch or resume has been reached.
            name: ServersQuotaExceeded
          - description: The maximum number of the Servers that you can launch or resume on current Farm has been reached.
            name: ServersPerFarmQuotaExceeded
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse_2'
        404:
          description: Resource not found.
          x-errorCodes:
          - description: This endpoint does not exist.
            name: EndpointNotFound
            noDoc: true
          - description: The object you are trying to access does not exist.
            name: ObjectNotFound
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse_2'
        409:
          description: Conflict with current state.
          x-errorCodes:
          - description: These changes aren't possible while this object is in use.
            name: ObjectInUse
          - description: These changes would violate a policy.
            name: PolicyViolation
          - description: These changes would violate a unicity constraint.
            name: UnicityViolation
          - description: Some of the objects being changed are locked and cannot be changed.
            name: Locked
          - description: This Cloud platform is not enabled.
            name: NotEnabledPlatform
          - description: The Operating System does not match.
            name: OperatingSystemMismatch
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse_2'
        422:
          description: This request was well-formed but was rejected due to semantic errors.
          x-errorCodes:
          - description: Some of the configuration settings cannot be applied because they conflict with the current state.
            name: ConfigurationMismatch
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse_2'
        500:
          description: A server-side error occurred.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse_2'
        501:
          description: This feature is not implemented in Scalr.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse_2'
        503:
          description: The service is currently unavailable.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse_2'
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/RoleImage_2'
        description: The JSON representation of a RoleImage object.
        required: true
      tags:
      - Roles
      summary: Create roles by role id images
      x-summary-source: derived
      operationId: postRolesByRoleIdImages
      x-operation-id-source: derived
    servers:
    - url: /api/v1beta0/global
  /roles/{roleId}/images/{imageId}/:
    parameters:
    - description: The ID of a Role object.
      in: path
      name: roleId
      required: true
      x-references:
        $ref: '#/components/schemas/Role_2'
      schema:
        type: integer
    - description: The ID of a Image object.
      in: path
      name: imageId
      required: true
      x-references:
        $ref: '#/components/schemas/Image_2'
      schema:
        type: string
        maxLength: 36
        minLength: 36
        pattern: '[A-Fa-f0-9]{8}-([A-Fa-f0-9]{4}-){3}[A-Fa-f0-9]{12}'
    delete:
      description: Dis-associates an Image from a given Role.
      responses:
        204:
          description: Successful deletion.
        400:
          description: A client-side error was made.
          x-errorCodes:
          - description: The request was structurally incorrect, and was not understood by the API.
            name: InvalidStructure
          - description: The request was understood by the API, but included invalid data.
            name: InvalidValue
          - description: The request wasn't understood by the API.
            name: BadRequest
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse_2'
        401:
          description: The request was not authenticated.
          x-errorCodes:
          - description: The request was not properly authenticated.
            name: BadAuthentication
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse_2'
        403:
          description: Insufficient permissions.
          x-errorCodes:
          - description: This request should be made in a different Scope.
            name: ScopeViolation
          - description: You do not have the necessary permissions to perform this request.
            name: PermissionViolation
          - description: The maximum memory limit in GB has been reached.
            name: MemoryQuotaExceeded
          - description: The vCPUs limit has been reached.
            name: VcpusQuotaExceeded
          - description: The maximum number of the Servers that you can launch or resume has been reached.
            name: ServersQuotaExceeded
          - description: The maximum number of the Servers that you can launch or resume on current Farm has been reached.
            name: ServersPerFarmQuotaExceeded
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse_2'
        404:
          description: Resource not found.
          x-errorCodes:
          - description: This endpoint does not exist.
            name: EndpointNotFound
            noDoc: true
          - description: The object you are trying to access does not exist.
            name: ObjectNotFound
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse_2'
        409:
          description: Conflict with current state.
          x-errorCodes:
          - description: These changes aren't possible while this object is in use.
            name: ObjectInUse
          - description: These changes would violate a policy.
            name: PolicyViolation
          - description: These changes would violate a unicity constraint.
            name: UnicityViolation
          - description: Some of the objects being changed are locked and cannot be changed.
            name: Locked
          - description: This Cloud platform is not enabled.
            name: NotEnabledPlatform
          - description: The Operating System does not match.
            name: OperatingSystemMismatch
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse_2'
        422:
          description: This request was well-formed but was rejected due to semantic errors.
          x-errorCodes:
          - description: Some of the configuration settings cannot be applied because they conflict with the current state.
            name: ConfigurationMismatch
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse_2'
        500:
          description: A server-side error occurred.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse_2'
        501:
          description: This feature is not implemented in Scalr.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse_2'
        503:
          description: The service is currently unavailable.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse_2'
      tags:
      - Roles
      summary: Delete roles by role id images by image id
      x-summary-source: derived
      operationId: deleteRolesByRoleIdImagesByImageId
      x-operation-id-source: derived
    get:
      description: Retrieve detailed information about an Image.
      responses:
        200:
          description: Redirects to the Image.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ImageDetailsResponse_2'
        400:
          description: A client-side error was made.
          x-errorCodes:
          - description: The request was structurally incorrect, and was not understood by the API.
            name: InvalidStructure
          - description: The request was understood by the API, but included invalid data.
            name: InvalidValue
          - description: The request wasn't understood by the API.
            name: BadRequest
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse_2'
        401:
          description: The request was not authenticated.
          x-errorCodes:
          - description: The request was not properly authenticated.
            name: BadAuthentication
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse_2'
        403:
          description: Insufficient permissions.
          x-errorCodes:
          - description: This request should be made in a different Scope.
            name: ScopeViolation
          - description: You do not have the necessary permissions to perform this request.
            name: PermissionViolation
          - description: The maximum memory limit in GB has been reached.
            name: MemoryQuotaExceeded
          - description: The vCPUs limit has been reached.
            name: VcpusQuotaExceeded
          - description: The maximum number of the Servers that you can launch or resume has been reached.
            name: ServersQuotaExceeded
          - description: The maximum number of the Servers that you can launch or resume on current Farm has been reached.
            name: ServersPerFarmQuotaExceeded
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse_2'
        404:
          description: Resource not found.
          x-errorCodes:
          - description: This endpoint does not exist.
            name: EndpointNotFound
            noDoc: true
          - description: The object you are trying to access does not exist.
            name: ObjectNotFound
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse_2'
        409:
          description: Conflict with current state.
          x-errorCodes:
          - description: These changes aren't possible while this object is in use.
            name: ObjectInUse
          - description: These changes would violate a policy.
            name: PolicyViolation
          - description: These changes would violate a unicity constraint.
            name: UnicityViolation
          - description: Some of the objects being changed are locked and cannot be changed.
            name: Locked
          - description: This Cloud platform is not enabled.
            name: NotEnabledPlatform
          - description: The Operating System does not match.
            name: OperatingSystemMismatch
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse_2'
        422:
          description: This request was well-formed but was rejected due to semantic errors.
          x-errorCodes:
          - description: Some of the configuration settings cannot be applied because they conflict with the current state.
            name: ConfigurationMismatch
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse_2'
        500:
          description: A server-side error occurred.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse_2'
        501:
          description: This feature is not implemented in Scalr.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse_2'
        503:
          description: The service is currently unavailable.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse_2'
      tags:
      - Roles
      summary: Get roles by role id images by image id
      x-summary-source: derived
      operationId: getRolesByRoleIdImagesByImageId
      x-operation-id-source: derived
    servers:
    - url: /api/v1beta0/global
  /roles/{roleId}/images/{imageId}/actions/replace/:
    parameters:
    - description: The ID of a Role object.
      in: path
      name: roleId
      required: true
      x-references:
        $ref: '#/components/schemas/Role_2'
      schema:
        type: integer
    - description: The ID of a Image object.
      in: path
      name: imageId
      required: true
      x-references:
        $ref: '#/components/schemas/Image_2'
      schema:
        type: string
        maxLength: 36
        minLength: 36
        pattern: '[A-Fa-f0-9]{8}-([A-Fa-f0-9]{4}-){3}[A-Fa-f0-9]{12}'
    post:
      description: Replaces an Image in-place in a given Role. This allows you to replace an Image without having to dis-associate it and then associate a new one, which may not be possible if a Farm Role is currently using this Image.
      responses:
        200:
          description: The JSON representation of a RoleImage object.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/RoleImageDetailsResponse_2'
        400:
          description: A client-side error was made.
          x-errorCodes:
          - description: The request was structurally incorrect, and was not understood by the API.
            name: InvalidStructure
          - description: The request was understood by the API, but included invalid data.
            name: InvalidValue
          - description: The request wasn't understood by the API.
            name: BadRequest
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse_2'
        401:
          description: The request was not authenticated.
          x-errorCodes:
          - description: The request was not properly authenticated.
            name: BadAuthentication
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse_2'
        403:
          description: Insufficient permissions.
          x-errorCodes:
          - description: This request should be made in a different Scope.
            name: ScopeViolation
          - description: You do not have the necessary permissions to perform this request.
            name: PermissionViolation
          - description: The maximum memory limit in GB has been reached.
            name: MemoryQuotaExceeded
          - description: The vCPUs limit has been reached.
            name: VcpusQuotaExceeded
          - description: The maximum number of the Servers that you can launch or resume has been reached.
            name: ServersQuotaExceeded
          - description: The maximum number of the Servers that you can launch or resume on current Farm has been reached.
            name: ServersPerFarmQuotaExceeded
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse_2'
        404:
          description: Resource not found.
          x-errorCodes:
          - description: This endpoint does not exist.
            name: EndpointNotFound
            noDoc: true
          - description: The object you are trying to access does not exist.
            name: ObjectNotFound
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse_2'
        409:
          description: Conflict with current state.
          x-errorCodes:
          - description: These changes aren't possible while this object is in use.
            name: ObjectInUse
          - description: These changes would violate a policy.
            name: PolicyViolation
          - description: These changes would violate a unicity constraint.
            name: UnicityViolation
          - description: Some of the objects being changed are locked and cannot be changed.
            name: Locked
          - description: This Cloud platform is not enabled.
            name: NotEnabledPlatform
          - description: The Operating System does not match.
            name: OperatingSystemMismatch
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse_2'
        422:
          description: This request was well-formed but was rejected due to semantic errors.
          x-errorCodes:
          - description: Some of the configuration settings cannot be applied because they conflict with the current state.
            name: ConfigurationMismatch
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse_2'
        500:
          description: A server-side error occurred.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse_2'
        501:
          description: This feature is not implemented in Scalr.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse_2'
        503:
          description: The service is currently unavailable.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse_2'
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/RoleImage_2'
        description: The JSON representation of a RoleImage object.
        required: true
      tags:
      - Roles
      summary: Create roles by role id images by image id actions replace
      x-summary-source: derived
      operationId: postRolesByRoleIdImagesByImageIdActionsReplace
      x-operation-id-source: derived
    servers:
    - url: /api/v1beta0/global
  /roles/{roleId}/orchestration-rules/:
    parameters:
    - description: The ID of a Role object.
      in: path
      name: roleId
      required: true
      x-references:
        $ref: '#/components/schemas/Role_2'
      schema:
        type: integer
    get:
      description: List the Orchestration Rules associated to a Role.
      responses:
        200:
          description: A list of OrchestrationRule objects.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/OrchestrationRuleListResponse_2'
        400:
          description: A client-side error was made.
          x-errorCodes:
          - description: The request was structurally incorrect, and was not understood by the API.
            name: InvalidStructure
          - description: The request was understood by the API, but included invalid data.
            name: InvalidValue
          - description: The request wasn't understood by the API.
            name: BadRequest
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse_2'
        401:
          description: The request was not authenticated.
          x-errorCodes:
          - description: The request was not properly authenticated.
            name: BadAuthentication
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse_2'
        403:
          description: Insufficient permissions.
          x-errorCodes:
          - description: This request should be made in a different Scope.
            name: ScopeViolation
          - description: You do not have the necessary permissions to perform this request.
            name: PermissionViolation
          - description: The maximum memory limit in GB has been reached.
            name: MemoryQuotaExceeded
          - description: The vCPUs limit has been reached.
            name: VcpusQuotaExceeded
          - description: The maximum number of the Servers that you can launch or resume has been reached.
            name: ServersQuotaExceeded
          - description: The maximum number of the Servers that you can launch or resume on current Farm has been reached.
            name: ServersPerFarmQuotaExceeded
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse_2'
        404:
          description: Resource not found.
          x-errorCodes:
          - description: This endpoint does not exist.
            name: EndpointNotFound
            noDoc: true
          - description: The object you are trying to access does not exist.
            name: ObjectNotFound
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse_2'
        409:
          description: Conflict with current state.
          x-errorCodes:
          - description: These changes aren't possible while this object is in use.
            name: ObjectInUse
          - description: These changes would violate a policy.
            name: PolicyViolation
          - description: These changes would violate a unicity constraint.
            name: UnicityViolation
          - description: Some of the objects being changed are locked and cannot be changed.
            name: Locked
          - description: This Cloud platform is not enabled.
            name: NotEnabledPlatform
          - description: The Operating System does not match.
            name: OperatingSystemMismatch
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse_2'
        422:
          description: This request was well-formed but was rejected due to semantic errors.
          x-errorCodes:
          - description: Some of the configuration settings cannot be applied because they conflict with the current state.
            name: ConfigurationMismatch
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse_2'
        500:
          description: A server-side error occurred.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse_2'
        501:
          description: This feature is not implemented in Scalr.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse_2'
        503:
          description: The service is currently unavailable.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse_2'
      tags:
      - Roles
      summary: Get roles by role id orchestration rules
      x-summary-source: derived
      operationId: getRolesByRoleIdOrchestrationRules
      x-operation-id-source: derived
    post:
      description: Create a new Orchestration Rule associated to a Role.
      responses:
        201:
          description: The JSON representation of a OrchestrationRule object.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/OrchestrationRuleDetailsResponse_2'
        400:
          description: A client-side error was made.
          x-errorCodes:
          - description: The request was structurally incorrect, and was not understood by the API.
            name: InvalidStructure
          - description: The request was understood by the API, but included invalid data.
            name: InvalidValue
          - description: The request wasn't understood by the API.
            name: BadRequest
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse_2'
        401:
          description: The request was not authenticated.
          x-errorCodes:
          - description: The request was not properly authenticated.
            name: BadAuthentication
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse_2'
        403:
          description: Insufficient permissions.
          x-errorCodes:
          - description: This request should be made in a different Scope.
            name: ScopeViolation
          - description: You do not have the necessary permissions to perform this request.
            name: PermissionViolation
          - description: The maximum memory limit in GB has been reached.
            name: MemoryQuotaExceeded
          - description: The vCPUs limit has been reached.
            name: VcpusQuotaExceeded
          - description: The maximum number of the Servers that you can launch or resume has been reached.
            name: ServersQuotaExceeded
          - description: The maximum number of the Servers that you can launch or resume on current Farm has been reached.
            name: ServersPerFarmQuotaExceeded
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse_2'
        404:
          description: Resource not found.
          x-errorCodes:
          - description: This endpoint does not exist.
            name: EndpointNotFound
            noDoc: true
          - description: The object you are trying to access does not exist.
            name: ObjectNotFound
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse_2'
        409:
          description: Conflict with current state.
          x-errorCodes:
          - description: These changes aren't possible while this object is in use.
            name: ObjectInUse
          - description: These changes would violate a policy.
            name: PolicyViolation
          - description: These changes would violate a unicity constraint.
            name: UnicityViolation
          - description: Some of the objects being changed are locked and cannot be changed.
            name: Locked
          - description: This Cloud platform is not enabled.
            name: NotEnabledPlatform
          - description: The Operating System does not match.
            name: OperatingSystemMismatch
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse_2'
        422:
          description: This request was well-formed but was rejected due to semantic errors.
          x-errorCodes:
          - description: Some of the configuration settings cannot be applied because they conflict with the current state.
            name: ConfigurationMismatch
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse_2'
        500:
          description: A server-side error occurred.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse_2'
        501:
          description: This feature is not implemented in Scalr.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse_2'
        503:
          description: The service is currently unavailable.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse_2'
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/OrchestrationRule_2'
        description: The JSON representation of a OrchestrationRule object.
        required: true
      tags:
      - Roles
      summary: Create roles by role id orchestration rules
      x-summary-source: derived
      operationId: postRolesByRoleIdOrchestrationRules
      x-operation-id-source: derived
    servers:
    - url: /api/v1beta0/global
  /roles/{roleId}/orchestration-rules/{orchestrationRuleId}/:
    parameters:
    - description: The ID of a Role object.
      in: path
      name: roleId
      required: true
      x-references:
        $ref: '#/components/schemas/Role_2'
      schema:
        type: integer
    - description: The ID of a OrchestrationRule object.
      in: path
      name: orchestrationRuleId
      required: true
      x-references:
        $ref: '#/components/schemas/OrchestrationRule_2'
      schema:
        type: string
        maxLength: 36
        minLength: 36
        pattern: '[A-Fa-f0-9]{8}-([A-Fa-f0-9]{4}-){3}[A-Fa-f0-9]{12}'
    delete:
      description: Deletes the specified Orchestration Rule from the Role.
      responses:
        204:
          description: Successful deletion.
        400:
          description: A client-side error was made.
          x-errorCodes:
          - description: The request was structurally incorrect, and was not understood by the API.
            name: InvalidStructure
          - description: The request was understood by the API, but included invalid data.
            name: InvalidValue
          - description: The request wasn't understood by the API.
            name: BadRequest
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse_2'
        401:
          description: The request was not authenticated.
          x-errorCodes:
          - description: The request was not properly authenticated.
            name: BadAuthentication
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse_2'
        403:
          description: Insufficient permissions.
          x-errorCodes:
          - description: This request should be made in a different Scope.
            name: ScopeViolation
          - description: You do not have the necessary permissions to perform this request.
            name: PermissionViolation
          - description: The maximum memory limit in GB has been reached.
            name: MemoryQuotaExceeded
          - description: The vCPUs limit has been reached.
            name: VcpusQuotaExceeded
          - description: The maximum number of the Servers that you can launch or resume has been reached.
            name: ServersQuotaExceeded
          - description: The maximum number of the Servers that you can launch or resume on current Farm has been reached.
            name: ServersPerFarmQuotaExceeded
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse_2'
        404:
          description: Resource not found.
          x-errorCodes:
          - description: This endpoint does not exist.
            name: EndpointNotFound
            noDoc: true
          - description: The object you are trying to access does not exist.
            name: ObjectNotFound
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse_2'
        409:
          description: Conflict with current state.
          x-errorCodes:
          - description: These changes aren't possible while this object is in use.
            name: ObjectInUse
          - description: These changes would violate a policy.
            name: PolicyViolation
          - description: These changes would violate a unicity constraint.
            name: UnicityViolation
          - description: Some of the objects being changed are locked and cannot be changed.
            name: Locked
          - description: This Cloud platform is not enabled.
            name: NotEnabledPlatform
          - description: The Operating System does not match.
            name: OperatingSystemMismatch
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse_2'
        422:
          description: This request was well-formed but was rejected due to semantic errors.
          x-errorCodes:
          - description: Some of the configuration settings cannot be applied because they conflict with the current state.
            name: ConfigurationMismatch
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse_2'
        500:
          description: A server-side error occurred.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse_2'
        501:
          description: This feature is not implemented in Scalr.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse_2'
        503:
          description: The service is currently unavailable.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse_2'
      tags:
      - Roles
      summary: Delete roles by role id orchestration rules by orchestration rule id
      x-summary-source: derived
      operationId: deleteRolesByRoleIdOrchestrationRulesByOrchestrationRuleId
      x-operation-id-source: derived
    get:
      description: Retrieve a given Orchestration Rule associated to a Role.
      responses:
        200:
          description: The JSON representation of a OrchestrationRule object.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/OrchestrationRuleDetailsResponse_2'
        400:
          description: A client-side error was made.
          x-errorCodes:
          - description: The request was structurally incorrect, and was not understood by the API.
            name: InvalidStructure
          - description: The request was understood by the API, but included invalid data.
            name: InvalidValue
          - description: The request wasn't understood by the API.
            name: BadRequest
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse_2'
        401:
          description: The request was not authenticated.
          x-errorCodes:
          - description: The request was not properly authenticated.
            name: BadAuthentication
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse_2'
        403:
          description: Insufficient permissions.
          x-errorCodes:
          - description: This request should be made in a different Scope.
            name: ScopeViolation
          - description: You do not have the necessary permissions to perform this request.
            name: PermissionViolation
          - description: The maximum memory limit in GB has been reached.
            name: MemoryQuotaExceeded
          - description: The vCPUs limit has been reached.
            name: VcpusQuotaExceeded
          - description: The maximum number of the Servers that you can launch or resume has been reached.
            name: ServersQuotaExceeded
          - description: The maximum number of the Servers that you can launch or resume on current Farm has been reached.
            name: ServersPerFarmQuotaExceeded
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse_2'
        404:
          description: Resource not found.
          x-errorCodes:
          - description: This endpoint does not exist.
            name: EndpointNotFound
            noDoc: true
          - description: The object you are trying to access does not exist.
            name: ObjectNotFound
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse_2'
        409:
          description: Conflict with current state.
          x-errorCodes:
          - description: These changes aren't possible while this object is in use.
            name: ObjectInUse
          - description: These changes would violate a policy.
            name: PolicyViolation
          - description: These changes would violate a unicity constraint.
            name: UnicityViolation
          - description: Some of the objects being changed are locked and cannot be changed.
            name: Locked
          - description: This Cloud platform is not enabled.
            name: NotEnabledPlatform
          - description: The Operating System does not match.
            name: OperatingSystemMismatch
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse_2'
        422:
          description: This request was well-formed but was rejected due to semantic errors.
          x-errorCodes:
          - description: Some of the configuration settings cannot be applied because they conflict with the current state.
            name: ConfigurationMismatch
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse_2'
        500:
          description: A server-side error occurred.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse_2'
        501:
          description: This feature is not implemented in Scalr.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse_2'
        503:
          description: The service is currently unavailable.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse_2'
      tags:
      - Roles
      summary: Get roles by role id orchestration rules by orchestration rule id
      x-summary-source: derived
      operationId: getRolesByRoleIdOrchestrationRulesByOrchestrationRuleId
      x-operation-id-source: derived
    patch:
      description: Modify the attributes of the specified Orchestration Rule.
      responses:
        200:
          description: The JSON representation of a OrchestrationRule object.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/OrchestrationRuleDetailsResponse_2'
        400:
          description: A client-side error was made.
          x-errorCodes:
          - description: The request was structurally incorrect, and was not understood by the API.
            name: InvalidStructure
          - description: The request was understood by the API, but included invalid data.
            name: InvalidValue
          - description: The request wasn't understood by the API.
            name: BadRequest
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse_2'
        401:
          description: The request was not authenticated.
          x-errorCodes:
          - description: The request was not properly authenticated.
            name: BadAuthentication
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse_2'
        403:
          description: Insufficient permissions.
          x-errorCodes:
          - description: This request should be made in a different Scope.
            name: ScopeViolation
          - description: You do not have the necessary permissions to perform this request.
            name: PermissionViolation
          - description: The maximum memory limit in GB has been reached.
            name: MemoryQuotaExceeded
          - description: The vCPUs limit has been reached.
            name: VcpusQuotaExceeded
          - description: The maximum number of the Servers that you can launch or resume has been reached.
            name: ServersQuotaExceeded
          - description: The maximum number of the Servers that you can launch or resume on current Farm has been reached.
            name: ServersPerFarmQuotaExceeded
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse_2'
        404:
          description: Resource not found.
          x-errorCodes:
          - description: This endpoint does not exist.
            name: EndpointNotFound
            noDoc: true
          - description: The object you are trying to access does not exist.
            name: ObjectNotFound
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse_2'
        409:
          description: Conflict with current state.
          x-errorCodes:
          - description: These changes aren't possible while this object is in use.
            name: ObjectInUse
          - description: These changes would violate a policy.
            name: PolicyViolation
          - description: These changes would violate a unicity constraint.
            name: UnicityViolation
          - description: Some of the objects being changed are locked and cannot be changed.
            name: Locked
          - description: This Cloud platform is not enabled.
            name: NotEnabledPlatform
          - description: The Operating System does not match.
            name: OperatingSystemMismatch
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse_2'
        422:
          description: This request was well-formed but was rejected due to semantic errors.
          x-errorCodes:
          - description: Some of the configuration settings cannot be applied because they conflict with the current state.
            name: ConfigurationMismatch
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse_2'
        500:
          description: A server-side error occurred.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse_2'
        501:
          description: This feature is not implemented in Scalr.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse_2'
        503:
          description: The service is currently unavailable.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse_2'
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/OrchestrationRule_2'
        description: The JSON representation of a OrchestrationRule object.
        required: true
      tags:
      - Roles
      summary: Update roles by role id orchestration rules by orchestration rule id
      x-summary-source: derived
      operationId: patchRolesByRoleIdOrchestrationRulesByOrchestrationRuleId
      x-operation-id-source: derived
    servers:
    - url: /api/v1beta0/global
  /{envId}/roles/:
    parameters:
    - description: The ID of the Environment scoping this request.
      in: path
      name: envId
      required: true
      schema:
        type: integer
    get:
      description: List all the Roles available in this Environment.
      responses:
        200:
          description: A list of Role objects.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/RoleListResponse_2'
        400:
          description: A client-side error was made.
          x-errorCodes:
          - description: The request was structurally incorrect, and was not understood by the API.
            name: InvalidStructure
          - description: The request was understood by the API, but included invalid data.
            name: InvalidValue
          - description: The request wasn't understood by the API.
            name: BadRequest
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse_3'
        401:
          description: The request was not authenticated.
          x-errorCodes:
          - description: The request was not properly authenticated.
            name: BadAuthentication
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse_3'
        403:
          description: Insufficient permissions.
          x-errorCodes:
          - description: This request should be made in a different Scope.
            name: ScopeViolation
          - description: You do not have the necessary permissions to perform this request.
            name: PermissionViolation
          - description: The maximum memory limit in GB has been reached.
            name: MemoryQuotaExceeded
          - description: The vCPUs limit has been reached.
            name: VcpusQuotaExceeded
          - description: The maximum number of the Servers that you can launch or resume has been reached.
            name: ServersQuotaExceeded
          - description: The maximum number of the Servers that you can launch or resume on current Farm has been reached.
            name: ServersPerFarmQuotaExceeded
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse_3'
        404:
          description: Resource not found.
          x-errorCodes:
          - description: This endpoint does not exist.
            name: EndpointNotFound
            noDoc: true
          - description: The object you are trying to access does not exist.
            name: ObjectNotFound
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse_3'
        409:
          description: Conflict with current state.
          x-errorCodes:
          - description: These changes aren't possible while this object is in use.
            name: ObjectInUse
          - description: These changes would violate a policy.
            name: PolicyViolation
          - description: These changes would violate a unicity constraint.
            name: UnicityViolation
          - description: Some of the objects being changed are locked and cannot be changed.
            name: Locked
          - description: This Cloud platform is not enabled.
            name: NotEnabledPlatform
          - description: The Operating System does not match.
            name: OperatingSystemMismatch
          - description: Some of the objects or actions being access are deprecated.
            name: Deprecated
          - description: The action can't be perfomed in the current object's state.
            name: UnacceptableState
          - description: The object that you are trying to access does not exist on the cloud.
            name: ObjectNotFoundOnCloud
          - description: Some of these settings cannot be applied, they either conflict with the current state or are invalid.
            name: UnacceptableObjectConfiguration
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse_3'
        422:
          description: This request was well-formed but was rejected due to semantic errors.
          x-errorCodes:
          - description: Some of the configuration settings cannot be applied because they conflict with the current state.
            name: ConfigurationMismatch
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse_3'
        500:
          description: A server-side error occurred.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse_3'
        501:
          description: This feature is not implemented in Scalr.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse_3'
        503:
          description: The service is currently unavailable.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse_3'
      tags:
      - Roles
      summary: Get by env id roles
      x-summary-source: derived
      operationId: getByEnvIdRoles
      x-operation-id-source: derived
    post:
      description: Create a new Role in this Environment.
      responses:
        201:
          description: The JSON representation of a Role object.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/RoleDetailsResponse_2'
        400:
          description: A client-side error was made.
          x-errorCodes:
          - description: The request was structurally incorrect, and was not understood by the API.
            name: InvalidStructure
          - description: The request was understood by the API, but included invalid data.
            name: InvalidValue
          - description: The request wasn't understood by the API.
            name: BadRequest
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse_3'
        401:
          description: The request was not authenticated.
          x-errorCodes:
          - description: The request was not properly authenticated.
            name: BadAuthentication
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse_3'
        403:
          description: Insufficient permissions.
          x-errorCodes:
          - description: This request should be made in a different Scope.
            name: ScopeViolation
          - description: You do not have the necessary permissions to perform this request.
            name: PermissionViolation
          - description: The maximum memory limit in GB has been reached.
            name: MemoryQuotaExceeded
          - description: The vCPUs limit has been reached.
            name: VcpusQuotaExceeded
          - description: The maximum number of the Servers that you can launch or resume has been reached.
            name: ServersQuotaExceeded
          - description: The maximum number of the Servers that you can launch or resume on current Farm has been reached.
            name: ServersPerFarmQuotaExceeded
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse_3'
        404:
          description: Resource not found.
          x-errorCodes:
          - description: This endpoint does not exist.
            name: EndpointNotFound
            noDoc: true
          - description: The object you are trying to access does not exist.
            name: ObjectNotFound
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse_3'
        409:
          description: Conflict with current state.
          x-errorCodes:
          - description: These changes aren't possible while this object is in use.
            name: ObjectInUse
          - description: These changes would violate a policy.
            name: PolicyViolation
          - description: These changes would violate a unicity constraint.
            name: UnicityViolation
          - description: Some of the objects being changed are locked and cannot be changed.
            name: Locked
          - description: This Cloud platform is not enabled.
            name: NotEnabledPlatform
          - description: The Operating System does not match.
            name: OperatingSystemMismatch
          - description: Some of the objects or actions being access are deprecated.
            name: Deprecated
          - description: The action can't be perfomed in the current object's state.
            name: UnacceptableState
          - description: The object that you are trying to access does not exist on the cloud.
            name: ObjectNotFoundOnCloud
          - description: Some of these settings cannot be applied, they either conflict with the current state or are invalid.
            name: UnacceptableObjectConfiguration
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse_3'
        422:
          description: This request was well-formed but was rejected due to semantic errors.
          x-errorCodes:
          - description: Some of the configuration settings cannot be applied because they conflict with the current state.
            name: ConfigurationMismatch
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse_3'
        500:
          description: A server-side error occurred.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse_3'
        501:
          description: This feature is not implemented in Scalr.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse_3'
        503:
          description: The service is currently unavailable.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse_3'
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/Role_3'
        description: The JSON representation of a Role object.
        required: true
      tags:
      - Roles
      summary: Create by env id roles
      x-summary-source: derived
      operationId: postByEnvIdRoles
      x-operation-id-source: derived
    servers:
    - url: /api/v1beta0/user
  /{envId}/roles/{roleId}/:
    parameters:
    - description: The ID of the Environment scoping this request.
      in: path
      name: envId
      required: true
      schema:
        type: integer
    - description: The ID of a Role object.
      in: path
      name: roleId
      required: true
      x-references:
        $ref: '#/components/schemas/Role_3'
      schema:
        type: integer
    delete:
      description: Delete a Role from this Environment.
      responses:
        204:
          description: Successful deletion.
        400:
          description: A client-side error was made.
          x-errorCodes:
          - description: The request was structurally incorrect, and was not understood by the API.
            name: InvalidStructure
          - description: The request was understood by the API, but included invalid data.
            name: InvalidValue
          - description: The request wasn't understood by the API.
            name: BadRequest
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse_3'
        401:
          description: The request was not authenticated.
          x-errorCodes:
          - description: The request was not properly authenticated.
            name: BadAuthentication
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse_3'
        403:
          description: Insufficient permissions.
          x-errorCodes:
          - description: This request should be made in a different Scope.
            name: ScopeViolation
          - description: You do not have the necessary permissions to perform this request.
            name: PermissionViolation
          - description: The maximum memory limit in GB has been reached.
            name: MemoryQuotaExceeded
          - description: The vCPUs limit has been reached.
            name: VcpusQuotaExceeded
          - description: The maximum number of the Servers that you can launch or resume has been reached.
            name: ServersQuotaExceeded
          - description: The maximum number of the Servers that you can launch or resume on current Farm has been reached.
            name: ServersPerFarmQuotaExceeded
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse_3'
        404:
          description: Resource not found.
          x-errorCodes:
          - description: This endpoint does not exist.
            name: EndpointNotFound
            noDoc: true
          - description: The object you are trying to access does not exist.
            name: ObjectNotFound
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse_3'
        409:
          description: Conflict with current state.
          x-errorCodes:
          - description: These changes aren't possible while this object is in use.
            name: ObjectInUse
          - description: These changes would violate a policy.
            name: PolicyViolation
          - description: These changes would violate a unicity constraint.
            name: UnicityViolation
          - description: Some of the objects being changed are locked and cannot be changed.
            name: Locked
          - description: This Cloud platform is not enabled.
            name: NotEnabledPlatform
          - description: The Operating System does not match.
            name: OperatingSystemMismatch
          - description: Some of the objects or actions being access are deprecated.
            name: Deprecated
          - description: The action can't be perfomed in the current object's state.
            name: UnacceptableState
          - description: The object that you are trying to access does not exist on the cloud.
            name: ObjectNotFoundOnCloud
          - description: Some of these settings cannot be applied, they either conflict with the current state or are invalid.
            name: UnacceptableObjectConfiguration
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse_3'
        422:
          description: This request was well-formed but was rejected due to semantic errors.
          x-errorCodes:
          - description: Some of the configuration settings cannot be applied because they conflict with the current state.
            name: ConfigurationMismatch
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse_3'
        500:
          description: A server-side error occurred.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse_3'
        501:
          description: This feature is not implemented in Scalr.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse_3'
        503:
          description: The service is currently unavailable.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse_3'
      tags:
      - Roles
      summary: Delete by env id roles by role id
      x-summary-source: derived
      operationId: deleteByEnvIdRolesByRoleId
      x-operation-id-source: derived
    get:
      description: Retrieve detailed information about a given Role.
      responses:
        200:
          description: The JSON representation of a Role object.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/RoleDetailsResponse_2'
        400:
          description: A client-side error was made.
          x-errorCodes:
          - description: The request was structurally incorrect, and was not understood by the API.
            name: InvalidStructure
          - description: The request was understood by the API, but included invalid data.
            name: InvalidValue
          - description: The request wasn't understood by the API.
            name: BadRequest
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse_3'
        401:
          description: The request was not authenticated.
          x-errorCodes:
          - description: The request was not properly authenticated.
            name: BadAuthentication
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse_3'
        403:
          description: Insufficient permissions.
          x-errorCodes:
          - description: This request should be made in a different Scope.
            name: ScopeViolation
          - description: You do not have the necessary permissions to perform this request.
            name: PermissionViolation
          - description: The maximum memory limit in GB has been reached.
            name: MemoryQuotaExceeded
          - description: The vCPUs limit has been reached.
            name: VcpusQuotaExceeded
          - description: The maximum number of the Servers that you can launch or resume has been reached.
            name: ServersQuotaExceeded
          - description: The maximum number of the Servers that you can launch or resume on current Farm has been reached.
            name: ServersPerFarmQuotaExceeded
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse_3'
        404:
          description: Resource not found.
          x-errorCodes:
          - description: This endpoint does not exist.
            name: EndpointNotFound
            noDoc: true
          - description: The object you are trying to access does not exist.
            name: ObjectNotFound
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse_3'
        409:
          description: Conflict with current state.
          x-errorCodes:
          - description: These changes aren't possible while this object is in use.
            name: ObjectInUse
          - description: These changes would violate a policy.
            name: PolicyViolation
          - description: These changes would violate a unicity constraint.
            name: UnicityViolation
          - description: Some of the objects being changed are locked and cannot be changed.
            name: Locked
          - description: This Cloud platform is not enabled.
            name: NotEnabledPlatform
          - description: The Operating System does not match.
            name: OperatingSystemMismatch
          - description: Some of the objects or actions being access are deprecated.
            name: Deprecated
          - description: The action can't be perfomed in the current object's state.
            name: UnacceptableState
          - description: The object that you are trying to access does not exist on the cloud.
            name: ObjectNotFoundOnCloud
          - description: Some of these settings cannot be applied, they either conflict with the current state or are invalid.
            name: UnacceptableObjectConfiguration
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse_3'
        422:
          description: This request was well-formed but was rejected due to semantic errors.
          x-errorCodes:
          - description: Some of the configuration settings cannot be applied because they conflict with the current state.
            name: ConfigurationMismatch
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse_3'
        500:
          description: A server-side error occurred.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse_3'
        501:
          description: This feature is not implemented in Scalr.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse_3'
        503:
          description: The service is currently unavailable.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse_3'
      tags:
      - Roles
      summary: Get by env id roles by role id
      x-summary-source: derived
      operationId: getByEnvIdRolesByRoleId
      x-operation-id-source: derived
    patch:
      description: Modify the attributes of a given Role.
      responses:
        200:
          description: The JSON representation of a Role object.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/RoleDetailsResponse_2'
        400:
          description: A client-side error was made.
          x-errorCodes:
          - description: The request was structurally incorrect, and was not understood by the API.
            name: InvalidStructure
          - description: The request was understood by the API, but included invalid data.
            name: InvalidValue
          - description: The request wasn't understood by the API.
            name: BadRequest
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse_3'
        401:
          description: The request was not authenticated.
          x-errorCodes:
          - description: The request was not properly authenticated.
            name: BadAuthentication
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse_3'
        403:
          description: Insufficient permissions.
          x-errorCodes:
          - description: This request should be made in a different Scope.
            name: ScopeViolation
          - description: You do not have the necessary permissions to perform this request.
            name: PermissionViolation
          - description: The maximum memory limit in GB has been reached.
            name: MemoryQuotaExceeded
          - description: The vCPUs limit has been reached.
            name: VcpusQuotaExceeded
          - description: The maximum number of the Servers that you can launch or resume has been reached.
            name: ServersQuotaExceeded
          - description: The maximum number of the Servers that you can launch or resume on current Farm has been reached.
            name: ServersPerFarmQuotaExceeded
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse_3'
        404:
          description: Resource not found.
          x-errorCodes:
          - description: This endpoint does not exist.
            name: EndpointNotFound
            noDoc: true
          - description: The object you are trying to access does not exist.
            name: ObjectNotFound
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse_3'
        409:
          description: Conflict with current state.
          x-errorCodes:
          - description: These changes aren't possible while this object is in use.
            name: ObjectInUse
          - description: These changes would violate a policy.
            name: PolicyViolation
          - description: These changes would violate a unicity constraint.
            name: UnicityViolation
          - description: Some of the objects being changed are locked and cannot be changed.
            name: Locked
          - description: This Cloud platform is not enabled.
            name: NotEnabledPlatform
          - description: The Operating System does not match.
            name: OperatingSystemMismatch
          - description: Some of the objects or actions being access are deprecated.
            name: Deprecated
          - description: The action can't be perfomed in the current object's state.
            name: UnacceptableState
          - description: The object that you are trying to access does not exist on the cloud.
            name: ObjectNotFoundOnCloud
          - description: Some of these settings cannot be applied, they either conflict with the current state or are invalid.
            name: UnacceptableObjectConfiguration
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse_3'
        422:
          description: This request was well-formed but was rejected due to semantic errors.
          x-errorCodes:
          - description: Some of the configuration settings cannot be applied because they conflict with the current state.
            name: ConfigurationMismatch
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse_3'
        500:
          description: A server-side error occurred.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse_3'
        501:
          description: This feature is not implemented in Scalr.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse_3'
        503:
          description: The service is currently unavailable.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse_3'
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/Role_3'
        description: The JSON representation of a Role object.
        required: true
      tags:
      - Roles
      summary: Update by env id roles by role id
      x-summary-source: derived
      operationId: patchByEnvIdRolesByRoleId
      x-operation-id-source: derived
    servers:
    - url: /api/v1beta0/user
  /{envId}/roles/{roleId}/actions/clone/:
    parameters:
    - description: The ID of the Environment scoping this request.
      in: path
      name: envId
      required: true
      schema:
        type: integer
    - description: The ID of a Role object.
      in: path
      name: roleId
      required: true
      x-references:
        $ref: '#/components/schemas/Role_3'
      schema:
        type: integer
    post:
      description: Make a copy of an existing Role by cloning it.
      responses:
        201:
          description: The JSON representation of a Role object.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/RoleDetailsResponse_2'
        400:
          description: A client-side error was made.
          x-errorCodes:
          - description: The request was structurally incorrect, and was not understood by the API.
            name: InvalidStructure
          - description: The request was understood by the API, but included invalid data.
            name: InvalidValue
          - description: The request wasn't understood by the API.
            name: BadRequest
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse_3'
        401:
          description: The request was not authenticated.
          x-errorCodes:
          - description: The request was not properly authenticated.
            name: BadAuthentication
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse_3'
        403:
          description: Insufficient permissions.
          x-errorCodes:
          - description: This request should be made in a different Scope.
            name: ScopeViolation
          - description: You do not have the necessary permissions to perform this request.
            name: PermissionViolation
          - description: The maximum memory limit in GB has been reached.
            name: MemoryQuotaExceeded
          - description: The vCPUs limit has been reached.
            name: VcpusQuotaExceeded
          - description: The maximum number of the Servers that you can launch or resume has been reached.
            name: ServersQuotaExceeded
          - description: The maximum number of the Servers that you can launch or resume on current Farm has been reached.
            name: ServersPerFarmQuotaExceeded
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse_3'
        404:
          description: Resource not found.
          x-errorCodes:
          - description: This endpoint does not exist.
            name: EndpointNotFound
            noDoc: true
          - description: The object you are trying to access does not exist.
            name: ObjectNotFound
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse_3'
        409:
          description: Conflict with current state.
          x-errorCodes:
          - description: These changes aren't possible while this object is in use.
            name: ObjectInUse
          - description: These changes would violate a policy.
            name: PolicyViolation
          - description: These changes would violate a unicity constraint.
            name: UnicityViolation
          - description: Some of the objects being changed are locked and cannot be changed.
            name: Locked
          - description: This Cloud platform is not enabled.
            name: NotEnabledPlatform
          - description: The Operating System does not match.
            name: OperatingSystemMismatch
          - description: Some of the objects or actions being access are deprecated.
            name: Deprecated
          - description: The action can't be perfomed in the current object's state.
            name: UnacceptableState
          - description: The object that you are trying to access does not exist on the cloud.
            name: ObjectNotFoundOnCloud
          - description: Some of these settings cannot be applied, they either conflict with the current state or are invalid.
            name: UnacceptableObjectConfiguration
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse_3'
        422:
          description: This request was well-formed but was rejected due to semantic errors.
          x-errorCodes:
          - description: Some of the configuration settings cannot be applied because they conflict with the current state.
            name: ConfigurationMismatch
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse_3'
        500:
          description: A server-side error occurred.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse_3'
        501:
          description: This feature is not implemented in Scalr.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse_3'
        503:
          description: The service is currently unavailable.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse_3'
      requestBody:
        content:
          application/json:
            schema:
              properties:
                name:
                  description: The name of the new Role.
                  type: string
              required:
              - name
        description: Role cloning options.
        required: true
      tags:
      - Roles
      summary: Create by env id roles by role id actions clone
      x-summary-source: derived
      operationId: postByEnvIdRolesByRoleIdActionsClone
      x-operation-id-source: derived
    servers:
    - url: /api/v1beta0/user
  /{envId}/roles/{roleId}/actions/deprecate/:
    parameters:
    - description: The ID of the Environment scoping this request.
      in: path
      name: envId
      required: true
      schema:
        type: integer
    - description: The ID of a Role object.
      in: path
      name: roleId
      required: true
      x-references:
        $ref: '#/components/schemas/Role_3'
      schema:
        type: integer
    post:
      description: Deprecate a Role and set another Role to replace it.
      responses:
        200:
          description: The JSON representation of a Role object.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/RoleDetailsResponse_2'
        400:
          description: A client-side error was made.
          x-errorCodes:
          - description: The request was structurally incorrect, and was not understood by the API.
            name: InvalidStructure
          - description: The request was understood by the API, but included invalid data.
            name: InvalidValue
          - description: The request wasn't understood by the API.
            name: BadRequest
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse_3'
        401:
          description: The request was not authenticated.
          x-errorCodes:
          - description: The request was not properly authenticated.
            name: BadAuthentication
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse_3'
        403:
          description: Insufficient permissions.
          x-errorCodes:
          - description: This request should be made in a different Scope.
            name: ScopeViolation
          - description: You do not have the necessary permissions to perform this request.
            name: PermissionViolation
          - description: The maximum memory limit in GB has been reached.
            name: MemoryQuotaExceeded
          - description: The vCPUs limit has been reached.
            name: VcpusQuotaExceeded
          - description: The maximum number of the Servers that you can launch or resume has been reached.
            name: ServersQuotaExceeded
          - description: The maximum number of the Servers that you can launch or resume on current Farm has been reached.
            name: ServersPerFarmQuotaExceeded
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse_3'
        404:
          description: Resource not found.
          x-errorCodes:
          - description: This endpoint does not exist.
            name: EndpointNotFound
            noDoc: true
          - description: The object you are trying to access does not exist.
            name: ObjectNotFound
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse_3'
        409:
          description: Conflict with current state.
          x-errorCodes:
          - description: These changes aren't possible while this object is in use.
            name: ObjectInUse
          - description: These changes would violate a policy.
            name: PolicyViolation
          - description: These changes would violate a unicity constraint.
            name: UnicityViolation
          - description: Some of the objects being changed are locked and cannot be changed.
            name: Locked
          - description: This Cloud platform is not enabled.
            name: NotEnabledPlatform
          - description: The Operating System does not match.
            name: OperatingSystemMismatch
          - description: Some of the objects or actions being access are deprecated.
            name: Deprecated
          - description: The action can't be perfomed in the current object's state.
            name: UnacceptableState
          - description: The object that you are trying to access does not exist on the cloud.
            name: ObjectNotFoundOnCloud
          - description: Some of these settings cannot be applied, they either conflict with the current state or are invalid.
            name: UnacceptableObjectConfiguration
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse_3'
        422:
          description: This request was well-formed but was rejected due to semantic errors.
          x-errorCodes:
          - description: Some of the configuration settings cannot be applied because they conflict with the current state.
            name: ConfigurationMismatch
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse_3'
        500:
          description: A server-side error occurred.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse_3'
        501:
          description: This feature is not implemented in Scalr.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse_3'
        503:
          description: The service is currently unavailable.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse_3'
      requestBody:
        content:
          application/json:
            schema:
              properties:
                deprecate:
                  default: true
                  description: By default deprecate Role If a value is true clears the deprecate from Role.
                  type: boolean
                replacement:
                  $ref: '#/components/schemas/RoleForeignKey'
                  description: The suggested replacement Role foreign key for the current Role.
        description: Deprecate Role options.
      tags:
      - Roles
      summary: Create by env id roles by role id actions deprecate
      x-summary-source: derived
      operationId: postByEnvIdRolesByRoleIdActionsDeprecate
      x-operation-id-source: derived
    servers:
    - url: /api/v1beta0/user
  /{envId}/roles/{roleId}/actions/promote/:
    parameters:
    - description: The ID of the Environment scoping this request.
      in: path
      name: envId
      required: true
      schema:
        type: integer
    - description: The ID of a Role object.
      in: path
      name: roleId
      required: true
      x-references:
        $ref: '#/components/schemas/Role_3'
      schema:
        type: integer
    post:
      description: Promote Role to account scope.
      responses:
        200:
          description: The JSON representation of a Role object.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/RoleDetailsResponse_2'
        400:
          description: A client-side error was made.
          x-errorCodes:
          - description: The request was structurally incorrect, and was not understood by the API.
            name: InvalidStructure
          - description: The request was understood by the API, but included invalid data.
            name: InvalidValue
          - description: The request wasn't understood by the API.
            name: BadRequest
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse_3'
        401:
          description: The request was not authenticated.
          x-errorCodes:
          - description: The request was not properly authenticated.
            name: BadAuthentication
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse_3'
        403:
          description: Insufficient permissions.
          x-errorCodes:
          - description: This request should be made in a different Scope.
            name: ScopeViolation
          - description: You do not have the necessary permissions to perform this request.
            name: PermissionViolation
          - description: The maximum memory limit in GB has been reached.
            name: MemoryQuotaExceeded
          - description: The vCPUs limit has been reached.
            name: VcpusQuotaExceeded
          - description: The maximum number of the Servers that you can launch or resume has been reached.
            name: ServersQuotaExceeded
          - description: The maximum number of the Servers that you can launch or resume on current Farm has been reached.
            name: ServersPerFarmQuotaExceeded
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse_3'
        404:
          description: Resource not found.
          x-errorCodes:
          - description: This endpoint does not exist.
            name: EndpointNotFound
            noDoc: true
          - description: The object you are trying to access does not exist.
            name: ObjectNotFound
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse_3'
        409:
          description: Conflict with current state.
          x-errorCodes:
          - description: These changes aren't possible while this object is in use.
            name: ObjectInUse
          - description: These changes would violate a policy.
            name: PolicyViolation
          - description: These changes would violate a unicity constraint.
            name: UnicityViolation
          - description: Some of the objects being changed are locked and cannot be changed.
            name: Locked
          - description: This Cloud platform is not enabled.
            name: NotEnabledPlatform
          - description: The Operating System does not match.
            name: OperatingSystemMismatch
          - description: Some of the objects or actions being access are deprecated.
            name: Deprecated
          - description: The action can't be perfomed in the current object's state.
            name: UnacceptableState
          - description: The object that you are trying to access does not exist on the cloud.
            name: ObjectNotFoundOnCloud
          - description: Some of these settings cannot be applied, they either conflict with the current state or are invalid.
            name: UnacceptableObjectConfiguration
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse_3'
        422:
          description: This request was well-formed but was rejected due to semantic errors.
          x-errorCodes:
          - description: Some of the configuration settings cannot be applied because they conflict with the current state.
            name: ConfigurationMismatch
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse_3'
        500:
          description: A server-side error occurred.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse_3'
        501:
          description: This feature is not implemented in Scalr.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse_3'
        503:
          description: The service is currently unavailable.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse_3'
      tags:
      - Roles
      summary: Create by env id roles by role id actions promote
      x-summary-source: derived
      operationId: postByEnvIdRolesByRoleIdActionsPromote
      x-operation-id-source: derived
    servers:
    - url: /api/v1beta0/user
  /{envId}/roles/{roleId}/global-variables/:
    parameters:
    - description: The ID of the Environment scoping this request.
      in: path
      name: envId
      required: true
      schema:
        type: integer
    - description: The ID of a Role object.
      in: path
      name: roleId
      required: true
      x-references:
        $ref: '#/components/schemas/Role_3'
      schema:
        type: integer
    get:
      description: List all the Global Variables associated with a given Role.
      responses:
        200:
          description: A list of GlobalVariable objects.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/GlobalVariableListResponse_3'
        400:
          description: A client-side error was made.
          x-errorCodes:
          - description: The request was structurally incorrect, and was not understood by the API.
            name: InvalidStructure
          - description: The request was understood by the API, but included invalid data.
            name: InvalidValue
          - description: The request wasn't understood by the API.
            name: BadRequest
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse_3'
        401:
          description: The request was not authenticated.
          x-errorCodes:
          - description: The request was not properly authenticated.
            name: BadAuthentication
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse_3'
        403:
          description: Insufficient permissions.
          x-errorCodes:
          - description: This request should be made in a different Scope.
            name: ScopeViolation
          - description: You do not have the necessary permissions to perform this request.
            name: PermissionViolation
          - description: The maximum memory limit in GB has been reached.
            name: MemoryQuotaExceeded
          - description: The vCPUs limit has been reached.
            name: VcpusQuotaExceeded
          - description: The maximum number of the Servers that you can launch or resume has been reached.
            name: ServersQuotaExceeded
          - description: The maximum number of the Servers that you can launch or resume on current Farm has been reached.
            name: ServersPerFarmQuotaExceeded
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse_3'
        404:
          description: Resource not found.
          x-errorCodes:
          - description: This endpoint does not exist.
            name: EndpointNotFound
            noDoc: true
          - description: The object you are trying to access does not exist.
            name: ObjectNotFound
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse_3'
        409:
          description: Conflict with current state.
          x-errorCodes:
          - description: These changes aren't possible while this object is in use.
            name: ObjectInUse
          - description: These changes would violate a policy.
            name: PolicyViolation
          - description: These changes would violate a unicity constraint.
            name: UnicityViolation
          - description: Some of the objects being changed are locked and cannot be changed.
            name: Locked
          - description: This Cloud platform is not enabled.
            name: NotEnabledPlatform
          - description: The Operating System does not match.
            name: OperatingSystemMismatch
          - description: Some of the objects or actions being access are deprecated.
            name: Deprecated
          - description: The action can't be perfomed in the current object's state.
            name: UnacceptableState
          - description: The object that you are trying to access does not exist on the cloud.
            name: ObjectNotFoundOnCloud
          - description: Some of these settings cannot be applied, they either conflict with the current state or are invalid.
            name: UnacceptableObjectConfiguration
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse_3'
        422:
          description: This request was well-formed but was rejected due to semantic errors.
          x-errorCodes:
          - description: Some of the configuration settings cannot be applied because they conflict with the current state.
            name: ConfigurationMismatch
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse_3'
        500:
          description: A server-side error occurred.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse_3'
        501:
          description: This feature is not implemented in Scalr.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse_3'
        503:
          description: The service is currently unavailable.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse_3'
      tags:
      - Roles
      summary: Get by env id roles by role id global variables
      x-summary-source: derived
      operationId: getByEnvIdRolesByRoleIdGlobalVariables
      x-operation-id-source: derived
    post:
      description: Declares a new Global Variable for this Role.
      responses:
        201:
          description: The JSON representation of a GlobalVariable object.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/GlobalVariableDetailsResponse_3'
        400:
          description: A client-side error was made.
          x-errorCodes:
          - description: The request was structurally incorrect, and was not understood by the API.
            name: InvalidStructure
          - description: The request was understood by the API, but included invalid data.
            name: InvalidValue
          - description: The request wasn't understood by the API.
            name: BadRequest
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse_3'
        401:
          description: The request was not authenticated.
          x-errorCodes:
          - description: The request was not properly authenticated.
            name: BadAuthentication
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse_3'
        403:
          description: Insufficient permissions.
          x-errorCodes:
          - description: This request should be made in a different Scope.
            name: ScopeViolation
          - description: You do not have the necessary permissions to perform this request.
            name: PermissionViolation
          - description: The maximum memory limit in GB has been reached.
            name: MemoryQuotaExceeded
          - description: The vCPUs limit has been reached.
            name: VcpusQuotaExceeded
          - description: The maximum number of the Servers that you can launch or resume has been reached.
            name: ServersQuotaExceeded
          - description: The maximum number of the Servers that you can launch or resume on current Farm has been reached.
            name: ServersPerFarmQuotaExceeded
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse_3'
        404:
          description: Resource not found.
          x-errorCodes:
          - description: This endpoint does not exist.
            name: EndpointNotFound
            noDoc: true
          - description: The object you are trying to access does not exist.
            name: ObjectNotFound
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse_3'
        409:
          description: Conflict with current state.
          x-errorCodes:
          - description: These changes aren't possible while this object is in use.
            name: ObjectInUse
          - description: These changes would violate a policy.
            name: PolicyViolation
          - description: These changes would violate a unicity constraint.
            name: UnicityViolation
          - description: Some of the objects being changed are locked and cannot be changed.
            name: Locked
          - description: This Cloud platform is not enabled.
            name: NotEnabledPlatform
          - description: The Operating System does not match.
            name: OperatingSystemMismatch
          - description: Some of the objects or actions being access are deprecated.
            name: Deprecated
          - description: The action can't be perfomed in the current object's state.
            name: UnacceptableState
          - description: The object that you are trying to access does not exist on the cloud.
            name: ObjectNotFoundOnCloud
          - description: Some of these settings cannot be applied, they either conflict with the current state or are invalid.
            name: UnacceptableObjectConfiguration
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse_3'
        422:
          description: This request was well-formed but was rejected due to semantic errors.
          x-errorCodes:
          - description: Some of the configuration settings cannot be applied because they conflict with the current state.
            name: ConfigurationMismatch
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse_3'
        500:
          description: A server-side error occurred.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse_3'
        501:
          description: This feature is not implemented in Scalr.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse_3'
        503:
          description: The service is currently unavailable.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse_3'
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/GlobalVariable_3'
        description: The JSON representation of a GlobalVariable object.
        required: true
      tags:
      - Roles
      summary: Create by env id roles by role id global variables
      x-summary-source: derived
      operationId: postByEnvIdRolesByRoleIdGlobalVariables
      x-operation-id-source: derived
    servers:
    - url: /api/v1beta0/user
  /{envId}/roles/{roleId}/global-variables/{globalVariableName}/:
    parameters:
    - description: The ID of the Environment scoping this request.
      in: path
      name: envId
      required: true
      schema:
        type: integer
    - description: The ID of a Role object.
      in: path
      name: roleId
      required: true
      x-references:
        $ref: '#/components/schemas/Role_3'
      schema:
        type: integer
    - description: The name of the Global Variable being accessed.
      in: path
      name: globalVariableName
      required: true
      schema:
        type: string
        maxLength: 128
        minLength: 2
        pattern: \w+
    delete:
      description: Deletes a Global Variable from this Role's scope.
      responses:
        204:
          description: Successful deletion.
        400:
          description: A client-side error was made.
          x-errorCodes:
          - description: The request was structurally incorrect, and was not understood by the API.
            name: InvalidStructure
          - description: The request was understood by the API, but included invalid data.
            name: InvalidValue
          - description: The request wasn't understood by the API.
            name: BadRequest
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse_3'
        401:
          description: The request was not authenticated.
          x-errorCodes:
          - description: The request was not properly authenticated.
            name: BadAuthentication
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse_3'
        403:
          description: Insufficient permissions.
          x-errorCodes:
          - description: This request should be made in a different Scope.
            name: ScopeViolation
          - description: You do not have the necessary permissions to perform this request.
            name: PermissionViolation
          - description: The maximum memory limit in GB has been reached.
            name: MemoryQuotaExceeded
          - description: The vCPUs limit has been reached.
            name: VcpusQuotaExceeded
          - description: The maximum number of the Servers that you can launch or resume has been reached.
            name: ServersQuotaExceeded
          - description: The maximum number of the Servers that you can launch or resume on current Farm has been reached.
            name: ServersPerFarmQuotaExceeded
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse_3'
        404:
          description: Resource not found.
          x-errorCodes:
          - description: This endpoint does not exist.
            name: EndpointNotFound
            noDoc: true
          - description: The object you are trying to access does not exist.
            name: ObjectNotFound
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse_3'
        409:
          description: Conflict with current state.
          x-errorCodes:
          - description: These changes aren't possible while this object is in use.
            name: ObjectInUse
          - description: These changes would violate a policy.
            name: PolicyViolation
          - description: These changes would violate a unicity constraint.
            name: UnicityViolation
          - description: Some of the objects being changed are locked and cannot be changed.
            name: Locked
          - description: This Cloud platform is not enabled.
            name: NotEnabledPlatform
          - description: The Operating System does not match.
            name: OperatingSystemMismatch
          - description: Some of the objects or actions being access are deprecated.
            name: Deprecated
          - description: The action can't be perfomed in the current object's state.
            name: UnacceptableState
          - description: The object that you are trying to access does not exist on the cloud.
            name: ObjectNotFoundOnCloud
          - description: Some of these settings cannot be applied, they either conflict with the current state or are invalid.
            name: UnacceptableObjectConfiguration
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse_3'
        422:
          description: This request was well-formed but was rejected due to semantic errors.
          x-errorCodes:
          - description: Some of the configuration settings cannot be applied because they conflict with the current state.
            name: ConfigurationMismatch
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse_3'
        500:
          description: A server-side error occurred.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse_3'
        501:
          description: This feature is not implemented in Scalr.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse_3'
        503:
          description: The service is currently unavailable.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse_3'
      tags:
      - Roles
      summary: Delete by env id roles by role id global variables by global variable name
      x-summary-source: derived
      operationId: deleteByEnvIdRolesByRoleIdGlobalVariablesByGlobalVariableName
      x-operation-id-source: derived
    get:
      description: Retrieve detailed information about Global Variable in the Role's scope.
      responses:
        200:
          description: The JSON representation of a GlobalVariable object.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/GlobalVariableDetailsResponse_3'
        400:
          description: A client-side error was made.
          x-errorCodes:
          - description: The request was structurally incorrect, and was not understood by the API.
            name: InvalidStructure
          - description: The request was understood by the API, but included invalid data.
            name: InvalidValue
          - description: The request wasn't understood by the API.
            name: BadRequest
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse_3'
        401:
          description: The request was not authenticated.
          x-errorCodes:
          - description: The request was not properly authenticated.
            name: BadAuthentication
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse_3'
        403:
          description: Insufficient permissions.
          x-errorCodes:
          - description: This request should be made in a different Scope.
            name: ScopeViolation
          - description: You do not have the necessary permissions to perform this request.
            name: PermissionViolation
          - description: The maximum memory limit in GB has been reached.
            name: MemoryQuotaExceeded
          - description: The vCPUs limit has been reached.
            name: VcpusQuotaExceeded
          - description: The maximum number of the Servers that you can launch or resume has been reached.
            name: ServersQuotaExceeded
          - description: The maximum number of the Servers that you can launch or resume on current Farm has been reached.
            name: ServersPerFarmQuotaExceeded
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse_3'
        404:
          description: Resource not found.
          x-errorCodes:
          - description: This endpoint does not exist.
            name: EndpointNotFound
            noDoc: true
          - description: The object you are trying to access does not exist.
            name: ObjectNotFound
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse_3'
        409:
          description: Conflict with current state.
          x-errorCodes:
          - description: These changes aren't possible while this object is in use.
            name: ObjectInUse
          - description: These changes would violate a policy.
            name: PolicyViolation
          - description: These changes would violate a unicity constraint.
            name: UnicityViolation
          - description: Some of the objects being changed are locked and cannot be changed.
            name: Locked
          - description: This Cloud platform is not enabled.
            name: NotEnabledPlatform
          - description: The Operating System does not match.
            name: OperatingSystemMismatch
          - description: Some of the objects or actions being access are deprecated.
            name: Deprecated
          - description: The action can't be perfomed in the current object's state.
            name: UnacceptableState
          - description: The object that you are trying to access does not exist on the cloud.
            name: ObjectNotFoundOnCloud
          - description: Some of these settings cannot be applied, they either conflict with the current state or are invalid.
            name: UnacceptableObjectConfiguration
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse_3'
        422:
          description: This request was well-formed but was rejected due to semantic errors.
          x-errorCodes:
          - description: Some of the configuration settings cannot be applied because they conflict with the current state.
            name: ConfigurationMismatch
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse_3'
        500:
          description: A server-side error occurred.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse_3'
        501:
          description: This feature is not implemented in Scalr.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse_3'
        503:
          description: The service is currently unavailable.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse_3'
      tags:
      - Roles
      summary: Get by env id roles by role id global variables by global variable name
      x-summary-source: derived
      operationId: getByEnvIdRolesByRoleIdGlobalVariablesByGlobalVariableName
      x-operation-id-source: derived
    patch:
      description: Modify the attributes of a Global Variable in the Role's scope.
      responses:
        200:
          description: The JSON representation of a GlobalVariable object.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/GlobalVariableDetailsResponse_3'
        400:
          description: A client-side error was made.
          x-errorCodes:
          - description: The request was structurally incorrect, and was not understood by the API.
            name: InvalidStructure
          - description: The request was understood by the API, but included invalid data.
            name: InvalidValue
          - description: The request wasn't understood by the API.
            name: BadRequest
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse_3'
        401:
          description: The request was not authenticated.
          x-errorCodes:
          - description: The request was not properly authenticated.
            name: BadAuthentication
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse_3'
        403:
          description: Insufficient permissions.
          x-errorCodes:
          - description: This request should be made in a different Scope.
            name: ScopeViolation
          - description: You do not have the necessary permissions to perform this request.
            name: PermissionViolation
          - description: The maximum memory limit in GB has been reached.
            name: MemoryQuotaExceeded
          - description: The vCPUs limit has been reached.
            name: VcpusQuotaExceeded
          - description: The maximum number of the Servers that you can launch or resume has been reached.
            name: ServersQuotaExceeded
          - description: The maximum number of the Servers that you can launch or resume on current Farm has been reached.
            name: ServersPerFarmQuotaExceeded
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse_3'
        404:
          description: Resource not found.
          x-errorCodes:
          - description: This endpoint does not exist.
            name: EndpointNotFound
            noDoc: true
          - description: The object you are trying to access does not exist.
            name: ObjectNotFound
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse_3'
        409:
          description: Conflict with current state.
          x-errorCodes:
          - description: These changes aren't possible while this object is in use.
            name: ObjectInUse
          - description: These changes would violate a policy.
            name: PolicyViolation
          - description: These changes would violate a unicity constraint.
            name: UnicityViolation
          - description: Some of the objects being changed are locked and cannot be changed.
            name: Locked
          - description: This Cloud platform is not enabled.
            name: NotEnabledPlatform
          - description: The Operating System does not match.
            name: OperatingSystemMismatch
          - description: Some of the objects or actions being access are deprecated.
            name: Deprecated
          - description: The action can't be perfomed in the current object's state.
            name: UnacceptableState
          - description: The object that you are trying to access does not exist on the cloud.
            name: ObjectNotFoundOnCloud
          - description: Some of these settings cannot be applied, they either conflict with the current state or are invalid.
            name: UnacceptableObjectConfiguration
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse_3'
        422:
          description: This request was well-formed but was rejected due to semantic errors.
          x-errorCodes:
          - description: Some of the configuration settings cannot be applied because they conflict with the current state.
            name: ConfigurationMismatch
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse_3'
        500:
          description: A server-side error occurred.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse_3'
        501:
          description: This feature is not implemented in Scalr.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse_3'
        503:
          description: The service is currently unavailable.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse_3'
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/GlobalVariable_3'
        description: The JSON representation of a GlobalVariable object.
        required: true
      tags:
      - Roles
      summary: Update by env id roles by role id global variables by global variable name
      x-summary-source: derived
      operationId: patchByEnvIdRolesByRoleIdGlobalVariablesByGlobalVariableName
      x-operation-id-source: derived
    servers:
    - url: /api/v1beta0/user
  /{envId}/roles/{roleId}/images/:
    parameters:
    - description: The ID of the Environment scoping this request.
      in: path
      name: envId
      required: true
      schema:
        type: integer
    - description: The ID of a Role object.
      in: path
      name: roleId
      required: true
      x-references:
        $ref: '#/components/schemas/Role_3'
      schema:
        type: integer
    get:
      description: List all Images associated with a given Role.
      responses:
        200:
          description: A list of RoleImage objects.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/RoleImageListResponse_3'
        400:
          description: A client-side error was made.
          x-errorCodes:
          - description: The request was structurally incorrect, and was not understood by the API.
            name: InvalidStructure
          - description: The request was understood by the API, but included invalid data.
            name: InvalidValue
          - description: The request wasn't understood by the API.
            name: BadRequest
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse_3'
        401:
          description: The request was not authenticated.
          x-errorCodes:
          - description: The request was not properly authenticated.
            name: BadAuthentication
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse_3'
        403:
          description: Insufficient permissions.
          x-errorCodes:
          - description: This request should be made in a different Scope.
            name: ScopeViolation
          - description: You do not have the necessary permissions to perform this request.
            name: PermissionViolation
          - description: The maximum memory limit in GB has been reached.
            name: MemoryQuotaExceeded
          - description: The vCPUs limit has been reached.
            name: VcpusQuotaExceeded
          - description: The maximum number of the Servers that you can launch or resume has been reached.
            name: ServersQuotaExceeded
          - description: The maximum number of the Servers that you can launch or resume on current Farm has been reached.
            name: ServersPerFarmQuotaExceeded
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse_3'
        404:
          description: Resource not found.
          x-errorCodes:
          - description: This endpoint does not exist.
            name: EndpointNotFound
            noDoc: true
          - description: The object you are trying to access does not exist.
            name: ObjectNotFound
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse_3'
        409:
          description: Conflict with current state.
          x-errorCodes:
          - description: These changes aren't possible while this object is in use.
            name: ObjectInUse
          - description: These changes would violate a policy.
            name: PolicyViolation
          - description: These changes would violate a unicity constraint.
            name: UnicityViolation
          - description: Some of the objects being changed are locked and cannot be changed.
            name: Locked
          - description: This Cloud platform is not enabled.
            name: NotEnabledPlatform
          - description: The Operating System does not match.
            name: OperatingSystemMismatch
          - description: Some of the objects or actions being access are deprecated.
            name: Deprecated
          - description: The action can't be perfomed in the current object's state.
            name: UnacceptableState
          - description: The object that you are trying to access does not exist on the cloud.
            name: ObjectNotFoundOnCloud
          - description: Some of these settings cannot be applied, they either conflict with the current state or are invalid.
            name: UnacceptableObjectConfiguration
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse_3'
        422:
          description: This request was well-formed but was rejected due to semantic errors.
          x-errorCodes:
          - description: Some of the configuration settings cannot be applied because they conflict with the current state.
            name: ConfigurationMismatch
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse_3'
        500:
          description: A server-side error occurred.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse_3'
        501:
          description: This feature is not implemented in Scalr.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse_3'
        503:
          description: The service is currently unavailable.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse_3'
      tags:
      - Roles
      summary: Get by env id roles by role id images
      x-summary-source: derived
      operationId: getByEnvIdRolesByRoleIdImages
      x-operation-id-source: derived
    post:
      description: Associates a new Image with a given Role. This will fail if an Image associated to the Role is in the same location than the Image you are trying to add.
      responses:
        201:
          description: The JSON representation of a RoleImage object.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/RoleImageDetailsResponse_3'
        400:
          description: A client-side error was made.
          x-errorCodes:
          - description: The request was structurally incorrect, and was not understood by the API.
            name: InvalidStructure
          - description: The request was understood by the API, but included invalid data.
            name: InvalidValue
          - description: The request wasn't understood by the API.
            name: BadRequest
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse_3'
        401:
          description: The request was not authenticated.
          x-errorCodes:
          - description: The request was not properly authenticated.
            name: BadAuthentication
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse_3'
        403:
          description: Insufficient permissions.
          x-errorCodes:
          - description: This request should be made in a different Scope.
            name: ScopeViolation
          - description: You do not have the necessary permissions to perform this request.
            name: PermissionViolation
          - description: The maximum memory limit in GB has been reached.
            name: MemoryQuotaExceeded
          - description: The vCPUs limit has been reached.
            name: VcpusQuotaExceeded
          - description: The maximum number of the Servers that you can launch or resume has been reached.
            name: ServersQuotaExceeded
          - description: The maximum number of the Servers that you can launch or resume on current Farm has been reached.
            name: ServersPerFarmQuotaExceeded
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse_3'
        404:
          description: Resource not found.
          x-errorCodes:
          - description: This endpoint does not exist.
            name: EndpointNotFound
            noDoc: true
          - description: The object you are trying to access does not exist.
            name: ObjectNotFound
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse_3'
        409:
          description: Conflict with current state.
          x-errorCodes:
          - description: These changes aren't possible while this object is in use.
            name: ObjectInUse
          - description: These changes would violate a policy.
            name: PolicyViolation
          - description: These changes would violate a unicity constraint.
            name: UnicityViolation
          - description: Some of the objects being changed are locked and cannot be changed.
            name: Locked
          - description: This Cloud platform is not enabled.
            name: NotEnabledPlatform
          - description: The Operating System does not match.
            name: OperatingSystemMismatch
          - description: Some of the objects or actions being access are deprecated.
            name: Deprecated
          - description: The action can't be perfomed in the current object's state.
            name: UnacceptableState
          - description: The object that you are trying to access does not exist on the cloud.
            name: ObjectNotFoundOnCloud
          - description: Some of these settings cannot be applied, they either conflict with the current state or are invalid.
            name: UnacceptableObjectConfiguration
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse_3'
        422:
          description: This request was well-formed but was rejected due to semantic errors.
          x-errorCodes:
          - description: Some of the configuration settings cannot be applied because they conflict with the current state.
            name: ConfigurationMismatch
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse_3'
        500:
          description: A server-side error occurred.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse_3'
        501:
          description: This feature is not implemented in Scalr.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse_3'
        503:
          description: The service is currently unavailable.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse_3'
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/RoleImage_3'
        description: The JSON representation of a RoleImage object.
        required: true
      tags:
      - Roles
      summary: Create by env id roles by role id images
      x-summary-source: derived
      operationId: postByEnvIdRolesByRoleIdImages
      x-operation-id-source: derived
    servers:
    - url: /api/v1beta0/user
  /{envId}/roles/{roleId}/images/{imageId}/:
    parameters:
    - description: The ID of the Environment scoping this request.
      in: path
      name: envId
      required: true
      schema:
        type: integer
    - description: The ID of a Role object.
      in: path
      name: roleId
      required: true
      x-references:
        $ref: '#/components/schemas/Role_3'
      schema:
        type: integer
    - description: The ID of a Image object.
      in: path
      name: imageId
      required: true
      x-references:
        $ref: '#/components/schemas/Image_3'
      schema:
        type: string
        maxLength: 36
        minLength: 36
        pattern: '[A-Fa-f0-9]{8}-([A-Fa-f0-9]{4}-){3}[A-Fa-f0-9]{12}'
    delete:
      description: Dis-associates an Image from a given Role.
      responses:
        204:
          description: Successful deletion.
        400:
          description: A client-side error was made.
          x-errorCodes:
          - description: The request was structurally incorrect, and was not understood by the API.
            name: InvalidStructure
          - description: The request was understood by the API, but included invalid data.
            name: InvalidValue
          - description: The request wasn't understood by the API.
            name: BadRequest
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse_3'
        401:
          description: The request was not authenticated.
          x-errorCodes:
          - description: The request was not properly authenticated.
            name: BadAuthentication
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse_3'
        403:
          description: Insufficient permissions.
          x-errorCodes:
          - description: This request should be made in a different Scope.
            name: ScopeViolation
          - description: You do not have the necessary permissions to perform this request.
            name: PermissionViolation
          - description: The maximum memory limit in GB has been reached.
            name: MemoryQuotaExceeded
          - description: The vCPUs limit has been reached.
            name: VcpusQuotaExceeded
          - description: The maximum number of the Servers that you can launch or resume has been reached.
            name: ServersQuotaExceeded
          - description: The maximum number of the Servers that you can launch or resume on current Farm has been reached.
            name: ServersPerFarmQuotaExceeded
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse_3'
        404:
          description: Resource not found.
          x-errorCodes:
          - description: This endpoint does not exist.
            name: EndpointNotFound
            noDoc: true
          - description: The object you are trying to access does not exist.
            name: ObjectNotFound
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse_3'
        409:
          description: Conflict with current state.
          x-errorCodes:
          - description: These changes aren't possible while this object is in use.
            name: ObjectInUse
          - description: These changes would violate a policy.
            name: PolicyViolation
          - description: These changes would violate a unicity constraint.
            name: UnicityViolation
          - description: Some of the objects being changed are locked and cannot be changed.
            name: Locked
          - description: This Cloud platform is not enabled.
            name: NotEnabledPlatform
          - description: The Operating System does not match.
            name: OperatingSystemMismatch
          - description: Some of the objects or actions being access are deprecated.
            name: Deprecated
          - description: The action can't be perfomed in the current object's state.
            name: UnacceptableState
          - description: The object that you are trying to access does not exist on the cloud.
            name: ObjectNotFoundOnCloud
          - description: Some of these settings cannot be applied, they either conflict with the current state or are invalid.
            name: UnacceptableObjectConfiguration
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse_3'
        422:
          description: This request was well-formed but was rejected due to semantic errors.
          x-errorCodes:
          - description: Some of the configuration settings cannot be applied because they conflict with the current state.
            name: ConfigurationMismatch
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse_3'
        500:
          description: A server-side error occurred.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse_3'
        501:
          description: This feature is not implemented in Scalr.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse_3'
        503:
          description: The service is currently unavailable.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse_3'
      tags:
      - Roles
      summary: Delete by env id roles by role id images by image id
      x-summary-source: derived
      operationId: deleteByEnvIdRolesByRoleIdImagesByImageId
      x-operation-id-source: derived
    get:
      description: Retrieve detailed information about an Image.
      responses:
        200:
          description: Redirects to the Image.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ImageDetailsResponse_3'
        400:
          description: A client-side error was made.
          x-errorCodes:
          - description: The request was structurally incorrect, and was not understood by the API.
            name: InvalidStructure
          - description: The request was understood by the API, but included invalid data.
            name: InvalidValue
          - description: The request wasn't understood by the API.
            name: BadRequest
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse_3'
        401:
          description: The request was not authenticated.
          x-errorCodes:
          - description: The request was not properly authenticated.
            name: BadAuthentication
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse_3'
        403:
          description: Insufficient permissions.
          x-errorCodes:
          - description: This request should be made in a different Scope.
            name: ScopeViolation
          - description: You do not have the necessary permissions to perform this request.
            name: PermissionViolation
          - description: The maximum memory limit in GB has been reached.
            name: MemoryQuotaExceeded
          - description: The vCPUs limit has been reached.
            name: VcpusQuotaExceeded
          - description: The maximum number of the Servers that you can launch or resume has been reached.
            name: ServersQuotaExceeded
          - description: The maximum number of the Servers that you can launch or resume on current Farm has been reached.
            name: ServersPerFarmQuotaExceeded
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse_3'
        404:
          description: Resource not found.
          x-errorCodes:
          - description: This endpoint does not exist.
            name: EndpointNotFound
            noDoc: true
          - description: The object you are trying to access does not exist.
            name: ObjectNotFound
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse_3'
        409:
          description: Conflict with current state.
          x-errorCodes:
          - description: These changes aren't possible while this object is in use.
            name: ObjectInUse
          - description: These changes would violate a policy.
            name: PolicyViolation
          - description: These changes would violate a unicity constraint.
            name: UnicityViolation
          - description: Some of the objects being changed are locked and cannot be changed.
            name: Locked
          - description: This Cloud platform is not enabled.
            name: NotEnabledPlatform
          - description: The Operating System does not match.
            name: OperatingSystemMismatch
          - description: Some of the objects or actions being access are deprecated.
            name: Deprecated
          - description: The action can't be perfomed in the current object's state.
            name: UnacceptableState
          - description: The object that you are trying to access does not exist on the cloud.
            name: ObjectNotFoundOnCloud
          - description: Some of these settings cannot be applied, they either conflict with the current state or are invalid.
            name: UnacceptableObjectConfiguration
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse_3'
        422:
          description: This request was well-formed but was rejected due to semantic errors.
          x-errorCodes:
          - description: Some of the configuration settings cannot be applied because they conflict with the current state.
            name: ConfigurationMismatch
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse_3'
        500:
          description: A server-side error occurred.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse_3'
        501:
          description: This feature is not implemented in Scalr.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse_3'
        503:
          description: The service is currently unavailable.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse_3'
      tags:
      - Roles
      summary: Get by env id roles by role id images by image id
      x-summary-source: derived
      operationId: getByEnvIdRolesByRoleIdImagesByImageId
      x-operation-id-source: derived
    servers:
    - url: /api/v1beta0/user
  /{envId}/roles/{roleId}/images/{imageId}/actions/replace/:
    parameters:
    - description: The ID of the Environment scoping this request.
      in: path
      name: envId
      required: true
      schema:
        type: integer
    - description: The ID of a Role object.
      in: path
      name: roleId
      required: true
      x-references:
        $ref: '#/components/schemas/Role_3'
      schema:
        type: integer
    - description: The ID of a Image object.
      in: path
      name: imageId
      required: true
      x-references:
        $ref: '#/components/schemas/Image_3'
      schema:
        type: string
        maxLength: 36
        minLength: 36
        pattern: '[A-Fa-f0-9]{8}-([A-Fa-f0-9]{4}-){3}[A-Fa-f0-9]{12}'
    post:
      description: Replace an Image in-place in a given Role. This allows to replace an Image without having to dis-associate it and then associate a new one, which may not be possible if a Farm Role is currently using this Image.
      responses:
        200:
          description: The JSON representation of a RoleImage object.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/RoleImageDetailsResponse_3'
        400:
          description: A client-side error was made.
          x-errorCodes:
          - description: The request was structurally incorrect, and was not understood by the API.
            name: InvalidStructure
          - description: The request was understood by the API, but included invalid data.
            name: InvalidValue
          - description: The request wasn't understood by the API.
            name: BadRequest
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse_3'
        401:
          description: The request was not authenticated.
          x-errorCodes:
          - description: The request was not properly authenticated.
            name: BadAuthentication
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse_3'
        403:
          description: Insufficient permissions.
          x-errorCodes:
          - description: This request should be made in a different Scope.
            name: ScopeViolation
          - description: You do not have the necessary permissions to perform this request.
            name: PermissionViolation
          - description: The maximum memory limit in GB has been reached.
            name: MemoryQuotaExceeded
          - description: The vCPUs limit has been reached.
            name: VcpusQuotaExceeded
          - description: The maximum number of the Servers that you can launch or resume has been reached.
            name: ServersQuotaExceeded
          - description: The maximum number of the Servers that you can launch or resume on current Farm has been reached.
            name: ServersPerFarmQuotaExceeded
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse_3'
        404:
          description: Resource not found.
          x-errorCodes:
          - description: This endpoint does not exist.
            name: EndpointNotFound
            noDoc: true
          - description: The object you are trying to access does not exist.
            name: ObjectNotFound
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse_3'
        409:
          description: Conflict with current state.
          x-errorCodes:
          - description: These changes aren't possible while this object is in use.
            name: ObjectInUse
          - description: These changes would violate a policy.
            name: PolicyViolation
          - description: These changes would violate a unicity constraint.
            name: UnicityViolation
          - description: Some of the objects being changed are locked and cannot be changed.
            name: Locked
          - description: This Cloud platform is not enabled.
            name: NotEnabledPlatform
          - description: The Operating System does not match.
            name: OperatingSystemMismatch
          - description: Some of the objects or actions being access are deprecated.
            name: Deprecated
          - description: The action can't be perfomed in the current object's state.
            name: UnacceptableState
          - description: The object that you are trying to access does not exist on the cloud.
            name: ObjectNotFoundOnCloud
          - description: Some of these settings cannot be applied, they either conflict with the current state or are invalid.
            name: UnacceptableObjectConfiguration
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse_3'
        422:
          description: This request was well-formed but was rejected due to semantic errors.
          x-errorCodes:
          - description: Some of the configuration settings cannot be applied because they conflict with the current state.
            name: ConfigurationMismatch
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse_3'
        500:
          description: A server-side error occurred.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse_3'
        501:
          description: This feature is not implemented in Scalr.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse_3'
        503:
          description: The service is currently unavailable.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse_3'
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/RoleImage_3'
        description: The JSON representation of a RoleImage object.
        required: true
      tags:
      - Roles
      summary: Create by env id roles by role id images by image id actions replace
      x-summary-source: derived
      operationId: postByEnvIdRolesByRoleIdImagesByImageIdActionsReplace
      x-operation-id-source: derived
    servers:
    - url: /api/v1beta0/user
  /{envId}/roles/{roleId}/orchestration-rules/:
    parameters:
    - description: The ID of the Environment scoping this request.
      in: path
      name: envId
      required: true
      schema:
        type: integer
    - description: The ID of a Role object.
      in: path
      name: roleId
      required: true
      x-references:
        $ref: '#/components/schemas/Role_3'
      schema:
        type: integer
    get:
      description: List the Orchestration Rules associated with a given Role, including Rules from higher Scopes (those are read-only).
      responses:
        200:
          description: A list of OrchestrationRule objects.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/OrchestrationRuleListResponse_3'
        400:
          description: A client-side error was made.
          x-errorCodes:
          - description: The request was structurally incorrect, and was not understood by the API.
            name: InvalidStructure
          - description: The request was understood by the API, but included invalid data.
            name: InvalidValue
          - description: The request wasn't understood by the API.
            name: BadRequest
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse_3'
        401:
          description: The request was not authenticated.
          x-errorCodes:
          - description: The request was not properly authenticated.
            name: BadAuthentication
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse_3'
        403:
          description: Insufficient permissions.
          x-errorCodes:
          - description: This request should be made in a different Scope.
            name: ScopeViolation
          - description: You do not have the necessary permissions to perform this request.
            name: PermissionViolation
          - description: The maximum memory limit in GB has been reached.
            name: MemoryQuotaExceeded
          - description: The vCPUs limit has been reached.
            name: VcpusQuotaExceeded
          - description: The maximum number of the Servers that you can launch or resume has been reached.
            name: ServersQuotaExceeded
          - description: The maximum number of the Servers that you can launch or resume on current Farm has been reached.
            name: ServersPerFarmQuotaExceeded
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse_3'
        404:
          description: Resource not found.
          x-errorCodes:
          - description: This endpoint does not exist.
            name: EndpointNotFound
            noDoc: true
          - description: The object you are trying to access does not exist.
            name: ObjectNotFound
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse_3'
        409:
          description: Conflict with current state.
          x-errorCodes:
          - description: These changes aren't possible while this object is in use.
            name: ObjectInUse
          - description: These changes would violate a policy.
            name: PolicyViolation
          - description: These changes would violate a unicity constraint.
            name: UnicityViolation
          - description: Some of the objects being changed are locked and cannot be changed.
            name: Locked
          - description: This Cloud platform is not enabled.
            name: NotEnabledPlatform
          - description: The Operating System does not match.
            name: OperatingSystemMismatch
          - description: Some of the objects or actions being access are deprecated.
            name: Deprecated
          - description: The action can't be perfomed in the current object's state.
            name: UnacceptableState
          - description: The object that you are trying to access does not exist on the cloud.
            name: ObjectNotFoundOnCloud
          - description: Some of these settings cannot be applied, they either conflict with the current state or are invalid.
            name: UnacceptableObjectConfiguration
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse_3'
        422:
          description: This request was well-formed but was rejected due to semantic errors.
          x-errorCodes:
          - description: Some of the configuration settings cannot be applied because they conflict with the current state.
            name: ConfigurationMismatch
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse_3'
        500:
          description: A server-side error occurred.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse_3'
        501:
          description: This feature is not implemented in Scalr.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse_3'
        503:
          description: The service is currently unavailable.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse_3'
      tags:
      - Roles
      summary: Get by env id roles by role id orchestration rules
      x-summary-source: derived
      operationId: getByEnvIdRolesByRoleIdOrchestrationRules
      x-operation-id-source: derived
    post:
      description: Create a new Orchestration Rule associated to a Role.
      responses:
        201:
          description: The JSON representation of a OrchestrationRule object.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/OrchestrationRuleDetailsResponse_3'
        400:
          description: A client-side error was made.
          x-errorCodes:
          - description: The request was structurally incorrect, and was not understood by the API.
            name: InvalidStructure
          - description: The request was understood by the API, but included invalid data.
            name: InvalidValue
          - description: The request wasn't understood by the API.
            name: BadRequest
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse_3'
        401:
          description: The request was not authenticated.
          x-errorCodes:
          - description: The request was not properly authenticated.
            name: BadAuthentication
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse_3'
        403:
          description: Insufficient permissions.
          x-errorCodes:
          - description: This request should be made in a different Scope.
            name: ScopeViolation
          - description: You do not have the necessary permissions to perform this request.
            name: PermissionViolation
          - description: The maximum memory limit in GB has been reached.
            name: MemoryQuotaExceeded
          - description: The vCPUs limit has been reached.
            name: VcpusQuotaExceeded
          - description: The maximum number of the Servers that you can launch or resume has been reached.
            name: ServersQuotaExceeded
          - description: The maximum number of the Servers that you can launch or resume on current Farm has been reached.
            name: ServersPerFarmQuotaExceeded
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse_3'
        404:
          description: Resource not found.
          x-errorCodes:
          - description: This endpoint does not exist.
            name: EndpointNotFound
            noDoc: true
          - description: The object you are trying to access does not exist.
            name: ObjectNotFound
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse_3'
        409:
          description: Conflict with current state.
          x-errorCodes:
          - description: These changes aren't possible while this object is in use.
            name: ObjectInUse
          - description: These changes would violate a policy.
            name: PolicyViolation
          - description: These changes would violate a unicity constraint.
            name: UnicityViolation
          - description: Some of the objects being changed are locked and cannot be changed.
            name: Locked
          - description: This Cloud platform is not enabled.
            name: NotEnabledPlatform
          - description: The Operating System does not match.
            name: OperatingSystemMismatch
          - description: Some of the objects or actions being access are deprecated.
            name: Deprecated
          - description: The action can't be perfomed in the current object's state.
            name: UnacceptableState
          - description: The object that you are trying to access does not exist on the cloud.
            name: ObjectNotFoundOnCloud
          - description: Some of these settings cannot be applied, they either conflict with the current state or are invalid.
            name: UnacceptableObjectConfiguration
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse_3'
        422:
          description: This request was well-formed but was rejected due to semantic errors.
          x-errorCodes:
          - description: Some of the configuration settings cannot be applied because they conflict with the current state.
            name: ConfigurationMismatch
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse_3'
        500:
          description: A server-side error occurred.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse_3'
        501:
          description: This feature is not implemented in Scalr.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse_3'
        503:
          description: The service is currently unavailable.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse_3'
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/OrchestrationRule_3'
        description: The JSON representation of a OrchestrationRule object.
        required: true
      tags:
      - Roles
      summary: Create by env id roles by role id orchestration rules
      x-summary-source: derived
      operationId: postByEnvIdRolesByRoleIdOrchestrationRules
      x-operation-id-source: derived
    servers:
    - url: /api/v1beta0/user
  /{envId}/roles/{roleId}/orchestration-rules/{orchestrationRuleId}/:
    parameters:
    - description: The ID of the Environment scoping this request.
      in: path
      name: envId
      required: true
      schema:
        type: integer
    - description: The ID of a Role object.
      in: path
      name: roleId
      required: true
      x-references:
        $ref: '#/components/schemas/Role_3'
      schema:
        type: integer
    - description: The ID of a OrchestrationRule object.
      in: path
      name: orchestrationRuleId
      required: true
      x-references:
        $ref: '#/components/schemas/OrchestrationRule_3'
      schema:
        type: string
        maxLength: 36
        minLength: 36
        pattern: '[A-Fa-f0-9]{8}-([A-Fa-f0-9]{4}-){3}[A-Fa-f0-9]{12}'
    delete:
      description: Delete the specified Orchestration Rule from the Role.
      responses:
        204:
          description: Successful deletion.
        400:
          description: A client-side error was made.
          x-errorCodes:
          - description: The request was structurally incorrect, and was not understood by the API.
            name: InvalidStructure
          - description: The request was understood by the API, but included invalid data.
            name: InvalidValue
          - description: The request wasn't understood by the API.
            name: BadRequest
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse_3'
        401:
          description: The request was not authenticated.
          x-errorCodes:
          - description: The request was not properly authenticated.
            name: BadAuthentication
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse_3'
        403:
          description: Insufficient permissions.
          x-errorCodes:
          - description: This request should be made in a different Scope.
            name: ScopeViolation
          - description: You do not have the necessary permissions to perform this request.
            name: PermissionViolation
          - description: The maximum memory limit in GB has been reached.
            name: MemoryQuotaExceeded
          - description: The vCPUs limit has been reached.
            name: VcpusQuotaExceeded
          - description: The maximum number of the Servers that you can launch or resume has been reached.
            name: ServersQuotaExceeded
          - description: The maximum number of the Servers that you can launch or resume on current Farm has been reached.
            name: ServersPerFarmQuotaExceeded
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse_3'
        404:
          description: Resource not found.
          x-errorCodes:
          - description: This endpoint does not exist.
            name: EndpointNotFound
            noDoc: true
          - description: The object you are trying to access does not exist.
            name: ObjectNotFound
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse_3'
        409:
          description: Conflict with current state.
          x-errorCodes:
          - description: These changes aren't possible while this object is in use.
            name: ObjectInUse
          - description: These changes would violate a policy.
            name: PolicyViolation
          - description: These changes would violate a unicity constraint.
            name: UnicityViolation
          - description: Some of the objects being changed are locked and cannot be changed.
            name: Locked
          - description: This Cloud platform is not enabled.
            name: NotEnabledPlatform
          - description: The Operating System does not match.
            name: OperatingSystemMismatch
          - description: Some of the objects or actions being access are deprecated.
            name: Deprecated
          - description: The action can't be perfomed in the current object's state.
            name: UnacceptableState
          - description: The object that you are trying to access does not exist on the cloud.
            name: ObjectNotFoundOnCloud
          - description: Some of these settings cannot be applied, they either conflict with the current state or are invalid.
            name: UnacceptableObjectConfiguration
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse_3'
        422:
          description: This request was well-formed but was rejected due to semantic errors.
          x-errorCodes:
          - description: Some of the configuration settings cannot be applied because they conflict with the current state.
            name: ConfigurationMismatch
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse_3'
        500:
          description: A server-side error occurred.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse_3'
        501:
          description: This feature is not implemented in Scalr.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse_3'
        503:
          description: The service is currently unavailable.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse_3'
      tags:
      - Roles
      summary: Delete by env id roles by role id orchestration rules by orchestration rule id
      x-summary-source: derived
      operationId: deleteByEnvIdRolesByRoleIdOrchestrationRulesByOrchestrationRuleId
      x-operation-id-source: derived
    get:
      description: Retrieve a given Orchestration Rule associated to a Role.
      responses:
        200:
          description: The JSON representation of a OrchestrationRule object.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/OrchestrationRuleDetailsResponse_3'
        400:
          description: A client-side error was made.
          x-errorCodes:
          - description: The request was structurally incorrect, and was not understood by the API.
            name: InvalidStructure
          - description: The request was understood by the API, but included invalid data.
            name: InvalidValue
          - description: The request wasn't understood by the API.
            name: BadRequest
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse_3'
        401:
          description: The request was not authenticated.
          x-errorCodes:
          - description: The request was not properly authenticated.
            name: BadAuthentication
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse_3'
        403:
          description: Insufficient permissions.
          x-errorCodes:
          - description: This request should be made in a different Scope.
            name: ScopeViolation
          - description: You do not have the necessary permissions to perform this request.
            name: PermissionViolation
          - description: The maximum memory limit in GB has been reached.
            name: MemoryQuotaExceeded
          - description: The vCPUs limit has been reached.
            name: VcpusQuotaExceeded
          - description: The maximum number of the Servers that you can launch or resume has been reached.
            name: ServersQuotaExceeded
          - description: The maximum number of the Servers that you can launch or resume on current Farm has been reached.
            name: ServersPerFarmQuotaExceeded
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse_3'
        404:
          description: Resource not found.
          x-errorCodes:
          - description: This endpoint does not exist.
            name: EndpointNotFound
            noDoc: true
          - description: The object you are trying to access does not exist.
            name: ObjectNotFound
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse_3'
        409:
          description: Conflict with current state.
          x-errorCodes:
          - description: These changes aren't possible while this object is in use.
            name: ObjectInUse
          - description: These changes would violate a policy.
            name: PolicyViolation
          - description: These changes would violate a unicity constraint.
            name: UnicityViolation
          - description: Some of the objects being changed are locked and cannot be changed.
            name: Locked
          - description: This Cloud platform is not enabled.
            name: NotEnabledPlatform
          - description: The Operating System does not match.
            name: OperatingSystemMismatch
          - description: Some of the objects or actions being access are deprecated.
            name: Deprecated
          - description: The action can't be perfomed in the current object's state.
            name: UnacceptableState
          - description: The object that you are trying to access does not exist on the cloud.
            name: ObjectNotFoundOnCloud
          - description: Some of these settings cannot be applied, they either conflict with the current state or are invalid.
            name: UnacceptableObjectConfiguration
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse_3'
        422:
          description: This request was well-formed but was rejected due to semantic errors.
          x-errorCodes:
          - description: Some of the configuration settings cannot be applied because they conflict with the current state.
            name: ConfigurationMismatch
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse_3'
        500:
          description: A server-side error occurred.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse_3'
        501:
          description: This feature is not implemented in Scalr.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse_3'
        503:
          description: The service is currently unavailable.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse_3'
      tags:
      - Roles
      summary: Get by env id roles by role id orchestration rules by orchestration rule id
      x-summary-source: derived
      operationId: getByEnvIdRolesByRoleIdOrchestrationRulesByOrchestrationRuleId
      x-operation-id-source: derived
    patch:
      description: Modify the attributes of a given Orchestration Rule.
      responses:
        200:
          description: The JSON representation of a OrchestrationRule object.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/OrchestrationRuleDetailsResponse_3'
        400:
          description: A client-side error was made.
          x-errorCodes:
          - description: The request was structurally incorrect, and was not understood by the API.
            name: InvalidStructure
          - description: The request was understood by the API, but included invalid data.
            name: InvalidValue
          - description: The request wasn't understood by the API.
            name: BadRequest
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse_3'
        401:
          description: The request was not authenticated.
          x-errorCodes:
          - description: The request was not properly authenticated.
            name: BadAuthentication
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse_3'
        403:
          description: Insufficient permissions.
          x-errorCodes:
          - description: This request should be made in a different Scope.
            name: ScopeViolation
          - description: You do not have the necessary permissions to perform this request.
            name: PermissionViolation
          - description: The maximum memory limit in GB has been reached.
            name: MemoryQuotaExceeded
          - description: The vCPUs limit has been reached.
            name: VcpusQuotaExceeded
          - description: The maximum number of the Servers that you can launch or resume has been reached.
            name: ServersQuotaExceeded
          - description: The maximum number of the Servers that you can launch or resume on current Farm has been reached.
            name: ServersPerFarmQuotaExceeded
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse_3'
        404:
          description: Resource not found.
          x-errorCodes:
          - description: This endpoint does not exist.
            name: EndpointNotFound
            noDoc: true
          - description: The object you are trying to access does not exist.
            name: ObjectNotFound
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse_3'
        409:
          description: Conflict with current state.
          x-errorCodes:
          - description: These changes aren't possible while this object is in use.
            name: ObjectInUse
          - description: These changes would violate a policy.
            name: PolicyViolation
          - description: These changes would violate a unicity constraint.
            name: UnicityViolation
          - description: Some of the objects being changed are locked and cannot be changed.
            name: Locked
          - description: This Cloud platform is not enabled.
            name: NotEnabledPlatform
          - description: The Operating System does not match.
            name: OperatingSystemMismatch
          - description: Some of the objects or actions being access are deprecated.
            name: Deprecated
          - description: The action can't be perfomed in the current object's state.
            name: UnacceptableState
          - description: The object that you are trying to access does not exist on the cloud.
            name: ObjectNotFoundOnCloud
          - description: Some of these settings cannot be applied, they either conflict with the current state or are invalid.
            name: UnacceptableObjectConfiguration
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse_3'
        422:
          description: This request was well-formed but was rejected due to semantic errors.
          x-errorCodes:
          - description: Some of the configuration settings cannot be applied because they conflict with the current state.
            name: ConfigurationMismatch
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse_3'
        500:
          description: A server-side error occurred.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse_3'
        501:
          description: This feature is not implemented in Scalr.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse_3'
        503:
          description: The service is currently unavailable.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse_3'
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/OrchestrationRule_3'
        description: The JSON representation of a OrchestrationRule object.
        required: true
      tags:
      - Roles
      summary: Update by env id roles by role id orchestration rules by orchestration rule id
      x-summary-source: derived
      operationId: patchByEnvIdRolesByRoleIdOrchestrationRulesByOrchestrationRuleId
      x-operation-id-source: derived
    servers:
    - url: /api/v1beta0/user
components:
  schemas:
    EventForeignKey:
      properties:
        id:
          description: The name of this Custom Event.
          type: string
      required:
      - id
      x-derived:
        $ref: '#/components/schemas/Event'
      x-references:
        $ref: '#/components/schemas/Event'
    ScriptVariableList:
      properties:
        allowedValues:
          description: The list of values to which the Global Variable value must correspond.
          items:
            $ref: '#/components/schemas/VariableAllowedValue'
          type: array
        category:
          description: The category of this Variable.
          type: string
        description:
          description: A description that explains what this Global Variable is used for.
          type: string
        hidden:
          description: Whether this Script Variable is hidden in Orchestration Rules or execute Script action.
          type: boolean
        locked:
          description: Whether this Script Variable value is locked in Orchestration Rules or execute Script action.
          type: boolean
        name:
          description: The Variable name.
          type: string
        required:
          description: Whether this Script Variable value is required in Orchestration Rules or execute Script action.
          type: boolean
        type:
          description: The type of Script Variable.
          enum:
          - ScriptVariableJson
          - ScriptVariableString
          - ScriptVariableList
          - ScriptVariableRemoteList
          type: string
        value:
          description: The value declared in the script level. This property can be redefined for not locked variables in Orhestration Rules or execute Script action. Also this field supports Global Variable interpolation.
          type: string
      required:
      - name
      - type
      - allowedValues
      x-abstractType:
        $ref: '#/components/schemas/ScriptVariable'
      x-createOnly:
      - name
      x-discriminator: type
    GlobalVariableDetailsResponse:
      properties:
        data:
          $ref: '#/components/schemas/GlobalVariable'
          readOnly: true
        errors:
          items:
            $ref: '#/components/schemas/ApiMessage'
          readOnly: true
          type: array
        meta:
          $ref: '#/components/schemas/ApiMetaContainer'
          readOnly: true
        warnings:
          items:
            $ref: '#/components/schemas/ApiMessage'
          readOnly: true
          type: array
      x-derived:
        $ref: '#/components/schemas/GlobalVariable'
      x-usedIn:
      - /account/{accountId}/global-variables/
      - /account/{accountId}/global-variables/{globalVariableName}/
      - /account/{accountId}/roles/{roleId}/global-variables/
      - /account/{accountId}/roles/{roleId}/global-variables/{globalVariableName}/
    ScriptVariableString:
      properties:
        category:
          description: The category of this Variable.
          type: string
        description:
          description: A description that explains what this Global Variable is used for.
          type: string
        hidden:
          description: Whether this Script Variable is hidden in Orchestration Rules or execute Script action.
          type: boolean
        locked:
          description: Whether this Script Variable value is locked in Orchestration Rules or execute Script action.
          type: boolean
        name:
          description: The Variable name.
          type: string
        outputFormat:
          description: The formatting to apply when displaying this variable.
          type: string
        required:
          description: Whether this Script Variable value is required in Orchestration Rules or execute Script action.
          type: boolean
        type:
          description: The type of Script Variable.
          enum:
          - ScriptVariableJson
          - ScriptVariableString
          - ScriptVariableList
          - ScriptVariableRemoteList
          type: string
        validationPattern:
          description: The validation regular expresion to validate the value.
          type: string
        value:
          description: The value declared in the script level. This property can be redefined for not locked variables in Orhestration Rules or execute Script action. Also this field supports Global Variable interpolation.
          type: string
      required:
      - name
      - type
      x-abstractType:
        $ref: '#/components/schemas/ScriptVariable'
      x-createOnly:
      - name
      x-discriminator: type
    ScriptVariableJson:
      properties:
        category:
          description: The category of this Variable.
          type: string
        description:
          description: A description that explains what this Global Variable is used for.
          type: string
        hidden:
          description: Whether this Script Variable is hidden in Orchestration Rules or execute Script action.
          type: boolean
        locked:
          description: Whether this Script Variable value is locked in Orchestration Rules or execute Script action.
          type: boolean
        name:
          description: The Variable name.
          type: string
        required:
          description: Whether this Script Variable value is required in Orchestration Rules or execute Script action.
          type: boolean
        type:
          description: The type of Script Variable.
          enum:
          - ScriptVariableJson
          - ScriptVariableString
          - ScriptVariableList
          - ScriptVariableRemoteList
          type: string
        value:
          description: The value declared in the script level. This property can be redefined for not locked variables in Orhestration Rules or execute Script action. Also this field supports Global Variable interpolation.
          type: string
      required:
      - name
      - type
      x-abstractType:
        $ref: '#/components/schemas/ScriptVariable'
      x-createOnly:
      - name
      x-discriminator: type
    AnsibleTowerAction:
      properties:
        actionType:
          enum:
          - ScalrScriptAction
          - RemoteScriptAction
          - ChefAction
          - LocalScriptAction
          - AnsibleTowerAction
          type: string
        configuration:
          $ref: '#/components/schemas/AnsibleTowerConfigurationIdentity'
          description: Ansible Tower identity stored at Bootstrap Configurations.
        jobTemplate:
          $ref: '#/components/schemas/JobTemplateIdentity'
          description: Ansible Tower Job template stored at Tower Server.
        variables:
          description: The Ansible Tower variables to override. JSON or YAML representation.
          type: string
      required:
      - actionType
      - configuration
      - jobTemplate
      x-abstractType:
        $ref: '#/components/schemas/Action'
      x-discriminator: actionType
    AccountRoleListResponse:
      properties:
        data:
          items:
            $ref: '#/components/schemas/AccountRole'
          readOnly: true
          type: array
        errors:
          items:
            $ref: '#/components/schemas/ApiMessage'
          readOnly: true
          type: array
        meta:
          $ref: '#/components/schemas/ApiMetaContainer'
          readOnly: true
        pagination:
          $ref: '#/components/schemas/ApiPagination'
          readOnly: true
        warnings:
          items:
            $ref: '#/components/schemas/ApiMessage'
          readOnly: true
          type: array
      x-derived:
        $ref: '#/components/schemas/AccountRole'
      x-usedIn:
      - /account/{accountId}/roles/
    TriggeringServerTarget:
      description: Select only the Server that triggered the Event.
      properties:
        conditions:
          description: 'Triggering servers conditions. '
          items:
            $ref: '#/components/schemas/OrchestrationRuleCondition'
          type: array
        targetType:
          enum:
          - TriggeringServerTarget
          - TriggeringFarmRoleTarget
          - FarmTarget
          type: string
      required:
      - targetType
      x-abstractType:
        $ref: '#/components/schemas/Target'
      x-discriminator: targetType
    TriggeringFarmRoleTarget:
      description: Selects all the Servers that belong to the same Farm Role as the Server that triggered the Event.
      properties:
        targetType:
          enum:
          - TriggeringServerTarget
          - TriggeringFarmRoleTarget
          - FarmTarget
          type: string
      required:
      - targetType
      x-abstractType:
        $ref: '#/components/schemas/Target'
      x-discriminator: targetType
    RoleImage:
      description: A Representation of a Role Image. Note that the `role` property is optional when POST-ing to a URL that already includes `{roleId}`.
      properties:
        image:
          $ref: '#/components/schemas/ImageForeignKey'
        role:
          $ref: '#/components/schemas/RoleForeignKey'
      required:
      - role
      - image
      x-filterable:
      - role
      - image
      x-usedIn:
      - /account/{accountId}/roles/{roleId}/images/
      - /account/{accountId}/roles/{roleId}/images/{imageId}/actions/replace/
    FarmTarget:
      description: Selects all the Servers that belong to the triggering Server's Farm.
      properties:
        conditions:
          description: 'Triggering servers conditions. '
          items:
            $ref: '#/components/schemas/OrchestrationRuleCondition'
          type: array
        targetType:
          enum:
          - TriggeringServerTarget
          - TriggeringFarmRoleTarget
          - FarmTarget
          type: string
      required:
      - targetType
      x-abstractType:
        $ref: '#/components/schemas/Target'
      x-discriminator: targetType
    Script:
      properties:
        added:
          format: date-time
          readOnly: true
          type: string
        blockingDefault:
          description: Whether this Script is blocking by default. This can be overridden in the Orchestration Rules that use this Script.
          type: boolean
        deprecated:
          default: false
          description: Whether the Script is deprecated.
          type: boolean
        description:
          type: string
        id:
          readOnly: true
          type: integer
        lastChanged:
          format: date-time
          readOnly: true
          type: string
        name:
          description: The name of the Script.
          type: string
        osType:
          description: Type of OS this Script should execute on. This will influence Orchestration Rules, and will impact the validation rules applied to the ScriptVersions.
          enum:
          - linux
          - windows
          - cross-platform
          type: string
        requireAdminPrivileges:
          default: false
          description: Whether the Script requires root/Administrator privileges to be executed.
          type: boolean
        scope:
          enum:
          - scalr
          - account
          - environment
          - farm
          - farmrole
          - role
          - server
          readOnly: true
          type: string
        tags:
          description: The list of tags that should be applied to the Script.
          items:
            type: string
          type: array
        timeoutDefault:
          description: The time after which Scalr will abort the execution of this Script, in seconds. This can be overridden in the Orchestration Rules that use this Script.
          type: integer
        variables:
          description: The list of redefined script variables for the current script.
          items:
            $ref: '#/components/schemas/ScriptVariable'
          type: array
      required:
      - name
      - osType
      x-createOnly:
      - osType
      x-filterable:
      - id
      - name
      - osType
      - blockingDefault
      - scope
      - deprecated
      - requireAdminPrivileges
      x-usedIn:
      - /account/{accountId}/scripts/
      - /account/{accountId}/scripts/{scriptId}/
      - /account/{accountId}/scripts/{scriptId}/script-versions/
      - /account/{accountId}/scripts/{scriptId}/script-versions/{scriptVersionNumber}/
    GlobalVariableListResponse:
      properties:
        data:
          items:
            $ref: '#/components/schemas/GlobalVariable'
          readOnly: true
          type: array
        errors:
          items:
            $ref: '#/components/schemas/ApiMessage'
          readOnly: true
          type: array
        meta:
          $ref: '#/components/schemas/ApiMetaContainer'
          readOnly: true
        pagination:
          $ref: '#/components/schemas/ApiPagination'
          readOnly: true
        warnings:
          items:
            $ref: '#/components/schemas/ApiMessage'
          readOnly: true
          type: array
      x-derived:
        $ref: '#/components/schemas/GlobalVariable'
      x-usedIn:
      - /account/{accountId}/global-variables/
      - /account/{accountId}/roles/{roleId}/global-variables/
    ChefAction:
      properties:
        actionType:
          enum:
          - ScalrScriptAction
          - RemoteScriptAction
          - ChefAction
          - LocalScriptAction
          - AnsibleTowerAction
          type: string
      required:
      - actionType
      x-abstractType:
        $ref: '#/components/schemas/Action'
      x-discriminator: actionType
    WebhookEndpoint:
      description: Webhook endpoint summary.
      properties:
        id:
          description: Identifier of the Webhook endpoint.
          maxLength: 36
          minLength: 36
          pattern: '[A-Fa-f0-9]{8}-([A-Fa-f0-9]{4}-){3}[A-Fa-f0-9]{12}'
          type: string
        scope:
          enum:
          - scalr
          - account
          - environment
          - farm
          - farmrole
          - role
          - server
          type: string
        url:
          description: Endpoint URL.
          type: string
    RoleCategory:
      properties:
        id:
          description: Identifier of the Role Category.
          readOnly: true
          type: integer
        name:
          description: The name of the Role Category.
          type: string
        scope:
          description: The Scope in which this Role Category is defined.
          enum:
          - scalr
          - account
          - environment
          - farm
          - farmrole
          - role
          - server
          readOnly: true
          type: string
      required:
      - name
      x-filterable:
      - id
      - name
      - scope
      x-usedIn:
      - /account/{accountId}/role-categories/
      - /account/{accountId}/role-categories/{roleCategoryId}/
    OrchestrationRuleDetailsResponse:
      properties:
        data:
          $ref: '#/components/schemas/OrchestrationRule'
          readOnly: true
        errors:
          items:
            $ref: '#/components/schemas/ApiMessage'
          readOnly: true
          type: array
        meta:
          $ref: '#/components/schemas/ApiMetaContainer'
          readOnly: true
        warnings:
          items:
            $ref: '#/components/schemas/ApiMessage'
          readOnly: true
          type: array
      x-derived:
        $ref: '#/components/schemas/OrchestrationRule'
      x-usedIn:
      - /account/{accountId}/orchestration-rules/
      - /account/{accountId}/orchestration-rules/{orchestrationRuleId}/
      - /account/{accountId}/roles/{roleId}/orchestration-rules/
      - /account/{accountId}/roles/{roleId}/orchestration-rules/{orchestrationRuleId}/
    RoleImageDetailsResponse:
      properties:
        data:
          $ref: '#/components/schemas/RoleImage'
          readOnly: true
        errors:
          items:
            $ref: '#/components/schemas/ApiMessage'
          readOnly: true
          type: array
        meta:
          $ref: '#/components/schemas/ApiMetaContainer'
          readOnly: true
        warnings:
          items:
            $ref: '#/components/schemas/ApiMessage'
          readOnly: true
          type: array
      x-derived:
        $ref: '#/components/schemas/RoleImage'
      x-usedIn:
      - /account/{accountId}/roles/{roleId}/images/
      - /account/{accountId}/roles/{roleId}/images/{imageId}/actions/replace/
    ImageDetailsResponse:
      properties:
        data:
          $ref: '#/components/schemas/Image'
          readOnly: true
        errors:
          items:
            $ref: '#/components/schemas/ApiMessage'
          readOnly: true
          type: array
        meta:
          $ref: '#/components/schemas/ApiMetaContainer'
          readOnly: true
        warnings:
          items:
            $ref: '#/components/schemas/ApiMessage'
          readOnly: true
          type: array
      x-derived:
        $ref: '#/components/schemas/Image'
      x-usedIn:
      - /account/{accountId}/roles/{roleId}/images/{imageId}/
      - /account/{accountId}/images/
      - /account/{accountId}/images/{imageId}/
      - /account/{accountId}/images/{imageId}/actions/copy/
    ScalrScriptAction:
      properties:
        actionType:
          enum:
          - ScalrScriptAction
          - RemoteScriptAction
          - ChefAction
          - LocalScriptAction
          - AnsibleTowerAction
          type: string
        scriptVersion:
          $ref: '#/components/schemas/ScriptVersionIdentity'
          description: ScriptVersion to execute.
      required:
      - actionType
      - scriptVersion
      x-abstractType:
        $ref: '#/components/schemas/Action'
      x-discriminator: actionType
    GlobalVariableList:
      properties:
        allowedValues:
          description: The list of values to which the Global Variable value must correspond.
          items:
            $ref: '#/components/schemas/VariableAllowedValue'
          type: array
        category:
          description: The category of this Global Variable.
          type: string
        computedValue:
          description: The value computed by Scalr for this Global Variable.
          readOnly: true
          type: string
        declaredIn:
          description: The Scope in which this Global Variable is declared.
          enum:
          - scalr
          - account
          - environment
          - farm
          - farmrole
          - role
          - server
          readOnly: true
          type: string
        definedIn:
          description: The Scope in which this Global Variable is given a value.
          enum:
          - scalr
          - account
          - environment
          - farm
          - farmrole
          - role
          - server
          readOnly: true
          type: string
        description:
          description: A description that explains what this Global Variable is used for.
          type: string
        hidden:
          description: Whether this Global Variable is hidden in inner scopes. This is read-only if `declaredIn` is not the current scope.
          type: boolean
        locked:
          description: Whether this Global Variable is locked in inner scopes. This is read-only if `declaredIn` is not the current scope.
          type: boolean
        name:
          type: string
        requiredIn:
          description: The scope where this Global Variable is required. This is read-only if `declaredIn` is not the current scope.
          enum:
          - scalr
          - account
          - environment
          - farm
          - farmrole
          - role
          - server
          type: string
        type:
          description: The type of Global Variable.
          enum:
          - GlobalVariableJson
          - GlobalVariableString
          - GlobalVariableList
          - GlobalVariableRemoteList
          type: string
        value:
          description: The value declared in the current scope for this Global Variable. If it is null, the value will be inherited from higher scopes. This field is ignored if `declaredIn` is not the current scope.
          type: string
      required:
      - name
      - type
      - allowedValues
      x-abstractType:
        $ref: '#/components/schemas/GlobalVariable'
      x-createOnly:
      - name
      x-discriminator: type
    AccountRoleDetailsResponse:
      properties:
        data:
          $ref: '#/components/schemas/AccountRole'
          readOnly: true
        errors:
          items:
            $ref: '#/components/schemas/ApiMessage'
          readOnly: true
          type: array
        meta:
          $ref: '#/components/schemas/ApiMetaContainer'
          readOnly: true
        warnings:
          items:
            $ref: '#/components/schemas/ApiMessage'
          readOnly: true
          type: array
      x-derived:
        $ref: '#/components/schemas/AccountRole'
      x-usedIn:
      - /account/{accountId}/roles/
      - /account/{accountId}/roles/{roleId}/
      - /account/{accountId}/roles/{roleId}/actions/clone/
      - /account/{accountId}/roles/{roleId}/actions/deprecate/
    Target:
      discriminator:
        propertyName: targetType
      properties:
        targetType:
          enum:
          - TriggeringServerTarget
          - TriggeringFarmRoleTarget
          - FarmTarget
          type: string
      required:
      - targetType
      x-concreteTypes:
      - $ref: '#/components/schemas/TriggeringServerTarget'
      - $ref: '#/components/schemas/TriggeringFarmRoleTarget'
      - $ref: '#/components/schemas/FarmTarget'
    LocalScriptAction:
      properties:
        actionType:
          enum:
          - ScalrScriptAction
          - RemoteScriptAction
          - ChefAction
          - LocalScriptAction
          - AnsibleTowerAction
          type: string
        path:
          description: Path to the script to execute.
          type: string
      required:
      - actionType
      - path
      x-abstractType:
        $ref: '#/components/schemas/Action'
      x-discriminator: actionType
    AwsImageCloudFeatures:
      description: Cloud specifics Image configuration.
      properties:
        accountId:
          description: AWS account ID
          type: string
        type:
          enum:
          - AwsImageCloudFeatures
          - VmwareImageCloudFeatures
          - OpenstackImageCloudFeatures
          type: string
        virtualization:
          description: 'The Image virtualization type. Can be one of two types of virtualization: paravirtual (pv) or hardware virtual machine (hvm). At the Environment scope this property is read-only.'
          enum:
          - hvm
          - paravirtual
          type: string
      required:
      - type
      x-abstractType:
        $ref: '#/components/schemas/ImageCloudFeatures'
      x-discriminator: type
    Os:
      properties:
        family:
          description: OS family.
          type: string
        generation:
          type: string
        id:
          description: An ID uniquely identifying this OS in Scalr.
          pattern: '[A-Za-z0-9-]+'
          type: string
        name:
          description: A human-readable name for this OS.
          type: string
        version:
          type: string
      required:
      - id
      - name
      - family
      - generation
      - version
      x-filterable:
      - id
      - name
      - family
      - generation
      x-usedIn:
      - /account/{accountId}/os/{osId}/
    GlobalVariableJson:
      properties:
        category:
          description: The category of this Global Variable.
          type: string
        computedValue:
          description: The value computed by Scalr for this Global Variable.
          readOnly: true
          type: string
        declaredIn:
          description: The Scope in which this Global Variable is declared.
          enum:
          - scalr
          - account
          - environment
          - farm
          - farmrole
          - role
          - server
          readOnly: true
          type: string
        definedIn:
          description: The Scope in which this Global Variable is given a value.
          enum:
          - scalr
          - account
          - environment
          - farm
          - farmrole
          - role
          - server
          readOnly: true
          type: string
        description:
          description: A description that explains what this Global Variable is used for.
          type: string
        hidden:
          description: Whether this Global Variable is hidden in inner scopes. This is read-only if `declaredIn` is not the current scope.
          type: boolean
        locked:
          description: Whether this Global Variable is locked in inner scopes. This is read-only if `declaredIn` is not the current scope.
          type: boolean
        name:
          type: string
        requiredIn:
          description: The scope where this Global Variable is required. This is read-only if `declaredIn` is not the current scope.
          enum:
          - scalr
          - account
          - environment
          - farm
          - farmrole
          - role
          - server
          type: string
        type:
          description: The type of Global Variable.
          enum:
          - GlobalVariableJson
          - GlobalVariableString
          - GlobalVariableList
          - GlobalVariableRemoteList
          type: string
        value:
          description: The value declared in the current scope for this Global Variable. If it is null, the value will be inherited from higher scopes. This field is ignored if `declaredIn` is not the current scope.
          type: string
      required:
      - name
      - type
      x-abstractType:
        $ref: '#/components/schemas/GlobalVariable'
      x-createOnly:
      - name
      x-discriminator: type
    JobTemplateIdentity:
      properties:
        id:
          description: The Ansible Tower Job template identifier.
          type: integer
        name:
          description: The Ansible Tower Job template name.
          type: string
        scmBranch:
          description: The Ansible Tower Job template SCM Branch.
          type: string
    EnvironmentForeignKey:
      properties:
        id:
          description: Unique identifier of a Environment object.
          type: integer
      required:
      - id
      x-derived:
        $ref: '#/components/schemas/Environment'
      x-references:
        $ref: '#/components/schemas/Environment'
    SpecificEventTrigger:
      description: Triggers when a specific Event fires.
      properties:
        event:
          $ref: '#/components/schemas/EventForeignKey'
        triggerType:
          enum:
          - AllEventsTrigger
          - SpecificEventTrigger
          type: string
      required:
      - triggerType
      - event
      x-abstractType:
        $ref: '#/components/schemas/Trigger'
      x-discriminator: triggerType
    FarmRoleIdentity:
      properties:
        alias:
          description: The Alias for this Farm Role.
          type: string
        id:
          description: Unique identifier for this Farm Role.
          readOnly: true
          type: integer
    ImageForeignKey:
      properties:
        id:
          type: string
      required:
      - id
      x-derived:
        $ref: '#/components/schemas/Image'
      x-references:
        $ref: '#/components/schemas/Image'
    AccountRole:
      properties:
        allowedEnvironments:
          description: List of allowed Environments accessible to this Role.
          items:
            $ref: '#/components/schemas/EnvironmentForeignKey'
          type: array
        builtinAutomation:
          items:
            enum:
            - base
            - chef
            - mysql
            - percona
            - postgresql
            - redis
            - apache
            - nginx
            - tomcat
            - haproxy
            - rabbitmq
            - memcached
            type: string
          type: array
        category:
          $ref: '#/components/schemas/RoleCategoryForeignKey'
        deprecated:
          $ref: '#/components/schemas/RoleDeprecatedOptions'
          description: The deprecation options associated with this Role.
          readOnly: true
        description:
          type: string
        id:
          readOnly: true
          type: integer
        name:
          type: string
        os:
          $ref: '#/components/schemas/OsForeignKey'
        quickStart:
          description: Whether this Role is a QuickStart Role.
          type: boolean
        quickStartGroup:
          description: The name of the QuickStart Group.
          type: string
        restrictedAccess:
          description: It restricts access to the Role except of allowed Environments.
          type: boolean
        scope:
          enum:
          - scalr
          - account
          - environment
          - farm
          - farmrole
          - role
          - server
          readOnly: true
          type: string
        tags:
          description: The list of tags that should be applied to the Role.
          items:
            type: string
          type: array
        useScalrAgent:
          description: Whether this Role uses Scalarizr, the Scalr agent.
          type: boolean
      required:
      - name
      - category
      - os
      x-createOnly:
      - useScalrAgent
      - builtinAutomation
      - os
      x-derived:
        $ref: '#/components/schemas/Role'
      x-filterable:
      - id
      - name
      - os
      - category
      - scope
      - quickStart
      - quickStartGroup
      - useScalrAgent
      - builtinAutomation
      - restrictedAccess
      x-references:
        $ref: '#/components/schemas/Role'
      x-usedIn:
      - /account/{accountId}/roles/
      - /account/{accountId}/roles/{roleId}/
    ImageCloudFeatures:
      description: Cloud specifics Image configuration.
      discriminator:
        propertyName: type
      properties:
        type:
          enum:
          - AwsImageCloudFeatures
          - VmwareImageCloudFeatures
          - OpenstackImageCloudFeatures
          type: string
      required:
      - type
      x-concreteTypes:
      - $ref: '#/components/schemas/AwsImageCloudFeatures'
      - $ref: '#/components/schemas/VmwareImageCloudFeatures'
      - $ref: '#/components/schemas/OpenstackImageCloudFeatures'
    RoleCategoryForeignKey:
      properties:
        id:
          description: Identifier of the Role Category.
          type: integer
      required:
      - id
      x-derived:
        $ref: '#/components/schemas/RoleCategory'
      x-references:
        $ref: '#/components/schemas/RoleCategory'
    RoleDeprecatedOptions:
      properties:
        deprecated:
          description: The timestamp at which this Role was deprecated.
          format: date-time
          readOnly: true
          type: string
        replacement:
          $ref: '#/components/schemas/RoleForeignKey'
          description: The suggested replacement Role foreign key for the current Role.
          readOnly: true
    OrchestrationRule:
      properties:
        action:
          $ref: '#/components/schemas/Action'
          description: Orchestration Rule action that will be executed on the target servers.
        blocking:
          description: Whether this Orchestration Rule's execution should delay the execution of further Orchestration Rules and the triggering of further Events. Defaults to the action's default when null.
          type: boolean
        enabled:
          default: true
          description: If Orchestration Rule is not enabled this Rule won't be triggered.
          type: boolean
        id:
          description: Unique identifier for an Orchestration Rule.
          maxLength: 36
          minLength: 36
          pattern: '[A-Fa-f0-9]{8}-([A-Fa-f0-9]{4}-){3}[A-Fa-f0-9]{12}'
          readOnly: true
          type: string
        order:
          description: The priority of this Orchestration Rule relative to other Orchestration Rules that use the same triggeringEvent. Default is relative to existing Rules.
          type: integer
        runAs:
          description: User the Orchestration Rule should execute as. Defaults to root / Administrator when null.
          type: string
        scope:
          enum:
          - scalr
          - account
          - environment
          - farm
          - farmrole
          - role
          - server
          readOnly: true
          type: string
        target:
          $ref: '#/components/schemas/Target'
          description: The set of Servers where the Orchestration Rules Action will be executed.
        timeout:
          description: The time Scalr should wait for before aborting the execution of this Orchestration Rule, in seconds. Defaults to the action's default when null.
          type: integer
        trigger:
          $ref: '#/components/schemas/Trigger'
          description: Orchestration Rule trigger.
      required:
      - trigger
      - action
      - target
      x-filterable:
      - id
      - blocking
      - order
      - runAs
      x-usedIn:
      - /account/{accountId}/orchestration-rules/
      - /account/{accountId}/orchestration-rules/{orchestrationRuleId}/
      - /account/{accountId}/roles/{roleId}/orchestration-rules/
      - /account/{accountId}/roles/{roleId}/orchestration-rules/{orchestrationRuleId}/
    ScriptVariableRemoteList:
      properties:
        category:
          description: The category of this Variable.
          type: string
        description:
          description: A description that explains what this Global Variable is used for.
          type: string
        hidden:
          description: Whether this Script Variable is hidden in Orchestration Rules or execute Script action.
          type: boolean
        integrationEndpoint:
          $ref: '#/components/schemas/WebhookEndpointForeignKey'
        locked:
          description: Whether this Script Variable value is locked in Orchestration Rules or execute Script action.
          type: boolean
        name:
          description: The Variable name.
          type: string
        required:
          description: Whether this Script Variable value is required in Orchestration Rules or execute Script action.
          type: boolean
        type:
          description: The type of Script Variable.
          enum:
          - ScriptVariableJson
          - ScriptVariableString
          - ScriptVariableList
          - ScriptVariableRemoteList
          type: string
        value:
          description: The value declared in the script level. This property can be redefined for not locked variables in Orhestration Rules or execute Script action. Also this field supports Global Variable interpolation.
          type: string
      required:
      - name
      - type
      - integrationEndpoint
      x-abstractType:
        $ref: '#/components/schemas/ScriptVariable'
      x-createOnly:
      - name
      x-discriminator: type
    GlobalVariableRemoteList:
      properties:
        category:
          description: The category of this Global Variable.
          type: string
        computedValue:
          description: The value computed by Scalr for this Global Variable.
          readOnly: true
          type: string
        declaredIn:
          description: The Scope in which this Global Variable is declared.
          enum:
          - scalr
          - account
          - environment
          - farm
          - farmrole
          - role
          - server
          readOnly: true
          type: string
        definedIn:
          description: The Scope in which this Global Variable is given a value.
          enum:
          - scalr
          - account
          - environment
          - farm
          - farmrole
          - role
          - server
          readOnly: true
          type: string
        description:
          description: A description that explains what this Global Variable is used for.
          type: string
        hidden:
          description: Whether this Global Variable is hidden in inner scopes. This is read-only if `declaredIn` is not the current scope.
          type: boolean
        integrationEndpoint:
          $ref: '#/components/schemas/WebhookEndpointForeignKey'
        locked:
          description: Whether this Global Variable is locked in inner scopes. This is read-only if `declaredIn` is not the current scope.
          type: boolean
        name:
          type: string
        requiredIn:
          description: The scope where this Global Variable is required. This is read-only if `declaredIn` is not the current scope.
          enum:
          - scalr
          - account
          - environment
          - farm
          - farmrole
          - role
          - server
          type: string
        type:
          description: The type of Global Variable.
          enum:
          - GlobalVariableJson
          - GlobalVariableString
          - GlobalVariableList
          - GlobalVariableRemoteList
          type: string
        value:
          description: The value declared in the current scope for this Global Variable. If it is null, the value will be inherited from higher scopes. This field is ignored if `declaredIn` is not the current scope.
          type: string
      required:
      - name
      - type
      - integrationEndpoint
      x-abstractType:
        $ref: '#/components/schemas/GlobalVariable'
      x-createOnly:
      - name
      x-discriminator: type
    OpenstackImageCloudFeatures:
      description: Cloud specifics Image configuration.
      properties:
        sourceType:
          default: image
          description: The Openstack image source type. Set shapshot for image based on volume shapshot.
          enum:
          - image
          - snapshot
          type: string
        type:
          enum:
          - AwsImageCloudFeatures
          - VmwareImageCloudFeatures
          - OpenstackImageCloudFeatures
          type: string
      required:
      - type
      x-abstractType:
        $ref: '#/components/schemas/ImageCloudFeatures'
      x-discriminator: type
    Environment:
      properties:
        added:
          format: date-time
          readOnly: true
          type: string
        costCenter:
          $ref: '#/components/schemas/CostCenterForeignKey'
        id:
          readOnly: true
          type: integer
        name:
          type: string
        status:
          enum:
          - active
          - inactive
          type: string
        type:
          default: cmp
          enum:
          - cmp
          - iacp
          type: string
      required:
      - name
      x-createOnly:
      - type
      x-filterable:
      - id
      - name
      - added
      - status
      - type
      - costCenter
      x-usedIn:
      - /account/{accountId}/environments/
      - /account/{accountId}/environments/{envId}/
    RoleForeignKey:
      properties:
        id:
          type: integer
      required:
      - id
      x-derived:
        $ref: '#/components/schemas/Role'
      x-references:
        $ref: '#/components/schemas/Role'
    TagsCondition:
      properties:
        tags:
          description: Selects all the Servers that belong to the Role with at least one tag from this list.
          items:
            type: string
          type: array
        type:
          description: Orchestration rule condition type. Selected only those servers that match conditions.
          enum:
          - FarmRolesCondition
          - OsCondition
          - TagsCondition
          type: string
      required:
      - type
      - tags
      x-abstractType:
        $ref: '#/components/schemas/OrchestrationRuleCondition'
      x-discriminator: type
    AnsibleTowerConfigurationIdentity:
      properties:
        id:
          description: The Ansible Tower configuration identifier.
          maxLength: 36
          minLength: 36
          pattern: '[A-Fa-f0-9]{8}-([A-Fa-f0-9]{4}-){3}[A-Fa-f0-9]{12}'
          type: string
        name:
          description: The Ansible Tower configuration name.
          type: string
    FarmRolesCondition:
      properties:
        farmRoles:
          description: Farm Role identity objects.
          items:
            $ref: '#/components/schemas/FarmRoleIdentity'
          type: array
        type:
          description: Orchestration rule condition type. Selected only those servers that match conditions.
          enum:
          - FarmRolesCondition
          - OsCondition
          - TagsCondition
          type: string
      required:
      - type
      x-abstractType:
        $ref: '#/components/schemas/OrchestrationRuleCondition'
      x-discriminator: type
    ScriptIdentity:
      properties:
        id:
          type: integer
        name:
          description: The name of the Script.
          type: string
      required:
      - id
      x-derived:
        $ref: '#/components/schemas/Script'
      x-references:
        $ref: '#/components/schemas/Script'
    ScriptVariable:
      discriminator:
        propertyName: type
      properties:
        category:
          description: The category of this Variable.
          type: string
        description:
          description: A description that explains what this Global Variable is used for.
          type: string
        hidden:
          description: Whether this Script Variable is hidden in Orchestration Rules or execute Script action.
          type: boolean
        locked:
          description: Whether this Script Variable value is locked in Orchestration Rules or execute Script action.
          type: boolean
        name:
          description: The Variable name.
          type: string
        required:
          description: Whether this Script Variable value is required in Orchestration Rules or execute Script action.
          type: boolean
        type:
          description: The type of Script Variable.
          enum:
          - ScriptVariableJson
          - ScriptVariableString
          - ScriptVariableList
          - ScriptVariableRemoteList
          type: string
        value:
          description: The value declared in the script level. This property can be redefined for not locked variables in Orhestration Rules or execute Script action. Also this field supports Global Variable interpolation.
          type: string
      required:
      - name
      - type
      x-concreteTypes:
      - $ref: '#/components/schemas/ScriptVariableString'
      - $ref: '#/components/schemas/ScriptVariableJson'
      - $ref: '#/components/schemas/ScriptVariableList'
      - $ref: '#/components/schemas/ScriptVariableRemoteList'
      x-createOnly:
      - name
    OrchestrationRuleCondition:
      discriminator:
        propertyName: type
      properties:
        type:
          description: Orchestration rule condition type. Selected only those servers that match conditions.
          enum:
          - FarmRolesCondition
          - OsCondition
          - TagsCondition
          type: string
      required:
      - type
      x-concreteTypes:
      - $ref: '#/components/schemas/FarmRolesCondition'
      - $ref: '#/components/schemas/OsCondition'
      - $ref: '#/components/schemas/TagsCondition'
    RemoteScriptAction:
      properties:
        actionType:
          enum:
          - ScalrScriptAction
          - RemoteScriptAction
          - ChefAction
          - LocalScriptAction
          - AnsibleTowerAction
          type: string
        path:
          description: Path or URL to the script to execute.
          type: string
      required:
      - actionType
      - path
      x-abstractType:
        $ref: '#/components/schemas/Action'
      x-discriminator: actionType
    VmwareImageCloudFeatures:
      description: Cloud specifics Image configuration.
      properties:
        type:
          enum:
          - AwsImageCloudFeatures
          - VmwareImageCloudFeatures
          - OpenstackImageCloudFeatures
          type: string
        vCenterHost:
          description: The vCenter host where the current Image exists. At the Environment scope this property is read-only.
          type: string
      required:
      - type
      x-abstractType:
        $ref: '#/components/schemas/ImageCloudFeatures'
      x-discriminator: type
    ApiPagination:
      properties:
        first:
          readOnly: true
          type: string
        last:
          readOnly: true
          type: string
        next:
          readOnly: true
          type: string
        prev:
          readOnly: true
          type: string
    Event:
      properties:
        description:
          type: string
        id:
          description: The name of this Custom Event.
          maxLength: 25
          pattern: '[A-Za-z0-9]+'
          type: string
        scope:
          enum:
          - scalr
          - account
          - environment
          - farm
          - farmrole
          - role
          - server
          readOnly: true
          type: string
      required:
      - id
      x-createOnly:
      - id
      x-filterable:
      - id
      - scope
      x-usedIn:
      - /account/{accountId}/events/
      - /account/{accountId}/events/{eventId}/
    RoleImageListResponse:
      properties:
        data:
          items:
            $ref: '#/components/schemas/RoleImage'
          readOnly: true
          type: array
        errors:
          items:
            $ref: '#/components/schemas/ApiMessage'
          readOnly: true
          type: array
        meta:
          $ref: '#/components/schemas/ApiMetaContainer'
          readOnly: true
        pagination:
          $ref: '#/components/schemas/ApiPagination'
          readOnly: true
        warnings:
          items:
            $ref: '#/components/schemas/ApiMessage'
          readOnly: true
          type: array
      x-derived:
        $ref: '#/components/schemas/RoleImage'
      x-usedIn:
      - /account/{accountId}/roles/{roleId}/images/
    ApiErrorResponse:
      description: Common response for erroneous requests.
      properties:
        errors:
          items:
            $ref: '#/components/schemas/ApiMessage'
          readOnly: true
          type: array
        meta:
          $ref: '#/components/schemas/ApiMetaContainer'
          readOnly: true
        warnings:
          items:
            $ref: '#/components/schemas/ApiMessage'
          readOnly: true
          type: array
      x-usedIn:
      - /account/{accountId}/global-variables/
      - /account/{accountId}/global-variables/{globalVariableName}/
      - /account/{accountId}/orchestration-rules/
      - /account/{accountId}/orchestration-rules/{orchestrationRuleId}/
      - /account/{accountId}/cost-centers/
      - /account/{accountId}/cost-centers/{costCenterId}/
      - /account/{accountId}/environments/
      - /account/{accountId}/environments/{envId}/
      - /account/{accountId}/environments/{envId}/clouds/
      - /account/{accountId}/environments/{envId}/clouds/{cloud}/
      - /account/{accountId}/environments/{envId}/teams/
      - /account/{accountId}/environments/{envId}/teams/{teamId}/
      - /account/{accountId}/teams/
      - /account/{accountId}/teams/{teamId}/
      - /account/{accountId}/acl-roles/
      - /account/{accountId}/role-categories/
      - /account/{accountId}/role-categories/{roleCategoryId}/
      - /account/{accountId}/roles/
      - /account/{accountId}/roles/{roleId}/
      - /account/{accountId}/roles/{roleId}/actions/clone/
      - /account/{accountId}/roles/{roleId}/actions/deprecate/
      - /account/{accountId}/roles/{roleId}/images/
      - /account/{accountId}/roles/{roleId}/images/{imageId}/
      - /account/{accountId}/roles/{roleId}/images/{imageId}/actions/replace/
      - /account/{accountId}/roles/{roleId}/global-variables/
      - /account/{accountId}/roles/{roleId}/global-variables/{globalVariableName}/
      - /account/{accountId}/roles/{roleId}/orchestration-rules/
      - /account/{accountId}/roles/{roleId}/orchestration-rules/{orchestrationRuleId}/
      - /account/{accountId}/images/
      - /account/{accountId}/images/{imageId}/
      - /account/{accountId}/images/{imageId}/actions/copy/
      - /account/{accountId}/images/{imageId}/actions/replace/
      - /account/{accountId}/projects/
      - /account/{accountId}/projects/{projectId}/
      - /account/{accountId}/scripts/
      - /account/{accountId}/scripts/{scriptId}/
      - /account/{accountId}/scripts/{scriptId}/script-versions/
      - /account/{accountId}/scripts/{scriptId}/script-versions/{scriptVersionNumber}/
      - /account/{accountId}/webhook-endpoints/
      - /account/{accountId}/os/
      - /account/{accountId}/os/{osId}/
      - /account/{accountId}/events/
      - /account/{accountId}/events/{eventId}/
      - /account/{accountId}/cloud-credentials/
      - /account/{accountId}/cloud-credentials/{cloudCredentialsId}/
      - /account/{accountId}/scaling-metrics/
      - /account/{accountId}/scaling-metrics/{metricName}/
      - /account/{accountId}/users/
      - /account/{accountId}/ansible-tower/servers/
      - /account/{accountId}/ansible-tower/configurations/
      - /account/{accountId}/ansible-tower/configurations/{ansibleTowerConfigurationId}/
    OsForeignKey:
      properties:
        id:
          description: An ID uniquely identifying this OS in Scalr.
          type: string
      required:
      - id
      x-derived:
        $ref: '#/components/schemas/Os'
      x-references:
        $ref: '#/components/schemas/Os'
    Role:
      properties:
        builtinAutomation:
          items:
            enum:
            - base
            - chef
            - mysql
            - percona
            - postgresql
            - redis
            - apache
            - nginx
            - tomcat
            - haproxy
            - rabbitmq
            - memcached
            type: string
          type: array
        category:
          $ref: '#/components/schemas/RoleCategoryForeignKey'
        deprecated:
          $ref: '#/components/schemas/RoleDeprecatedOptions'
          description: The deprecation options associated with this Role.
          readOnly: true
        description:
          type: string
        id:
          readOnly: true
          type: integer
        name:
          type: string
        os:
          $ref: '#/components/schemas/OsForeignKey'
        quickStart:
          description: Whether this Role is a QuickStart Role.
          type: boolean
        quickStartGroup:
          description: The name of the QuickStart Group.
          type: string
        scope:
          enum:
          - scalr
          - account
          - environment
          - farm
          - farmrole
          - role
          - server
          readOnly: true
          type: string
        tags:
          description: The list of tags that should be applied to the Role.
          items:
            type: string
          type: array
        useScalrAgent:
          description: Whether this Role uses Scalarizr, the Scalr agent.
          type: boolean
      required:
      - name
      - category
      - os
      x-createOnly:
      - useScalrAgent
      - builtinAutomation
      - os
      x-filterable:
      - id
      - name
      - os
      - category
      - scope
      - quickStart
      - quickStartGroup
      - useScalrAgent
      - builtinAutomation
      x-usedIn:
      - /account/{accountId}/roles/{roleId}/
      - /account/{accountId}/roles/{roleId}/actions/clone/
      - /account/{accountId}/roles/{roleId}/actions/deprecate/
      - /account/{accountId}/roles/{roleId}/images/
      - /account/{accountId}/roles/{roleId}/images/{imageId}/
      - /account/{accountId}/roles/{roleId}/images/{imageId}/actions/replace/
      - /account/{accountId}/roles/{roleId}/global-variables/
      - /account/{accountId}/roles/{roleId}/global-variables/{globalVariableName}/
      - /account/{accountId}/roles/{roleId}/orchestration-rules/
      - /account/{accountId}/roles/{roleId}/orchestration-rules/{orchestrationRuleId}/
    OsCondition:
      properties:
        osType:
          description: OS family or type.
          type: string
        type:
          description: Orchestration rule condition type. Selected only those servers that match conditions.
          enum:
          - FarmRolesCondition
          - OsCondition
          - TagsCondition
          type: string
      required:
      - type
      - osType
      x-abstractType:
        $ref: '#/components/schemas/OrchestrationRuleCondition'
      x-discriminator: type
    ScriptVariableValue:
      properties:
        name:
          description: The Variable name.
          type: string
        value:
          description: The redefined value declared in the Script level.
          type: string
      required:
      - name
    ScriptVersionIdentity:
      properties:
        script:
          $ref: '#/components/schemas/ScriptIdentity'
        variables:
          description: The list of redefined script variables for the current script.
          items:
            $ref: '#/components/schemas/ScriptVariableValue'
          type: array
        version:
          description: The Script version. null is interpreted as the latest/default Version.
          type: string
    ApiMessage:
      properties:
        code:
          description: A machine-readable representation of the message.
          type: string
        message:
          description: A human-readable representation of the message.
          type: string
    Action:
      discriminator:
        propertyName: actionType
      properties:
        actionType:
          enum:
          - ScalrScriptAction
          - RemoteScriptAction
          - ChefAction
          - LocalScriptAction
          - AnsibleTowerAction
          type: string
      required:
      - actionType
      x-concreteTypes:
      - $ref: '#/components/schemas/AnsibleTowerAction'
      - $ref: '#/components/schemas/ScalrScriptAction'
      - $ref: '#/components/schemas/RemoteScriptAction'
      - $ref: '#/components/schemas/LocalScriptAction'
      - $ref: '#/components/schemas/ChefAction'
    WebhookEndpointForeignKey:
      properties:
        id:
          description: Webhook endpoint identifier.
          type: string
      required:
      - id
      x-derived:
        $ref: '#/components/schemas/WebhookEndpoint'
      x-references:
        $ref: '#/components/schemas/WebhookEndpoint'
    VariableAllowedValue:
      properties:
        label:
          description: The Global Variable Value label
          type: string
        value:
          description: The Global Variable value
          type: string
      required:
      - value
    Trigger:
      discriminator:
        propertyName: triggerType
      properties:
        triggerType:
          enum:
          - AllEventsTrigger
          - SpecificEventTrigger
          type: string
      required:
      - triggerType
      x-concreteTypes:
      - $ref: '#/components/schemas/AllEventsTrigger'
      - $ref: '#/components/schemas/SpecificEventTrigger'
    AllEventsTrigger:
      description: Triggers when any Event fires.
      properties:
        triggerType:
          enum:
          - AllEventsTrigger
          - SpecificEventTrigger
          type: string
      required:
      - triggerType
      x-abstractType:
        $ref: '#/components/schemas/Trigger'
      x-discriminator: triggerType
    GlobalVariableString:
      properties:
        category:
          description: The category of this Global Variable.
          type: string
        computedValue:
          description: The value computed by Scalr for this Global Variable.
          readOnly: true
          type: string
        declaredIn:
          description: The Scope in which this Global Variable is declared.
          enum:
          - scalr
          - account
          - environment
          - farm
          - farmrole
          - role
          - server
          readOnly: true
          type: string
        definedIn:
          description: The Scope in which this Global Variable is given a value.
          enum:
          - scalr
          - account
          - environment
          - farm
          - farmrole
          - role
          - server
          readOnly: true
          type: string
        description:
          description: A description that explains what this Global Variable is used for.
          type: string
        hidden:
          description: Whether this Global Variable is hidden in inner scopes. This is read-only if `declaredIn` is not the current scope.
          type: boolean
        locked:
          description: Whether this Global Variable is locked in inner scopes. This is read-only if `declaredIn` is not the current scope.
          type: boolean
        name:
          type: string
        outputFormat:
          description: The formatting to apply when displaying this variable. This is read-only if `declaredIn` is not the current scope.
          type: string
        requiredIn:
          description: The scope where this Global Variable is required. This is read-only if `declaredIn` is not the current scope.
          enum:
          - scalr
          - account
          - environment
          - farm
          - farmrole
          - role
          - server
          type: string
        type:
          description: The type of Global Variable.
          enum:
          - GlobalVariableJson
          - GlobalVariableString
          - GlobalVariableList
          - GlobalVariableRemoteList
          type: string
        validationPattern:
          type: string
        value:
          description: The value declared in the current scope for this Global Variable. If it is null, the value will be inherited from higher scopes. This field is ignored if `declaredIn` is not the current scope.
          type: string
      required:
      - name
      - type
      x-abstractType:
        $ref: '#/components/schemas/GlobalVariable'
      x-createOnly:
      - name
      x-discriminator: type
    Image:
      properties:
        added:
          description: Date and time at which this Image was created.
          format: date-time
          readOnly: true
          type: string
        architecture:
          enum:
          - i386
          - x86_64
          type: string
        cloudFeatures:
          $ref: '#/components/schemas/ImageCloudFeatures'
        cloudImageId:
          type: string
        cloudInitInstalled:
          description: Whether cloud-init is installed on this Image.
          type: boolean
        cloudLocation:
          description: 'Cloud Location, inside a given Cloud Platform, for this resource. May be null if this resource does not have a specific location. Note: if you would like to filter on this property, you **must** also filter on `cloudPlatform`.'
          type: string
        cloudPlatform:
          description: The Cloud Platform this resource resides in.
          enum:
          - ec2
          - gce
          type: string
        deprecated:
          description: Whether this Image is deprecated.
          type: boolean
        id:
          maxLength: 36
          minLength: 36
          pattern: '[A-Fa-f0-9]{8}-([A-Fa-f0-9]{4}-){3}[A-Fa-f0-9]{12}'
          readOnly: true
          type: string
        lastUsed:
          description: Date and time at which this Image was last used.
          format: date-time
          readOnly: true
          type: string
        name:
          type: string
        os:
          $ref: '#/components/schemas/OsForeignKey'
        scalrAgentInstalled:
          description: Whether Scalr Agent is installed on this Image.
          type: boolean
        scope:
          enum:
          - scalr
          - account
          - environment
          - farm
          - farmrole
          - role
          - server
          readOnly: true
          type: string
        size:
          description: Size of the image in GB. This property is read-only at the Environment scope.
          type: integer
        source:
          description: Indicates how this Image was created.
          enum:
          - Manual
          - BundleTask
          readOnly: true
          type: string
        status:
          description: Current status for this Image. `failed` means that the deletion failed.
          enum:
          - active
          - pending_delete
          - failed
          readOnly: true
          type: string
        statusError:
          description: If the Image is in a failed state, this indicates why.
          readOnly: true
          type: string
        type:
          description: (Only used on EC2 currently) Whether this Image is HVM or EBS. At the Environment scope this property is read-only. Property is deprecated and will be removed soon. Use 'cloudFeatures' instead.
          type: string
      required:
      - name
      - cloudPlatform
      - cloudImageId
      - architecture
      - os
      x-createOnly:
      - cloudImageId
      - size
      - type
      - scalrAgentInstalled
      - cloudInitInstalled
      - cloudLocation
      - cloudPlatform
      - os
      - architecture
      - cloudFeatures
      x-filterable:
      - id
      - name
      - scope
      - cloudPlatform
      - cloudLocation
      - cloudImageId
      - architecture
      - source
      - status
      - deprecated
      - os
      - scalrAgentInstalled
      - cloudInitInstalled
      x-usedIn:
      - /account/{accountId}/roles/{roleId}/images/{imageId}/
      - /account/{accountId}/roles/{roleId}/images/{imageId}/actions/replace/
      - /account/{accountId}/images/
      - /account/{accountId}/images/{imageId}/
      - /account/{accountId}/images/{imageId}/actions/copy/
      - /account/{accountId}/images/{imageId}/actions/replace/
    OrchestrationRuleListResponse:
      properties:
        data:
          items:
            $ref: '#/components/schemas/OrchestrationRule'
          readOnly: true
          type: array
        errors:
          items:
            $ref: '#/components/schemas/ApiMessage'
          readOnly: true
          type: array
        meta:
          $ref: '#/components/schemas/ApiMetaContainer'
          readOnly: true
        pagination:
          $ref: '#/components/schemas/ApiPagination'
          readOnly: true
        warnings:
          items:
            $ref: '#/components/schemas/ApiMessage'
          readOnly: true
          type: array
      x-derived:
        $ref: '#/components/schemas/OrchestrationRule'
      x-usedIn:
      - /account/{accountId}/orchestration-rules/
      - /account/{accountId}/roles/{roleId}/orchestration-rules/
    ApiMetaContainer:
      properties:
        sample:
          description: Actual properties TBD.
          type: string
    GlobalVariable:
      discriminator:
        propertyName: type
      properties:
        category:
          description: The category of this Global Variable.
          type: string
        computedValue:
          description: The value computed by Scalr for this Global Variable.
          readOnly: true
          type: string
        declaredIn:
          description: The Scope in which this Global Variable is declared.
          enum:
          - scalr
          - account
          - environment
          - farm
          - farmrole
          - role
          - server
          readOnly: true
          type: string
        definedIn:
          description: The Scope in which this Global Variable is given a value.
          enum:
          - scalr
          - account
          - environment
          - farm
          - farmrole
          - role
          - server
          readOnly: true
          type: string
        description:
          description: A description that explains what this Global Variable is used for.
          type: string
        hidden:
          description: Whether this Global Variable is hidden in inner scopes. This is read-only if `declaredIn` is not the current scope.
          type: boolean
        locked:
          description: Whether this Global Variable is locked in inner scopes. This is read-only if `declaredIn` is not the current scope.
          type: boolean
        name:
          type: string
        requiredIn:
          description: The scope where this Global Variable is required. This is read-only if `declaredIn` is not the current scope.
          enum:
          - scalr
          - account
          - environment
          - farm
          - farmrole
          - role
          - server
          type: string
        type:
          description: The type of Global Variable.
          enum:
          - GlobalVariableJson
          - GlobalVariableString
          - GlobalVariableList
          - GlobalVariableRemoteList
          type: string
        value:
          description: The value declared in the current scope for this Global Variable. If it is null, the value will be inherited from higher scopes. This field is ignored if `declaredIn` is not the current scope.
          type: string
      required:
      - name
      - type
      x-concreteTypes:
      - $ref: '#/components/schemas/GlobalVariableString'
      - $ref: '#/components/schemas/GlobalVariableJson'
      - $ref: '#/components/schemas/GlobalVariableList'
      - $ref: '#/components/schemas/GlobalVariableRemoteList'
      x-createOnly:
      - name
      x-usedIn:
      - /account/{accountId}/global-variables/
      - /account/{accountId}/global-variables/{globalVariableName}/
      - /account/{accountId}/roles/{roleId}/global-variables/
      - /account/{accountId}/roles/{roleId}/global-variables/{globalVariableName}/
    EventForeignKey_2:
      properties:
        id:
          description: The name of this custom event.
          type: string
      required:
      - id
      x-derived:
        $ref: '#/components/schemas/Event_2'
      x-references:
        $ref: '#/components/schemas/Event_2'
    GlobalVariableDetailsResponse_2:
      properties:
        data:
          $ref: '#/components/schemas/GlobalVariable_2'
          readOnly: true
        errors:
          items:
            $ref: '#/components/schemas/ApiMessage'
          readOnly: true
          type: array
        meta:
          $ref: '#/components/schemas/ApiMetaContainer'
          readOnly: true
        warnings:
          items:
            $ref: '#/components/schemas/ApiMessage'
          readOnly: true
          type: array
      x-derived:
        $ref: '#/components/schemas/GlobalVariable_2'
      x-usedIn:
      - /global/global-variables/
      - /global/global-variables/{globalVariableName}/
      - /global/roles/{roleId}/global-variables/
      - /global/roles/{roleId}/global-variables/{globalVariableName}/
    RoleImage_2:
      description: A Representation of a Role Image. Note that the `role` property is optional when POST-ing to a URL that already includes `{roleId}`.
      properties:
        image:
          $ref: '#/components/schemas/ImageForeignKey'
        role:
          $ref: '#/components/schemas/RoleForeignKey'
      required:
      - role
      - image
      x-filterable:
      - role
      - image
      x-usedIn:
      - /global/roles/{roleId}/images/
      - /global/roles/{roleId}/images/{imageId}/actions/replace/
    Script_2:
      properties:
        added:
          format: date-time
          readOnly: true
          type: string
        blockingDefault:
          description: Whether this Script is blocking by default. This can be overridden in the Orchestration Rules that use this Script.
          type: boolean
        deprecated:
          default: false
          description: Whether the Script is deprecated.
          type: boolean
        description:
          type: string
        id:
          readOnly: true
          type: integer
        lastChanged:
          format: date-time
          readOnly: true
          type: string
        name:
          description: The name of the Script.
          type: string
        osType:
          description: Type of OS this Script should execute on. This will influence Orchestration Rules, and will impact the validation rules applied to the ScriptVersions.
          enum:
          - linux
          - windows
          - cross-platform
          type: string
        requireAdminPrivileges:
          default: false
          description: Whether the Script requires root/Administrator privileges to be executed.
          type: boolean
        scope:
          enum:
          - scalr
          - account
          - environment
          - farm
          - farmrole
          - role
          - server
          readOnly: true
          type: string
        tags:
          description: The list of tags that should be applied to the Script.
          items:
            type: string
          type: array
        timeoutDefault:
          description: The time after which Scalr will abort the execution of this Script, in seconds. This can be overridden in the Orchestration Rules that use this Script.
          type: integer
      required:
      - name
      - osType
      x-createOnly:
      - osType
      x-filterable:
      - id
      - name
      - osType
      - blockingDefault
      - scope
      - deprecated
      - requireAdminPrivileges
    GlobalVariableListResponse_2:
      properties:
        data:
          items:
            $ref: '#/components/schemas/GlobalVariable_2'
          readOnly: true
          type: array
        errors:
          items:
            $ref: '#/components/schemas/ApiMessage'
          readOnly: true
          type: array
        meta:
          $ref: '#/components/schemas/ApiMetaContainer'
          readOnly: true
        pagination:
          $ref: '#/components/schemas/ApiPagination'
          readOnly: true
        warnings:
          items:
            $ref: '#/components/schemas/ApiMessage'
          readOnly: true
          type: array
      x-derived:
        $ref: '#/components/schemas/GlobalVariable_2'
      x-usedIn:
      - /global/global-variables/
      - /global/roles/{roleId}/global-variables/
    RoleCategory_2:
      properties:
        id:
          description: Identifier of the Role Category.
          readOnly: true
          type: integer
        name:
          description: The name of the Role Category.
          type: string
        scope:
          description: The Scope in which this Role Category is defined.
          enum:
          - scalr
          - account
          - environment
          - farm
          - farmrole
          - role
          - server
          readOnly: true
          type: string
      required:
      - name
      x-filterable:
      - id
      - name
      - scope
      x-usedIn:
      - /global/role-categories/
      - /global/role-categories/{roleCategoryId}/
    RoleDetailsResponse:
      properties:
        data:
          $ref: '#/components/schemas/Role_2'
          readOnly: true
        errors:
          items:
            $ref: '#/components/schemas/ApiMessage'
          readOnly: true
          type: array
        meta:
          $ref: '#/components/schemas/ApiMetaContainer'
          readOnly: true
        warnings:
          items:
            $ref: '#/components/schemas/ApiMessage'
          readOnly: true
          type: array
      x-derived:
        $ref: '#/components/schemas/Role_2'
      x-usedIn:
      - /global/roles/
      - /global/roles/{roleId}/
      - /global/roles/{roleId}/actions/deprecate/
    OrchestrationRuleDetailsResponse_2:
      properties:
        data:
          $ref: '#/components/schemas/OrchestrationRule_2'
          readOnly: true
        errors:
          items:
            $ref: '#/components/schemas/ApiMessage'
          readOnly: true
          type: array
        meta:
          $ref: '#/components/schemas/ApiMetaContainer'
          readOnly: true
        warnings:
          items:
            $ref: '#/components/schemas/ApiMessage'
          readOnly: true
          type: array
      x-derived:
        $ref: '#/components/schemas/OrchestrationRule_2'
      x-usedIn:
      - /global/roles/{roleId}/orchestration-rules/
      - /global/roles/{roleId}/orchestration-rules/{orchestrationRuleId}/
    RoleImageDetailsResponse_2:
      properties:
        data:
          $ref: '#/components/schemas/RoleImage_2'
          readOnly: true
        errors:
          items:
            $ref: '#/components/schemas/ApiMessage'
          readOnly: true
          type: array
        meta:
          $ref: '#/components/schemas/ApiMetaContainer'
          readOnly: true
        warnings:
          items:
            $ref: '#/components/schemas/ApiMessage'
          readOnly: true
          type: array
      x-derived:
        $ref: '#/components/schemas/RoleImage_2'
      x-usedIn:
      - /global/roles/{roleId}/images/
      - /global/roles/{roleId}/images/{imageId}/actions/replace/
    ImageDetailsResponse_2:
      properties:
        data:
          $ref: '#/components/schemas/Image_2'
          readOnly: true
        errors:
          items:
            $ref: '#/components/schemas/ApiMessage'
          readOnly: true
          type: array
        meta:
          $ref: '#/components/schemas/ApiMetaContainer'
          readOnly: true
        warnings:
          items:
            $ref: '#/components/schemas/ApiMessage'
          readOnly: true
          type: array
      x-derived:
        $ref: '#/components/schemas/Image_2'
      x-usedIn:
      - /global/roles/{roleId}/images/{imageId}/
      - /global/images/
      - /global/images/{imageId}/
      - /global/images/{imageId}/actions/copy/
    Os_2:
      properties:
        family:
          description: OS family.
          type: string
        generation:
          type: string
        id:
          description: An ID uniquely identifying this OS in Scalr.
          pattern: '[A-Za-z0-9-]+'
          type: string
        name:
          description: A human-readable name for this OS.
          type: string
        version:
          type: string
      required:
      - id
      - name
      - family
      - generation
      - version
      x-filterable:
      - id
      - name
      - family
      - generation
      x-usedIn:
      - /global/os/{osId}/
    OrchestrationRule_2:
      properties:
        action:
          $ref: '#/components/schemas/Action_2'
          description: Orchestration Rule action that will be executed on the target servers.
        blocking:
          description: Whether this Orchestration Rule's execution should delay the execution of further Orchestration Rules and the triggering of further Events. Defaults to the action's default when null.
          type: boolean
        enabled:
          default: true
          description: If Orchestration Rule is not enabled this Rule won't be triggered.
          type: boolean
        id:
          description: Unique identifier for an Orchestration Rule.
          maxLength: 36
          minLength: 36
          pattern: '[A-Fa-f0-9]{8}-([A-Fa-f0-9]{4}-){3}[A-Fa-f0-9]{12}'
          readOnly: true
          type: string
        order:
          description: The priority of this Orchestration Rule relative to other Orchestration Rules that use the same triggeringEvent. Default is relative to existing Rules.
          type: integer
        runAs:
          description: User the Orchestration Rule should execute as. Defaults to root / Administrator when null.
          type: string
        scope:
          enum:
          - scalr
          - account
          - environment
          - farm
          - farmrole
          - role
          - server
          readOnly: true
          type: string
        target:
          $ref: '#/components/schemas/Target'
          description: The set of Servers where the Orchestration Rules Action will be executed.
        timeout:
          description: The time Scalr should wait for before aborting the execution of this Orchestration Rule, in seconds. Defaults to the action's default when null.
          type: integer
        trigger:
          $ref: '#/components/schemas/Trigger'
          description: Orchestration Rule trigger.
      required:
      - trigger
      - action
      - target
      x-filterable:
      - id
      - blocking
      - order
      - runAs
      x-usedIn:
      - /global/roles/{roleId}/orchestration-rules/
      - /global/roles/{roleId}/orchestration-rules/{orchestrationRuleId}/
    Event_2:
      properties:
        description:
          type: string
        id:
          description: The name of this custom event.
          pattern: '[A-Za-z0-9]+'
          type: string
        scope:
          enum:
          - scalr
          - account
          - environment
          - farm
          - farmrole
          - role
          - server
          readOnly: true
          type: string
      required:
      - id
      x-createOnly:
      - id
      x-filterable:
      - id
      - scope
    RoleImageListResponse_2:
      properties:
        data:
          items:
            $ref: '#/components/schemas/RoleImage_2'
          readOnly: true
          type: array
        errors:
          items:
            $ref: '#/components/schemas/ApiMessage'
          readOnly: true
          type: array
        meta:
          $ref: '#/components/schemas/ApiMetaContainer'
          readOnly: true
        pagination:
          $ref: '#/components/schemas/ApiPagination'
          readOnly: true
        warnings:
          items:
            $ref: '#/components/schemas/ApiMessage'
          readOnly: true
          type: array
      x-derived:
        $ref: '#/components/schemas/RoleImage_2'
      x-usedIn:
      - /global/roles/{roleId}/images/
    Role_2:
      properties:
        builtinAutomation:
          items:
            enum:
            - base
            - chef
            - mysql
            - percona
            - postgresql
            - redis
            - apache
            - nginx
            - tomcat
            - haproxy
            - rabbitmq
            - memcached
            type: string
          type: array
        category:
          $ref: '#/components/schemas/RoleCategoryForeignKey'
        deprecated:
          $ref: '#/components/schemas/RoleDeprecatedOptions'
          description: The deprecation options associated with this Role.
          readOnly: true
        description:
          type: string
        id:
          readOnly: true
          type: integer
        name:
          type: string
        os:
          $ref: '#/components/schemas/OsForeignKey'
        quickStart:
          description: Whether this Role is a QuickStart Role.
          type: boolean
        quickStartGroup:
          description: The name of the QuickStart Group.
          type: string
        scope:
          enum:
          - scalr
          - account
          - environment
          - farm
          - farmrole
          - role
          - server
          readOnly: true
          type: string
        tags:
          description: The list of tags that should be applied to the Role.
          items:
            type: string
          type: array
        useScalrAgent:
          description: Whether this Role uses Scalarizr, the Scalr agent.
          type: boolean
      required:
      - name
      - category
      - os
      x-createOnly:
      - useScalrAgent
      - builtinAutomation
      - os
      x-filterable:
      - id
      - name
      - os
      - category
      - scope
      - quickStart
      - quickStartGroup
      - useScalrAgent
      - builtinAutomation
      x-usedIn:
      - /global/roles/
      - /global/roles/{roleId}/
      - /global/roles/{roleId}/actions/deprecate/
      - /global/roles/{roleId}/images/
      - /global/roles/{roleId}/images/{imageId}/
      - /global/roles/{roleId}/images/{imageId}/actions/replace/
      - /global/roles/{roleId}/global-variables/
      - /global/roles/{roleId}/global-variables/{globalVariableName}/
      - /global/roles/{roleId}/orchestration-rules/
      - /global/roles/{roleId}/orchestration-rules/{orchestrationRuleId}/
    RoleListResponse:
      properties:
        data:
          items:
            $ref: '#/components/schemas/Role_2'
          readOnly: true
          type: array
        errors:
          items:
            $ref: '#/components/schemas/ApiMessage'
          readOnly: true
          type: array
        meta:
          $ref: '#/components/schemas/ApiMetaContainer'
          readOnly: true
        pagination:
          $ref: '#/components/schemas/ApiPagination'
          readOnly: true
        warnings:
          items:
            $ref: '#/components/schemas/ApiMessage'
          readOnly: true
          type: array
      x-derived:
        $ref: '#/components/schemas/Role_2'
      x-usedIn:
      - /global/roles/
    Image_2:
      properties:
        added:
          description: Date and time at which this Image was created.
          format: date-time
          readOnly: true
          type: string
        architecture:
          enum:
          - i386
          - x86_64
          type: string
        cloudFeatures:
          $ref: '#/components/schemas/ImageCloudFeatures'
        cloudImageId:
          type: string
        cloudInitInstalled:
          description: Whether cloud-init is installed on this Image.
          type: boolean
        cloudLocation:
          description: 'Cloud Location, inside a given Cloud Platform, for this resource. May be null if this resource does not have a specific location. Note: if you would like to filter on this property, you **must** also filter on `cloudPlatform`.'
          type: string
        cloudPlatform:
          description: The Cloud Platform this resource resides in.
          enum:
          - ec2
          - gce
          - azure
          - cloudstack
          - idcf
          - openstack
          - ocs
          - otc
          - rackspacenguk
          - rackspacengus
          - hpcloud
          - mirantis
          - vio
          - cisco
          - vmware
          type: string
        deprecated:
          description: Whether this Image is deprecated.
          type: boolean
        id:
          maxLength: 36
          minLength: 36
          pattern: '[A-Fa-f0-9]{8}-([A-Fa-f0-9]{4}-){3}[A-Fa-f0-9]{12}'
          readOnly: true
          type: string
        lastUsed:
          description: Date and time at which this Image was last used.
          format: date-time
          readOnly: true
          type: string
        name:
          type: string
        os:
          $ref: '#/components/schemas/OsForeignKey'
        scalrAgentInstalled:
          description: Whether Scalr Agent is installed on this Image.
          type: boolean
        scope:
          enum:
          - scalr
          - account
          - environment
          - farm
          - farmrole
          - role
          - server
          readOnly: true
          type: string
        size:
          description: Size of the image in GB. This property is read-only at the Environment scope.
          type: integer
        source:
          description: Indicates how this Image was created.
          enum:
          - Manual
          - BundleTask
          readOnly: true
          type: string
        status:
          description: Current status for this Image. `failed` means that the deletion failed.
          enum:
          - active
          - pending_delete
          - failed
          readOnly: true
          type: string
        statusError:
          description: If the Image is in a failed state, this indicates why.
          readOnly: true
          type: string
        type:
          description: (Only used on EC2 currently) Whether this Image is HVM or EBS. At the Environment scope this property is read-only. Property is deprecated and will be removed soon. Use 'cloudFeatures' instead.
          type: string
      required:
      - name
      - cloudPlatform
      - cloudImageId
      - architecture
      - os
      x-createOnly:
      - cloudImageId
      - size
      - type
      - scalrAgentInstalled
      - cloudInitInstalled
      - cloudLocation
      - cloudPlatform
      - os
      - architecture
      - cloudFeatures
      x-filterable:
      - id
      - name
      - scope
      - cloudPlatform
      - cloudLocation
      - cloudImageId
      - architecture
      - source
      - status
      - deprecated
      - os
      - scalrAgentInstalled
      - cloudInitInstalled
      x-usedIn:
      - /global/roles/{roleId}/images/{imageId}/
      - /global/roles/{roleId}/images/{imageId}/actions/replace/
      - /global/images/
      - /global/images/{imageId}/
      - /global/images/{imageId}/actions/copy/
      - /global/images/{imageId}/actions/replace/
    Action_2:
      discriminator:
        propertyName: actionType
      properties:
        actionType:
          enum:
          - ScalrScriptAction
          - RemoteScriptAction
          - ChefAction
          - LocalScriptAction
          - AnsibleTowerAction
          type: string
      required:
      - actionType
      x-concreteTypes:
      - $ref: '#/components/schemas/ScalrScriptAction'
      - $ref: '#/components/schemas/RemoteScriptAction'
      - $ref: '#/components/schemas/LocalScriptAction'
      - $ref: '#/components/schemas/ChefAction'
      - $ref: '#/components/schemas/AnsibleTowerAction'
    ApiErrorResponse_2:
      description: Common response for erroneous requests.
      properties:
        errors:
          items:
            $ref: '#/components/schemas/ApiMessage'
          readOnly: true
          type: array
        meta:
          $ref: '#/components/schemas/ApiMetaContainer'
          readOnly: true
        warnings:
          items:
            $ref: '#/components/schemas/ApiMessage'
          readOnly: true
          type: array
      x-usedIn:
      - /global/global-variables/
      - /global/global-variables/{globalVariableName}/
      - /global/role-categories/
      - /global/role-categories/{roleCategoryId}/
      - /global/roles/
      - /global/roles/{roleId}/
      - /global/roles/{roleId}/actions/deprecate/
      - /global/roles/{roleId}/images/
      - /global/roles/{roleId}/images/{imageId}/
      - /global/roles/{roleId}/images/{imageId}/actions/replace/
      - /global/roles/{roleId}/global-variables/
      - /global/roles/{roleId}/global-variables/{globalVariableName}/
      - /global/roles/{roleId}/orchestration-rules/
      - /global/roles/{roleId}/orchestration-rules/{orchestrationRuleId}/
      - /global/images/
      - /global/images/{imageId}/
      - /global/images/{imageId}/actions/copy/
      - /global/images/{imageId}/actions/replace/
      - /global/os/
      - /global/os/{osId}/
      - /global/projects/
      - /global/projects/{projectId}/
      - /global/cloud-credentials/
      - /global/cloud-credentials/{cloudCredentialsId}/
      - /global/users/
      - /global/ansible-tower/servers/
      - /global/ansible-tower/configurations/
      - /global/ansible-tower/configurations/{ansibleTowerConfigurationId}/
    OrchestrationRuleListResponse_2:
      properties:
        data:
          items:
            $ref: '#/components/schemas/OrchestrationRule_2'
          readOnly: true
          type: array
        errors:
          items:
            $ref: '#/components/schemas/ApiMessage'
          readOnly: true
          type: array
        meta:
          $ref: '#/components/schemas/ApiMetaContainer'
          readOnly: true
        pagination:
          $ref: '#/components/schemas/ApiPagination'
          readOnly: true
        warnings:
          items:
            $ref: '#/components/schemas/ApiMessage'
          readOnly: true
          type: array
      x-derived:
        $ref: '#/components/schemas/OrchestrationRule_2'
      x-usedIn:
      - /global/roles/{roleId}/orchestration-rules/
    GlobalVariable_2:
      discriminator:
        propertyName: type
      properties:
        category:
          description: The category of this Global Variable.
          type: string
        computedValue:
          description: The value computed by Scalr for this Global Variable.
          readOnly: true
          type: string
        declaredIn:
          description: The Scope in which this Global Variable is declared.
          enum:
          - scalr
          - account
          - environment
          - farm
          - farmrole
          - role
          - server
          readOnly: true
          type: string
        definedIn:
          description: The Scope in which this Global Variable is given a value.
          enum:
          - scalr
          - account
          - environment
          - farm
          - farmrole
          - role
          - server
          readOnly: true
          type: string
        description:
          description: A description that explains what this Global Variable is used for.
          type: string
        hidden:
          description: Whether this Global Variable is hidden in inner scopes. This is read-only if `declaredIn` is not the current scope.
          type: boolean
        locked:
          description: Whether this Global Variable is locked in inner scopes. This is read-only if `declaredIn` is not the current scope.
          type: boolean
        name:
          type: string
        requiredIn:
          description: The scope where this Global Variable is required. This is read-only if `declaredIn` is not the current scope.
          enum:
          - scalr
          - account
          - environment
          - farm
          - farmrole
          - role
          - server
          type: string
        type:
          description: The type of Global Variable.
          enum:
          - GlobalVariableJson
          - GlobalVariableString
          - GlobalVariableList
          - GlobalVariableRemoteList
          type: string
        value:
          description: The value declared in the current scope for this Global Variable. If it is null, the value will be inherited from higher scopes. This field is ignored if `declaredIn` is not the current scope.
          type: string
      required:
      - name
      - type
      x-concreteTypes:
      - $ref: '#/components/schemas/GlobalVariableString'
      - $ref: '#/components/schemas/GlobalVariableJson'
      - $ref: '#/components/schemas/GlobalVariableList'
      - $ref: '#/components/schemas/GlobalVariableRemoteList'
      x-createOnly:
      - name
      x-usedIn:
      - /global/global-variables/
      - /global/global-variables/{globalVariableName}/
      - /global/roles/{roleId}/global-variables/
      - /global/roles/{roleId}/global-variables/{globalVariableName}/
    ScriptVariableList_2:
      properties:
        allowedValues:
          description: The list of values to which the Global Variable value must correspond.
          items:
            $ref: '#/components/schemas/VariableAllowedValue_2'
          type: array
        category:
          description: The category of this Variable.
          type: string
        description:
          description: A description that explains what this Global Variable is used for.
          type: string
        hidden:
          description: Whether this Script Variable is hidden in Orchestration Rules or execute Script action.
          type: boolean
        locked:
          description: Whether this Script Variable value is locked in Orchestration Rules or execute Script action.
          type: boolean
        name:
          description: The Variable Name
          type: string
        required:
          description: Whether this Script Variable value is required in Orchestration Rules or execute Script action.
          type: boolean
        type:
          description: The type of Script Variable.
          enum:
          - ScriptVariableJson
          - ScriptVariableString
          - ScriptVariableList
          - ScriptVariableRemoteList
          type: string
        value:
          description: The value declared in the script level. This property can be redefined for not locked variables in Orhestration Rules or execute Script action. Also this field supports Global Variable interpolation.
          type: string
      required:
      - name
      - type
      - allowedValues
      x-abstractType:
        $ref: '#/components/schemas/ScriptVariable_2'
      x-createOnly:
      - name
      x-discriminator: type
    GlobalVariableDetailsResponse_3:
      properties:
        data:
          $ref: '#/components/schemas/GlobalVariable_3'
          readOnly: true
        errors:
          items:
            $ref: '#/components/schemas/ApiMessage'
          readOnly: true
          type: array
        meta:
          $ref: '#/components/schemas/ApiMetaContainer'
          readOnly: true
        warnings:
          items:
            $ref: '#/components/schemas/ApiMessage'
          readOnly: true
          type: array
      x-derived:
        $ref: '#/components/schemas/GlobalVariable_3'
      x-usedIn:
      - /user/{envId}/global-variables/
      - /user/{envId}/global-variables/{globalVariableName}/
      - /user/{envId}/roles/{roleId}/global-variables/
      - /user/{envId}/roles/{roleId}/global-variables/{globalVariableName}/
      - /user/{envId}/servers/{serverId}/global-variables/
      - /user/{envId}/servers/{serverId}/global-variables/{globalVariableName}/
      - /user/{envId}/farms/{farmId}/global-variables/
      - /user/{envId}/farms/{farmId}/global-variables/{globalVariableName}/
      - /user/{envId}/farm-roles/{farmRoleId}/global-variables/
      - /user/{envId}/farm-roles/{farmRoleId}/global-variables/{globalVariableName}/
    ScriptVariableString_2:
      properties:
        category:
          description: The category of this Variable.
          type: string
        description:
          description: A description that explains what this Global Variable is used for.
          type: string
        hidden:
          description: Whether this Script Variable is hidden in Orchestration Rules or execute Script action.
          type: boolean
        locked:
          description: Whether this Script Variable value is locked in Orchestration Rules or execute Script action.
          type: boolean
        name:
          description: The Variable Name
          type: string
        outputFormat:
          description: The formatting to apply when displaying this variable.
          type: string
        required:
          description: Whether this Script Variable value is required in Orchestration Rules or execute Script action.
          type: boolean
        type:
          description: The type of Script Variable.
          enum:
          - ScriptVariableJson
          - ScriptVariableString
          - ScriptVariableList
          - ScriptVariableRemoteList
          type: string
        validationPattern:
          description: The validation regular expresion to validate the value.
          type: string
        value:
          description: The value declared in the script level. This property can be redefined for not locked variables in Orhestration Rules or execute Script action. Also this field supports Global Variable interpolation.
          type: string
      required:
      - name
      - type
      x-abstractType:
        $ref: '#/components/schemas/ScriptVariable_2'
      x-createOnly:
      - name
      x-discriminator: type
    ScriptVariableJson_2:
      properties:
        category:
          description: The category of this Variable.
          type: string
        description:
          description: A description that explains what this Global Variable is used for.
          type: string
        hidden:
          description: Whether this Script Variable is hidden in Orchestration Rules or execute Script action.
          type: boolean
        locked:
          description: Whether this Script Variable value is locked in Orchestration Rules or execute Script action.
          type: boolean
        name:
          description: The Variable Name
          type: string
        required:
          description: Whether this Script Variable value is required in Orchestration Rules or execute Script action.
          type: boolean
        type:
          description: The type of Script Variable.
          enum:
          - ScriptVariableJson
          - ScriptVariableString
          - ScriptVariableList
          - ScriptVariableRemoteList
          type: string
        value:
          description: The value declared in the script level. This property can be redefined for not locked variables in Orhestration Rules or execute Script action. Also this field supports Global Variable interpolation.
          type: string
      required:
      - name
      - type
      x-abstractType:
        $ref: '#/components/schemas/ScriptVariable_2'
      x-createOnly:
      - name
      x-discriminator: type
    RoleImage_3:
      description: A Representation of a Role Image. Note that the `role` property is optional when POST-ing to a URL that already includes `{roleId}`.
      properties:
        image:
          $ref: '#/components/schemas/ImageForeignKey'
        role:
          $ref: '#/components/schemas/RoleForeignKey'
      required:
      - role
      - image
      x-filterable:
      - role
      - image
      x-usedIn:
      - /user/{envId}/roles/{roleId}/images/
      - /user/{envId}/roles/{roleId}/images/{imageId}/actions/replace/
    Script_3:
      properties:
        added:
          format: date-time
          readOnly: true
          type: string
        blockingDefault:
          description: Whether this Script is blocking by default. This can be overridden in the Orchestration Rules that use this Script.
          type: boolean
        deprecated:
          default: false
          description: Whether the Script is deprecated.
          type: boolean
        description:
          type: string
        id:
          readOnly: true
          type: integer
        lastChanged:
          format: date-time
          readOnly: true
          type: string
        name:
          description: The name of the Script.
          type: string
        osType:
          description: Type of OS this Script should execute on. This will influence Orchestration Rules, and will impact the validation rules applied to the ScriptVersions.
          enum:
          - linux
          - windows
          - cross-platform
          type: string
        requireAdminPrivileges:
          default: false
          description: Whether the Script requires root/Administrator privileges to be executed.
          type: boolean
        scope:
          enum:
          - scalr
          - account
          - environment
          - farm
          - farmrole
          - role
          - server
          readOnly: true
          type: string
        tags:
          description: The list of tags that should be applied to the Script.
          items:
            type: string
          type: array
        timeoutDefault:
          description: The time after which Scalr will abort the execution of this Script, in seconds. This can be overridden in the Orchestration Rules that use this Script.
          type: integer
        variables:
          description: The list of Script variables for the current script.
          items:
            $ref: '#/components/schemas/ScriptVariable_2'
          type: array
      required:
      - name
      - osType
      x-createOnly:
      - osType
      x-filterable:
      - id
      - name
      - osType
      - blockingDefault
      - scope
      - deprecated
      - requireAdminPrivileges
      x-usedIn:
      - /user/{envId}/scripts/
      - /user/{envId}/scripts/{scriptId}/
      - /user/{envId}/scripts/{scriptId}/actions/execute/
      - /user/{envId}/scripts/{scriptId}/script-versions/
      - /user/{envId}/scripts/{scriptId}/script-versions/{scriptVersionNumber}/
      - /user/{envId}/scripts/{scriptId}/script-versions/{scriptVersionNumber}/actions/execute/
    GlobalVariableListResponse_3:
      properties:
        data:
          items:
            $ref: '#/components/schemas/GlobalVariable_3'
          readOnly: true
          type: array
        errors:
          items:
            $ref: '#/components/schemas/ApiMessage'
          readOnly: true
          type: array
        meta:
          $ref: '#/components/schemas/ApiMetaContainer'
          readOnly: true
        pagination:
          $ref: '#/components/schemas/ApiPagination'
          readOnly: true
        warnings:
          items:
            $ref: '#/components/schemas/ApiMessage'
          readOnly: true
          type: array
      x-derived:
        $ref: '#/components/schemas/GlobalVariable_3'
      x-usedIn:
      - /user/{envId}/global-variables/
      - /user/{envId}/roles/{roleId}/global-variables/
      - /user/{envId}/servers/{serverId}/global-variables/
      - /user/{envId}/farms/{farmId}/global-variables/
      - /user/{envId}/farm-roles/{farmRoleId}/global-variables/
    WebhookEndpoint_2:
      description: Webhook endpoint summary.
      properties:
        id:
          description: Webhook endpoint identifier.
          maxLength: 36
          minLength: 36
          pattern: '[A-Fa-f0-9]{8}-([A-Fa-f0-9]{4}-){3}[A-Fa-f0-9]{12}'
          type: string
        scope:
          enum:
          - scalr
          - account
          - environment
          - farm
          - farmrole
          - role
          - server
          type: string
        url:
          description: Endpoint URL.
          type: string
    RoleCategory_3:
      properties:
        id:
          description: Identifier of the Role Category.
          readOnly: true
          type: integer
        name:
          description: The name of the Role Category.
          type: string
        scope:
          description: The Scope in which this Role Category is defined.
          enum:
          - scalr
          - account
          - environment
          - farm
          - farmrole
          - role
          - server
          readOnly: true
          type: string
      required:
      - name
      x-filterable:
      - id
      - name
      - scope
      x-usedIn:
      - /user/{envId}/role-categories/
      - /user/{envId}/role-categories/{roleCategoryId}/
    RoleDetailsResponse_2:
      properties:
        data:
          $ref: '#/components/schemas/Role_3'
          readOnly: true
        errors:
          items:
            $ref: '#/components/schemas/ApiMessage'
          readOnly: true
          type: array
        meta:
          $ref: '#/components/schemas/ApiMetaContainer'
          readOnly: true
        warnings:
          items:
            $ref: '#/components/schemas/ApiMessage'
          readOnly: true
          type: array
      x-derived:
        $ref: '#/components/schemas/Role_3'
      x-usedIn:
      - /user/{envId}/roles/
      - /user/{envId}/roles/{roleId}/
      - /user/{envId}/roles/{roleId}/actions/promote/
      - /user/{envId}/roles/{roleId}/actions/clone/
      - /user/{envId}/roles/{roleId}/actions/deprecate/
    OrchestrationRuleDetailsResponse_3:
      properties:
        data:
          $ref: '#/components/schemas/OrchestrationRule_3'
          readOnly: true
        errors:
          items:
            $ref: '#/components/schemas/ApiMessage'
          readOnly: true
          type: array
        meta:
          $ref: '#/components/schemas/ApiMetaContainer'
          readOnly: true
        warnings:
          items:
            $ref: '#/components/schemas/ApiMessage'
          readOnly: true
          type: array
      x-derived:
        $ref: '#/components/schemas/OrchestrationRule_3'
      x-usedIn:
      - /user/{envId}/orchestration-rules/
      - /user/{envId}/orchestration-rules/{orchestrationRuleId}/
      - /user/{envId}/roles/{roleId}/orchestration-rules/
      - /user/{envId}/roles/{roleId}/orchestration-rules/{orchestrationRuleId}/
      - /user/{envId}/farm-roles/{farmRoleId}/orchestration-rules/
      - /user/{envId}/farm-roles/{farmRoleId}/orchestration-rules/{orchestrationRuleId}/
    RoleImageDetailsResponse_3:
      properties:
        data:
          $ref: '#/components/schemas/RoleImage_3'
          readOnly: true
        errors:
          items:
            $ref: '#/components/schemas/ApiMessage'
          readOnly: true
          type: array
        meta:
          $ref: '#/components/schemas/ApiMetaContainer'
          readOnly: true
        warnings:
          items:
            $ref: '#/components/schemas/ApiMessage'
          readOnly: true
          type: array
      x-derived:
        $ref: '#/components/schemas/RoleImage_3'
      x-usedIn:
      - /user/{envId}/roles/{roleId}/images/
      - /user/{envId}/roles/{roleId}/images/{imageId}/actions/replace/
    ImageDetailsResponse_3:
      properties:
        data:
          $ref: '#/components/schemas/Image_3'
          readOnly: true
        errors:
          items:
            $ref: '#/components/schemas/ApiMessage'
          readOnly: true
          type: array
        meta:
          $ref: '#/components/schemas/ApiMetaContainer'
          readOnly: true
        warnings:
          items:
            $ref: '#/components/schemas/ApiMessage'
          readOnly: true
          type: array
      x-derived:
        $ref: '#/components/schemas/Image_3'
      x-usedIn:
      - /user/{envId}/roles/{roleId}/images/{imageId}/
      - /user/{envId}/images/
      - /user/{envId}/images/{imageId}/
      - /user/{envId}/images/{imageId}/actions/copy/
    Os_3:
      properties:
        family:
          description: OS family.
          type: string
        generation:
          type: string
        id:
          description: An ID uniquely identifying this OS in Scalr.
          pattern: '[A-Za-z0-9-]+'
          type: string
        name:
          description: A human-readable name for this OS.
          type: string
        version:
          type: string
      required:
      - id
      - name
      - family
      - generation
      - version
      x-filterable:
      - id
      - name
      - family
      - generation
      x-usedIn:
      - /user/{envId}/os/{osId}/
    FarmRoleIdentity_2:
      properties:
        alias:
          description: The Alias for this Farm Role.
          type: string
        id:
          description: Unique identifier for this Farm Role.
          readOnly: true
          type: integer
      required:
      - alias
    SpecificEventTrigger_2:
      description: Triggers when a specific Event fires.
      properties:
        event:
          $ref: '#/components/schemas/EventForeignKey'
        triggerType:
          enum:
          - AllEventsTrigger
          - SpecificEventTrigger
          type: string
      required:
      - triggerType
      x-abstractType:
        $ref: '#/components/schemas/Trigger'
      x-discriminator: triggerType
    OrchestrationRule_3:
      properties:
        action:
          $ref: '#/components/schemas/Action_3'
          description: Orchestration Rule action that will be executed on the target servers.
        blocking:
          description: Whether this Orchestration Rule's execution should delay the execution of further Orchestration Rules and the triggering of further Events. Defaults to the action's default when null.
          type: boolean
        enabled:
          default: true
          description: If Orchestration Rule is not enabled this Rule won't be triggered
          type: boolean
        id:
          description: Unique identifier for an Orchestration Rule.
          maxLength: 36
          minLength: 36
          pattern: '[A-Fa-f0-9]{8}-([A-Fa-f0-9]{4}-){3}[A-Fa-f0-9]{12}'
          readOnly: true
          type: string
        order:
          description: The priority of this Orchestration Rule relative to other Orchestration Rules that use the same triggeringEvent. Default is relative to existing Rules.
          type: integer
        runAs:
          description: A name of the system user the Orchestration Rule should be executed as. Administrator / root is used by default.
          type: string
        scope:
          enum:
          - scalr
          - account
          - environment
          - farm
          - farmrole
          - role
          - server
          readOnly: true
          type: string
        target:
          $ref: '#/components/schemas/Target'
          description: The set of Servers where the Orchestration Rules Action will be executed.
        timeout:
          description: The time Scalr should wait for before aborting the execution of this Orchestration Rule, in seconds. Defaults to the action's default when null.
          type: integer
        trigger:
          $ref: '#/components/schemas/Trigger'
          description: Orchestration Rule trigger.
      required:
      - trigger
      - action
      - target
      x-filterable:
      - id
      - blocking
      - order
      - runAs
      x-usedIn:
      - /user/{envId}/orchestration-rules/
      - /user/{envId}/orchestration-rules/{orchestrationRuleId}/
      - /user/{envId}/roles/{roleId}/orchestration-rules/
      - /user/{envId}/roles/{roleId}/orchestration-rules/{orchestrationRuleId}/
      - /user/{envId}/farm-roles/{farmRoleId}/orchestration-rules/
      - /user/{envId}/farm-roles/{farmRoleId}/orchestration-rules/{orchestrationRuleId}/
    ScriptVariableRemoteList_2:
      properties:
        category:
          description: The category of this Variable.
          type: string
        description:
          description: A description that explains what this Global Variable is used for.
          type: string
        hidden:
          description: Whether this Script Variable is hidden in Orchestration Rules or execute Script action.
          type: boolean
        integrationEndpoint:
          $ref: '#/components/schemas/WebhookEndpointForeignKey'
        locked:
          description: Whether this Script Variable value is locked in Orchestration Rules or execute Script action.
          type: boolean
        name:
          description: The Variable Name
          type: string
        required:
          description: Whether this Script Variable value is required in Orchestration Rules or execute Script action.
          type: boolean
        type:
          description: The type of Script Variable.
          enum:
          - ScriptVariableJson
          - ScriptVariableString
          - ScriptVariableList
          - ScriptVariableRemoteList
          type: string
        value:
          description: The value declared in the script level. This property can be redefined for not locked variables in Orhestration Rules or execute Script action. Also this field supports Global Variable interpolation.
          type: string
      required:
      - name
      - type
      - integrationEndpoint
      x-abstractType:
        $ref: '#/components/schemas/ScriptVariable_2'
      x-createOnly:
      - name
      x-discriminator: type
    ScriptVariable_2:
      discriminator:
        propertyName: type
      properties:
        category:
          description: The category of this Variable.
          type: string
        description:
          description: A description that explains what this Global Variable is used for.
          type: string
        hidden:
          description: Whether this Script Variable is hidden in Orchestration Rules or execute Script action.
          type: boolean
        locked:
          description: Whether this Script Variable value is locked in Orchestration Rules or execute Script action.
          type: boolean
        name:
          description: The Variable Name
          type: string
        required:
          description: Whether this Script Variable value is required in Orchestration Rules or execute Script action.
          type: boolean
        type:
          description: The type of Script Variable.
          enum:
          - ScriptVariableJson
          - ScriptVariableString
          - ScriptVariableList
          - ScriptVariableRemoteList
          type: string
        value:
          description: The value declared in the script level. This property can be redefined for not locked variables in Orhestration Rules or execute Script action. Also this field supports Global Variable interpolation.
          type: string
      required:
      - name
      - type
      x-concreteTypes:
      - $ref: '#/components/schemas/ScriptVariableString_2'
      - $ref: '#/components/schemas/ScriptVariableJson_2'
      - $ref: '#/components/schemas/ScriptVariableList_2'
      - $ref: '#/components/schemas/ScriptVariableRemoteList_2'
      x-createOnly:
      - name
    RemoteScriptAction_2:
      properties:
        actionType:
          enum:
          - ScalrScriptAction
          - RemoteScriptAction
          - ChefAction
          - LocalScriptAction
          - AnsibleTowerAction
          type: string
        path:
          description: URL to the script to execute.
          type: string
      required:
      - actionType
      - path
      x-abstractType:
        $ref: '#/components/schemas/Action_3'
      x-discriminator: actionType
    Event_3:
      properties:
        description:
          type: string
        id:
          description: The name of this Custom Event.
          maxLength: 25
          pattern: '[A-Za-z0-9]+'
          type: string
        scope:
          enum:
          - scalr
          - account
          - environment
          - farm
          - farmrole
          - role
          - server
          readOnly: true
          type: string
      required:
      - id
      x-createOnly:
      - id
      x-filterable:
      - id
      - scope
      x-usedIn:
      - /user/{envId}/events/
      - /user/{envId}/events/{eventId}/
      - /user/{envId}/events/{eventId}/actions/fire/
    RoleImageListResponse_3:
      properties:
        data:
          items:
            $ref: '#/components/schemas/RoleImage_3'
          readOnly: true
          type: array
        errors:
          items:
            $ref: '#/components/schemas/ApiMessage'
          readOnly: true
          type: array
        meta:
          $ref: '#/components/schemas/ApiMetaContainer'
          readOnly: true
        pagination:
          $ref: '#/components/schemas/ApiPagination'
          readOnly: true
        warnings:
          items:
            $ref: '#/components/schemas/ApiMessage'
          readOnly: true
          type: array
      x-derived:
        $ref: '#/components/schemas/RoleImage_3'
      x-usedIn:
      - /user/{envId}/roles/{roleId}/images/
    Role_3:
      properties:
        builtinAutomation:
          items:
            enum:
            - base
            - chef
            - mysql
            - percona
            - postgresql
            - redis
            - apache
            - nginx
            - tomcat
            - haproxy
            - rabbitmq
            - memcached
            type: string
          type: array
        category:
          $ref: '#/components/schemas/RoleCategoryForeignKey'
        deprecated:
          $ref: '#/components/schemas/RoleDeprecatedOptions'
          description: The deprecation options associated with this Role.
          readOnly: true
        description:
          type: string
        id:
          readOnly: true
          type: integer
        name:
          description: The name of the Role.
          type: string
        os:
          $ref: '#/components/schemas/OsForeignKey'
        quickStart:
          description: Whether this Role is a QuickStart Role.
          type: boolean
        quickStartGroup:
          description: The name of the QuickStart Group.
          type: string
        scope:
          enum:
          - scalr
          - account
          - environment
          - farm
          - farmrole
          - role
          - server
          readOnly: true
          type: string
        tags:
          description: The list of tags that should be applied to the Role.
          items:
            type: string
          type: array
        useScalrAgent:
          description: Whether this Role uses Scalarizr, the Scalr agent.
          type: boolean
      required:
      - name
      - category
      - os
      x-createOnly:
      - useScalrAgent
      - builtinAutomation
      - os
      x-filterable:
      - id
      - name
      - os
      - category
      - scope
      - quickStart
      - quickStartGroup
      - useScalrAgent
      - builtinAutomation
      x-usedIn:
      - /user/{envId}/roles/
      - /user/{envId}/roles/{roleId}/
      - /user/{envId}/roles/{roleId}/actions/promote/
      - /user/{envId}/roles/{roleId}/actions/clone/
      - /user/{envId}/roles/{roleId}/actions/deprecate/
      - /user/{envId}/roles/{roleId}/images/
      - /user/{envId}/roles/{roleId}/images/{imageId}/
      - /user/{envId}/roles/{roleId}/images/{imageId}/actions/replace/
      - /user/{envId}/roles/{roleId}/global-variables/
      - /user/{envId}/roles/{roleId}/global-variables/{globalVariableName}/
      - /user/{envId}/roles/{roleId}/orchestration-rules/
      - /user/{envId}/roles/{roleId}/orchestration-rules/{orchestrationRuleId}/
    RoleListResponse_2:
      properties:
        data:
          items:
            $ref: '#/components/schemas/Role_3'
          readOnly: true
          type: array
        errors:
          items:
            $ref: '#/components/schemas/ApiMessage'
          readOnly: true
          type: array
        meta:
          $ref: '#/components/schemas/ApiMetaContainer'
          readOnly: true
        pagination:
          $ref: '#/components/schemas/ApiPagination'
          readOnly: true
        warnings:
          items:
            $ref: '#/components/schemas/ApiMessage'
          readOnly: true
          type: array
      x-derived:
        $ref: '#/components/schemas/Role_3'
      x-usedIn:
      - /user/{envId}/roles/
    Image_3:
      properties:
        added:
          description: Date and time at which this Image was created.
          format: date-time
          readOnly: true
          type: string
        architecture:
          enum:
          - i386
          - x86_64
          type: string
        cloudFeatures:
          $ref: '#/components/schemas/ImageCloudFeatures'
        cloudImageId:
          type: string
        cloudInitInstalled:
          description: Whether cloud-init is installed on this Image.
          type: boolean
        cloudLocation:
          description: 'Cloud Location, inside a given Cloud Platform, for this resource. May be null if this resource does not have a specific location. Note: if you would like to filter on this property, you **must** also filter on `cloudPlatform`.'
          type: string
        cloudPlatform:
          description: The Cloud Platform this resource resides in.
          enum:
          - ec2
          - gce
          type: string
        deprecated:
          description: Whether this Image is deprecated.
          type: boolean
        id:
          maxLength: 36
          minLength: 36
          pattern: '[A-Fa-f0-9]{8}-([A-Fa-f0-9]{4}-){3}[A-Fa-f0-9]{12}'
          readOnly: true
          type: string
        lastUsed:
          description: Date and time at which this Image was last used.
          format: date-time
          readOnly: true
          type: string
        name:
          type: string
        os:
          $ref: '#/components/schemas/OsForeignKey'
        scalrAgentInstalled:
          description: Whether Scalr Agent is installed on this Image.
          type: boolean
        scope:
          enum:
          - scalr
          - account
          - environment
          - farm
          - farmrole
          - role
          - server
          readOnly: true
          type: string
        size:
          description: Size of the image in GB. This property is read-only at the Environment scope.
          type: integer
        source:
          description: Indicates how this Image was created.
          enum:
          - Manual
          - BundleTask
          readOnly: true
          type: string
        status:
          description: Current status for this Image. `failed` means that the deletion failed.
          enum:
          - active
          - pending_delete
          - failed
          readOnly: true
          type: string
        statusError:
          description: If the Image is in a failed state, this indicates why.
          readOnly: true
          type: string
        type:
          description: (Only used on EC2 currently) Whether this Image is HVM or EBS. At the Environment scope this property is read-only. Property is deprecated and will be removed soon. Use 'cloudFeatures' instead.
          type: string
      required:
      - name
      - cloudPlatform
      - cloudImageId
      - architecture
      - os
      x-createOnly:
      - cloudImageId
      - size
      - type
      - scalrAgentInstalled
      - cloudInitInstalled
      - cloudLocation
      - cloudPlatform
      - os
      - architecture
      - cloudFeatures
      x-filterable:
      - id
      - name
      - scope
      - cloudPlatform
      - cloudLocation
      - cloudImageId
      - architecture
      - source
      - status
      - deprecated
      - os
      - scalrAgentInstalled
      - cloudInitInstalled
      x-usedIn:
      - /user/{envId}/roles/{roleId}/images/{imageId}/
      - /user/{envId}/roles/{roleId}/images/{imageId}/actions/replace/
      - /user/{envId}/images/
      - /user/{envId}/images/{imageId}/
      - /user/{envId}/images/{imageId}/actions/copy/
      - /user/{envId}/images/{imageId}/actions/replace/
    Action_3:
      discriminator:
        propertyName: actionType
      properties:
        actionType:
          enum:
          - ScalrScriptAction
          - RemoteScriptAction
          - ChefAction
          - LocalScriptAction
          - AnsibleTowerAction
          type: string
      required:
      - actionType
      x-concreteTypes:
      - $ref: '#/components/schemas/ScalrScriptAction'
      - $ref: '#/components/schemas/RemoteScriptAction_2'
      - $ref: '#/components/schemas/LocalScriptAction'
      - $ref: '#/components/schemas/ChefAction'
      - $ref: '#/components/schemas/AnsibleTowerAction'
    VariableAllowedValue_2:
      properties:
        label:
          description: The Variable Value label.
          type: string
        value:
          description: The Variable value.
          type: string
      required:
      - value
    ScriptVersionIdentity_2:
      properties:
        script:
          $ref: '#/components/schemas/ScriptIdentity'
        variables:
          description: The list of redefined script variables for the current script.
          items:
            $ref: '#/components/schemas/ScriptVariableValue'
          type: array
        version:
          description: The Script version number. null is interpreted as the latest Version.
          type: string
    ApiErrorResponse_3:
      description: Common response for erroneous requests.
      properties:
        errors:
          items:
            $ref: '#/components/schemas/ApiMessage'
          readOnly: true
          type: array
        meta:
          $ref: '#/components/schemas/ApiMetaContainer'
          readOnly: true
        warnings:
          items:
            $ref: '#/components/schemas/ApiMessage'
          readOnly: true
          type: array
      x-usedIn:
      - /user/{envId}/global-variables/
      - /user/{envId}/global-variables/{globalVariableName}/
      - /user/{envId}/orchestration-rules/
      - /user/{envId}/orchestration-rules/{orchestrationRuleId}/
      - /user/{envId}/projects/
      - /user/{envId}/projects/{projectId}/
      - /user/{envId}/cost-centers/
      - /user/{envId}/cost-centers/{costCenterId}/
      - /user/{envId}/role-categories/
      - /user/{envId}/role-categories/{roleCategoryId}/
      - /user/{envId}/roles/
      - /user/{envId}/roles/{roleId}/
      - /user/{envId}/roles/{roleId}/actions/promote/
      - /user/{envId}/roles/{roleId}/actions/clone/
      - /user/{envId}/roles/{roleId}/actions/deprecate/
      - /user/{envId}/roles/{roleId}/images/
      - /user/{envId}/roles/{roleId}/images/{imageId}/
      - /user/{envId}/roles/{roleId}/images/{imageId}/actions/replace/
      - /user/{envId}/roles/{roleId}/global-variables/
      - /user/{envId}/roles/{roleId}/global-variables/{globalVariableName}/
      - /user/{envId}/roles/{roleId}/orchestration-rules/
      - /user/{envId}/roles/{roleId}/orchestration-rules/{orchestrationRuleId}/
      - /user/{envId}/images/
      - /user/{envId}/images/{imageId}/
      - /user/{envId}/images/{imageId}/actions/copy/
      - /user/{envId}/images/{imageId}/actions/replace/
      - /user/{envId}/servers/
      - /user/{envId}/servers/{serverId}/
      - /user/{envId}/servers/{serverId}/actions/suspend/
      - /user/{envId}/servers/{serverId}/actions/terminate/
      - /user/{envId}/servers/{serverId}/actions/resume/
      - /user/{envId}/servers/{serverId}/actions/reboot/
      - /user/{envId}/servers/{serverId}/actions/sync/
      - /user/{envId}/servers/{serverId}/global-variables/
      - /user/{envId}/servers/{serverId}/global-variables/{globalVariableName}/
      - /user/{envId}/farms/{farmId}/servers/
      - /user/{envId}/farm-roles/{farmRoleId}/servers/
      - /user/{envId}/scripts/
      - /user/{envId}/scripts/{scriptId}/
      - /user/{envId}/scripts/{scriptId}/actions/execute/
      - /user/{envId}/scripts/{scriptId}/script-versions/
      - /user/{envId}/scripts/{scriptId}/script-versions/{scriptVersionNumber}/
      - /user/{envId}/scripts/{scriptId}/script-versions/{scriptVersionNumber}/actions/execute/
      - /user/{envId}/script-executions/{scriptExecutionId}/
      - /user/{envId}/orchestration-logs/
      - /user/{envId}/orchestration-logs/{logEntryId}/
      - /user/{envId}/event-logs/
      - /user/{envId}/event-logs/{eventLogEntryId}/
      - /user/{envId}/webhook-endpoints/
      - /user/{envId}/os/
      - /user/{envId}/os/{osId}/
      - /user/{envId}/events/
      - /user/{envId}/events/{eventId}/
      - /user/{envId}/events/{eventId}/actions/fire/
      - /user/{envId}/farms/
      - /user/{envId}/farms/{farmId}/
      - /user/{envId}/farms/{farmId}/cloud-services/
      - /user/{envId}/farms/{farmId}/cloud-services/{farmCloudServiceLinkId}/
      - /user/{envId}/farms/{farmId}/actions/generate-template/
      - /user/{envId}/farms/{farmId}/actions/launch/
      - /user/{envId}/farms/{farmId}/actions/suspend/
      - /user/{envId}/farms/{farmId}/actions/resume/
      - /user/{envId}/farms/{farmId}/actions/terminate/
      - /user/{envId}/farms/{farmId}/actions/lock/
      - /user/{envId}/farms/{farmId}/actions/unlock/
      - /user/{envId}/farms/{farmId}/actions/clone/
      - /user/{envId}/farms/{farmId}/global-variables/
      - /user/{envId}/farms/{farmId}/global-variables/{globalVariableName}/
      - /user/{envId}/farms/{farmId}/farm-roles/
      - /user/{envId}/farm-roles/{farmRoleId}/
      - /user/{envId}/farm-roles/{farmRoleId}/actions/import-server/
      - /user/{envId}/farm-roles/{farmRoleId}/actions/generate-template/
      - /user/{envId}/farm-roles/{farmRoleId}/actions/clone/
      - /user/{envId}/farm-roles/{farmRoleId}/scaling/
      - /user/{envId}/farm-roles/{farmRoleId}/scaling/{metricName}/
      - /user/{envId}/farm-roles/{farmRoleId}/storage/
      - /user/{envId}/farm-roles/{farmRoleId}/storage/{storageConfigurationId}/
      - /user/{envId}/scaling-metrics/
      - /user/{envId}/scaling-metrics/{metricName}/
      - /user/{envId}/farm-roles/{farmRoleId}/global-variables/
      - /user/{envId}/farm-roles/{farmRoleId}/global-variables/{globalVariableName}/
      - /user/{envId}/farm-roles/{farmRoleId}/orchestration-rules/
      - /user/{envId}/farm-roles/{farmRoleId}/orchestration-rules/{orchestrationRuleId}/
      - /user/{envId}/clouds/{cloudPlatform}/cloud-locations/
      - /user/{envId}/clouds/{cloudPlatform}/cloud-locations/{cloudLocation}/instance-types/
    OrchestrationRuleListResponse_3:
      properties:
        data:
          items:
            $ref: '#/components/schemas/OrchestrationRule_3'
          readOnly: true
          type: array
        errors:
          items:
            $ref: '#/components/schemas/ApiMessage'
          readOnly: true
          type: array
        meta:
          $ref: '#/components/schemas/ApiMetaContainer'
          readOnly: true
        pagination:
          $ref: '#/components/schemas/ApiPagination'
          readOnly: true
        warnings:
          items:
            $ref: '#/components/schemas/ApiMessage'
          readOnly: true
          type: array
      x-derived:
        $ref: '#/components/schemas/OrchestrationRule_3'
      x-usedIn:
      - /user/{envId}/orchestration-rules/
      - /user/{envId}/roles/{roleId}/orchestration-rules/
      - /user/{envId}/farm-roles/{farmRoleId}/orchestration-rules/
    GlobalVariable_3:
      discriminator:
        propertyName: type
      properties:
        category:
          description: The category of this Global Variable.
          type: string
        computedValue:
          description: The value computed by Scalr for this Global Variable.
          readOnly: true
          type: string
        declaredIn:
          description: The Scope in which this Global Variable is declared.
          enum:
          - scalr
          - account
          - environment
          - farm
          - farmrole
          - role
          - server
          readOnly: true
          type: string
        definedIn:
          description: The Scope in which this Global Variable is given a value.
          enum:
          - scalr
          - account
          - environment
          - farm
          - farmrole
          - role
          - server
          readOnly: true
          type: string
        description:
          description: A description that explains what this Global Variable is used for.
          type: string
        hidden:
          description: Whether this Global Variable is hidden in inner scopes. This is read-only if `declaredIn` is not the current scope.
          type: boolean
        locked:
          description: Whether this Global Variable is locked in inner scopes. This is read-only if `declaredIn` is not the current scope.
          type: boolean
        name:
          type: string
        requiredIn:
          description: The scope where this Global Variable is required. This is read-only if `declaredIn` is not the current scope.
          enum:
          - scalr
          - account
          - environment
          - farm
          - farmrole
          - role
          - server
          type: string
        type:
          description: The type of Global Variable.
          enum:
          - GlobalVariableJson
          - GlobalVariableString
          - GlobalVariableList
          - GlobalVariableRemoteList
          type: string
        value:
          description: The value declared in the current scope for this Global Variable. If it is null, the value will be inherited from higher scopes. This field is ignored if `declaredIn` is not the current scope.
          type: string
      required:
      - name
      - type
      x-concreteTypes:
      - $ref: '#/components/schemas/GlobalVariableString'
      - $ref: '#/components/schemas/GlobalVariableJson'
      - $ref: '#/components/schemas/GlobalVariableList'
      - $ref: '#/components/schemas/GlobalVariableRemoteList'
      x-createOnly:
      - name
      x-usedIn:
      - /user/{envId}/global-variables/
      - /user/{envId}/global-variables/{globalVariableName}/
      - /user/{envId}/roles/{roleId}/global-variables/
      - /user/{envId}/roles/{roleId}/global-variables/{globalVariableName}/
      - /user/{envId}/servers/{serverId}/global-variables/
      - /user/{envId}/servers/{serverId}/global-variables/{globalVariableName}/
      - /user/{envId}/farms/{farmId}/global-variables/
      - /user/{envId}/farms/{farmId}/global-variables/{globalVariableName}/
      - /user/{envId}/farm-roles/{farmRoleId}/global-variables/
      - /user/{envId}/farm-roles/{farmRoleId}/global-variables/{globalVariableName}/
x-refined-from:
- scalr-account-openapi.yml
- scalr-global-openapi.yml
- scalr-user-openapi.yml
x-dry-yaml-definitions:
  architecture:
    enum:
    - i386
    - x86_64
    type: string
  cloudLocationProperties:
    cloudLocation:
      description: 'Cloud Location, inside a given Cloud Platform, for this resource. May be null if this resource does not have a specific location. Note: if you would like to filter on this property, you **must** also filter on `cloudPlatform`.'
      type: string
    cloudPlatform:
      description: The Cloud Platform this resource resides in.
      enum:
      - ec2
      - gce
      type: string
  cloudstackProvider:
    description: Cloud provider that should be specified for CloudStack based clouds.
    enum:
    - cloudstack
    - idcf
    type: string
  openstackProvider:
    description: Cloud provider that should be specified for OpenStack based clouds.
    enum:
    - openstack
    - ocs
    - hpcloud
    - mirantis
    - vio
    - cisco
    type: string
  pathParamsPatterns:
    uuid:
      maxLength: 36
      minLength: 36
      pattern: '[A-Fa-f0-9]{8}-([A-Fa-f0-9]{4}-){3}[A-Fa-f0-9]{12}'
    uuidShort:
      maxLength: 12
      minLength: 12
      pattern: '[A-Fa-f0-9]+'
  scopeProperty:
    enum:
    - scalr
    - account
    - environment
    - farm
    - farmrole
    - role
    - server
    type: string
